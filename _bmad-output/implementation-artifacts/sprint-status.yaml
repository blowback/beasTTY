# generated: 2026-07-01
# last_updated: 2026-07-22
# project: beastty
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones
#
# DEPENDENCY ORDER (from EPIC-SPLIT.md — informational, not enforced by status):
# ==============================================================================
#   E0 → E1 → {E2, E3, E5, E6};  E4 depends on E1 + E2;  E7 depends on E0.
#   E1 is the backbone and must land before the other menu/modal epics.

generated: 2026-07-01
last_updated: 2026-08-06  # E11-4 -> DONE. Code review recorded in the story file (E8 retro action #1 - filled at write time, not backfilled): 5 findings, 2 fixed and committed in d570904, 3 carried as follow-ups. FIXED: (1) maybeExitSendMode exits on STATE_CANCEL_PEND, so the first inbound chunk after slide.cancel() - an in-flight ACK, or slide.asm's msg_cancelled text - flipped mode to 'terminal' and the peer's CTRL_CAN echo then routed to dispatchTerminalMode instead of the state machine, timing out the new Step 3 wait and force_idling a cancel the Z80 had answered correctly (it also printed the 'Sent N files' summary on a cancelled transfer); it now returns early while sendCancelInFlight is set, so the cancel sequence's own Step 5 forceExitSendMode owns the exit. maybeExitRecvMode has no CancelPending arm, which is why only the send side lost its echo - the THIRD instance of the recv-only-predicate asymmetry (after keyboard.js Esc and chrome.js isSlideActiveRef). (2) lastCancelEchoArrived was never reset at cancel entry, so a cancel that threw before Step 3 left the previous cancel's true standing. CARRIED: (3) main.js:458 pagehide teardown never fires for SEND sessions - wireChrome gets the recv-only isSlideActive while main.js:667 already uses isSlideActive() || getWireOwner()==='slide' for exactly this reason; pre-dates the story but AC-6 made pagehide the sole hide-time trigger, so this is now the only remaining protection and it has a hole. (4) the bot's new recv-role CTRL_CAN echo has no injectNoEchoOnCancel equivalent, so cancelSlideSend's !echoArrived -> force_idle branch and its 2 s hatch are unreachable in any spec (the recv mirror IS covered at slide-cancel.spec.js:117). (5) abandonPendingStateWait is wired into forceExitRecvMode but not exitRecvMode - a mid-cancel STATE_ERROR burns the full 500 ms; latency only. Suite 735 passed / 1 skipped. Still NOT hardware-verified (the clamp is simulated) - carry a checkpoint before E11 ships. Next: dev-story e11-1. Earlier — E11-1 STORY CREATED (e11-1-two-tabs-two-beasts-per-tab-port-identity.md, ready-for-dev). Full dev guide for the three pre-existing serial.js defects E11 stands on, plus THREE CORRECTIONS to the epic's own reading of them. (a) The epic's AC-2 ("user clicks Connect -> picker is shown rather than opening the claimed port") describes something that already happens: connectMicroBeast (serial.js:455-467) always calls requestMicroBeastPort() unless handed a preselectedPort, and the only caller that does is chooseMicroBeast (main.js:539), which ran its own picker. The ONLY skip-the-picker open in the module is the auto-connect branch (serial.js:261-301, prefs.autoConnect default false), so AC-2 is really "auto-connect must decline on an ambiguous match, and decline SILENTLY" - the click path is not touched. (b) The boot scan does NOT use isMicroBeast(): it matches the STORED PRESET VID/PID (serial.js:239-240), which after a 'Show all serial devices' connect is not a CP2102N, while countMicroBeastAdapters() (serial.js:611) is hardcoded to 10c4:ea60 - so a cue sourced from the latter can read '0 MicroBeasts detected' while the scan matched two. The count must come from the boot scan's own filter pass; countMicroBeastAdapters stays menu-bar's Choose MicroBeast... gate and nothing more. (c) The in-use classifier (serial.js:490) keys on InvalidStateError, which per the Web Serial spec is the SAME-PAGE 'this SerialPort is already open' check - a second tab holds a different SerialPort whose state is closed, so the real cross-tab rejection is very likely NetworkError and the D-29 branch has probably never fired in the field. errors.spec.js:83-100 forces InvalidStateError by hand, so no spec has ever exercised the real shape. Made AC-6: verify on two real tabs + two beasts BEFORE finalising the classifier (E9 retro action #2 - checkpoints run in the story that raises them). Also pinned: the error-log PRECEDENCE TRAP (status-bar.js:163-168 returns lastConnectError before the boot cue, so any error the ambiguous auto-connect branch logs hides the new multi-adapter message entirely - the most likely way to implement this correctly and still fail AC-3); lastPortRef has FOUR readers not one (auto-connect :262, onNavSerialDisconnect port-lost trigger :864, D-25 identity preference :844 - unreachable at boot, getConnectionDevice :595), each consequence of leaving it null enumerated; and the TEST-FIXTURE TRAP - 30+ transport specs index _grantedPorts[0] while the three mock hooks target _grantedPorts[length-1], so the second pre-granted port MUST be opt-in and the default mock path byte-for-byte unchanged. Verbatim strings locked: '{n} MicroBeasts detected - click Connect to choose' (n is always >=2, so no pluralisation logic) and 'That MicroBeast is already connected in another Beastty tab. Choose a different one, or disconnect it there first.' Zero new DOM, CSS, prefs, modules or deps; sanctioned API growth is one signal argument, one __getStateForTests field, one mock hook, one internal classifier helper. Baseline d570904, working tree clean, all story line numbers are HEAD. Next: dev-story e11-1, then S11.2 (peer-link.js). NOTE e11-4 is still at 'review' - its code review ran 2026-08-05 (2 fixes committed in d570904, 3 findings recorded as follow-ups) and it is ready to be marked done. Earlier — E11-4 → REVIEW (dev-story, same day as creation): both SLIDE cancel Step-3 echo waits converted from a 10 ms poll-against-a-deadline to resolve-on-transition, notified from the inbound byte dispatcher with the transitioned-to state passed BY VALUE (the recv waiter could never read it back — exitRecvMode nulls slideRef in the same synchronous read-loop callback that feeds the echo, so every receive cancel ever made burned its full 500 ms and reported no echo), against ONE non-chained deadline a clamp can only make fire late. Tab-hide no longer cancels: the SLIDE branch is off visibilitychange and kept verbatim on pagehide (the teardown case D-13's own rationale argues for), recorded as a dated [E11 2026-08-05] AD-13 amendment + docs/architecture-www.md; slide-bridge.spec.js:185 inverted, :210/:226 untouched. TWO FINDINGS BEYOND THE STORY'S BRIEF. (a) AC-10 bot parity paid off exactly as E9 retro §3 warned: slide.asm respond_to_cancel (:407-421) echoes CTRL_CAN role-independently, but the mock bot honoured that only in its send role — in recv role it swallowed the page's CAN silently, so no page-initiated SEND cancel had ever been answered in any spec and that echo path was wholly untested. Fixed with a faithful can_echoed mirror of the Z80's can_initiated latch. (b) A THIRD defect the story does not name: under a clamped clock the cancel sequence's OWN windows eat the whole escape hatch — Step 1's 200 ms and Step 4's 100 ms each floor to ~1 s, so a healthy cancel reaches Step 5 at ~2016 ms and the 2 s hatch fires first, force_idling a session the peer had already acknowledged and printing a failure that did not happen (instrumented timer log in the story's Debug Log). Resolve-on-transition alone does NOT fix it and AC-7(c) is unreachable without it; the hatch is now disarmed the moment the echo is confirmed, its 2000 ms value untouched and still guarding the no-echo path in full (slide-cancel.spec.js:117-137's elapsed >= 600 stays green). New spec slide-hidden-tab-clamp.spec.js, 5 cases proven red with --retries=0 then green — (c) took two rounds to make honest: it first passed for the wrong reason (bot silent), then passed again because the poll checks state before the deadline, and only became genuinely red once the console assertion waited past the 2 s hatch. Recv cancel now completes in ~335 ms where it always burned 600 ms+. Sanctioned API growth held to exactly what AC-8 allows: lastCancelEchoArrived in the two __getStateForTests snapshots + one notifyRecvStateTransition export; no window.__* hook, no main.js change, no Rust/wasm change. Full suite 734 passed / 1 pre-existing flaky (lifecycle boot race) / 1 skipped; transport project 201 tests; cargo test green. New-case flakes in two full runs were chased, not accepted — every reproduction was the documented wasm-boot starvation (window.__menuBar undefined in commonReset), and the cases pass 10/10 and 6/6 in isolation. NOT hardware-verified (the clamp is simulated) — worth a checkpoint before E11 ships. Next: code review → done, then create-story for e11-1. Earlier — E11-4 STORY CREATED (e11-4-hidden-tab-never-invents-a-failure.md, ready-for-dev; epic-e11 → in-progress). Analysis surfaced TWO defects beyond the one the epic names, and one scope decision taken with Ant. (a) The recv wait `waitForState` (slide-recv.js:683) can never resolve true on ANY clock — not just a clamped one: the CAN echo is fed at slide.js:772, `stAfter` sees Done at :773, and :783 maybeExitRecvMode → exitRecvMode → :890 setSlideRecvRef(null) all run synchronously inside one read-loop callback, so by the time any setTimeout tick fires `slideRef` is null and the guard `slideRef && slideRef.state()===target` is false forever. Every recv cancel today burns the full 500 ms and reports no echo. Fix requires the transition notification to CARRY the state value, not point at a handle. (b) chrome.js:227-230 deliberately cancels an active receive and writes raw CTRL_CAN on visibilitychange→hidden (Phase 11 D-13 / SLIDE-31; isSlideActive is recv-only so sends are unaffected) — so hiding a tab mid-receive DESTROYS the transfer by design, making FR-15's second AC unreachable by a poll fix alone. DECISION (Ant, 2026-08-05): split the trigger — pagehide keeps the cancel (the teardown case D-13's own rationale argues for), visibilitychange→hidden drops it; recorded as a dated AD-13 amendment in ARCHITECTURE-SPINE.md + docs/architecture-www.md:42, since AD-13 forbids losing that behaviour SILENTLY, not narrowing it deliberately. slide-bridge.spec.js:185 must be inverted; :210 (pagehide) and :226 (idle) stay. (c) Epic reach claim corrected: both helpers have exactly ONE call site each — the ADR-003 cancel Step 3 (slide.js:1255, slide-recv.js:647) — so the poll defect affects every CANCEL, not every send; the transfer path is inbound-byte-driven and already clamp-immune. Also pinned: ADR-003 §4 (CancelPending emits NO events, so the notify must hang off the post-feed point, not drainEventsAndOutbound, whose filter at slide.js:806 admits only 3 kinds); slide.asm §2 :19-21 publishes the ~500 ms echo budget (E9 bot-parity-first action #1 binds this story); slide-cancel.spec.js:117-137's `elapsed >= 600` lower bound is the assertion most at risk and is protected by AC-4; the clamp is simulated by a setTimeout floor shim via page.evaluate (NOT page.clock, which would freeze the wasm boot and the bot pump); `npm test` pins --project=chromium and does NOT run the transport suite — real baseline is 724 tests / 81 files. Story line numbers are WORKING-TREE (the uncommitted SLIDE.COM-location rework moves waitForSendState from HEAD's :1367 to :1176); no in-flight hunk touches the cancel/wait machinery. Next: dev-story e11-4, or create-story e11-1. Earlier — E11 SPRINT-PLANNED: epic-e11 'Beast-to-Beast Drag Transfer' + 4 stories added as backlog from epics-beast-to-beast.md (15 FRs / 8 NFRs / 7 additional / 7 UX-DRs; single epic; no PRD — E9/E10 precedent; design settled in the 2026-08-05 session). Scope: select a filename in beast A's terminal, drag it onto beast B's terminal in a second tab of the same Chrome window (Split View), file copied A→B. Mechanism: dragstart stamps a custom dataTransfer type carrying the source tab's session id + a single-use nonce + S9.2-parsed filenames (a JS-built File cannot travel — S9.4 proved Chromium strips it from the drag store); the destination reads the id and asks that tab over a same-origin BroadcastChannel; the source runs an ordinary S9.3 pull into its bound folder, reads the file back and posts Blobs; the destination feeds sendFiles → enterSendMode so its own device receives it as an ordinary local-file send. No server, no protocol or firmware change, no disk hop between tabs, no pane, no peer list, no nicknames — getInfo() exposes only usbVendorId/usbProductId (serial.js:46) so two MicroBeasts are indistinguishable, and the drag carries identity instead. Confirm honours the existing 'Confirm file transfers' pref. Explicitly out of scope with reasons recorded: non-Split-View drag (Chrome's tab strip is browser chrome and delivers no drop event to a page; hover-to-reveal is unproven and would background the source tab mid-pull), a menu-driven handoff (parked), more than two beasts ('the other beast' label is correct for exactly two and documented as that assumption). Dependency order S11.1 → S11.2 → S11.3; S11.4 depends on nothing. TWO OF THE FOUR STORIES ARE PRE-EXISTING DEFECTS the design surfaced: e11-1 (serial.js:236 boot-scan ports.find() hands both tabs the same lastPortRef, so the skip-the-picker reconnect at serial.js:262 opens the port the other tab owns; the recognition cue can name the wrong device; and the in-use message tells the user to close the tab E11 requires them to keep open — note explicit Connect already works via requestPort(), so this is narrower than 'two tabs are blocked') and e11-4 (waitForSendState slide.js:1177 + its receive twin slide-recv.js:693 poll setTimeout(tick,10) against a deadline; a hidden tab's timer clamp lets the deadline win and reports a failure that did not happen — affects every send made today; FR-11's request-time visibility check cannot cover a tab hidden MID-transfer, reachable from Split View by selecting a third tab; the real delays like INTER_FILE_HEADER_DELAY_MS are minimums and stretch harmlessly, and canvas.js:425 is paint-only so parsing is unaffected). Best AC in the epic (FR-13): a send-side failure points the user at the copy already sitting in the source tab's pull folder, so the feature degrades to the shipped local-file drag. 13 verbatim strings locked in S11.3 at write time. Next: create-story for e11-1, or e11-4 first since it stands alone. Earlier: E10-2 → REVIEW (dev-story, same day as creation): CSUM -V drag-to-diff implemented — pure parseCsumV + classifyCsumDiff in csum.js, injected per AD-3 (sanctioned opt growth: exactly those two); detail-open drops route to an in-place record diff (mismatch rows: 12% accent tint + textual ' ≠ DDDD' suffix + "local ≠ device" title — never red, never colour alone), aria-live result region carries caption / count line / at most one diagnosis (count mismatch suppresses pattern lines; tail ⓘ title split verbatim; stride uses English ordinals), FR-9 failure message on unparseable drops, FR-11 held (diff lands under an active SLIDE session; no-detail drops still refused; zero transmit asserted); detail footer hint live (drop-active "⤓ Drop to compare"; hidden on error bodies); bloom amendments per AC-9 (diffable detail blooms, landed diff keeps the bloom, Back un-blooms — exitReview parity); host records stowed module-scope at openDetail paint (resolves the S10.1 records-not-retained decision). 14 new specs + the two S10.1 interim pins updated in place (routing spec inverted, detail-foot now expected visible). Suite 513 passed + 6 flaky (boot-race under parallel machine load, all pass on retry; both pull-pane spec files 91/91 serial with retries disabled) + 1 skipped. Next: code review → done. Earlier: E10-2 STORY CREATED (e10-2-drag-csum-v-output-record-diff-diagnosis.md, ready-for-dev): full dev guide for the CSUM -V drag-to-diff — parser grammar pinned from csumhost.c printf formats + the real pc.txt capture (z80.txt flagged as an unusable stale debug capture), pure parseCsumV + classifyCsumDiff in csum.js injected per AD-3 (sanctioned opt growth: exactly those two), dropAcceptable detail branch ordered BEFORE the slideActiveNow check (FR-11: diff never suspended), host records stowed module-scope at openDetail paint (resolves the S10.1-review records-not-retained decision), classifier precedence fixed (count > tail > run > stride > none; count suppresses pattern lines), four caption cases + ordinal rule + tail-line ⓘ split all fixed verbatim, bloom composition amendments (drag blooms onto a diffable detail; landed diff keeps bloom; Back un-blooms — exitReview parity), and the two S10.1 interim specs (drop-refusal (j), hidden detail foot in (g)) named as must-update. Earlier: E10-1 → DONE (dev-story + code review same day): csum.js (pure, injected per AD-3; CRC-32 table-based + Fletcher-16, all 5 embedded csumhost vectors byte-identical), ambient .pp-cs column with session cache keyed name:size:mtime + sequential epoch-guarded fill running on BOTH enumerateAndRender exits (the same-size-edit trap), per-record detail sub-state on the S9.2 paint-once pattern with keyboard-reachable ⁞ button (delegated handlers — per-button retainFocus would grow the focus.js registry per rebuild), interim drop-refusal while detail open (S10.2 boundary), 14 new specs (pull-pane-csum.spec.js) + old fake mkFile pins lastModified:0. Code review (high effort, --fix): 10 findings fixed — openDetail staleness guard (rebind/view/vanish + last-click-wins), '—' cells no longer regress to '…' on Back, focus restored when a rebuild destroys the focused ⁞, cache cleared on rebind + pruned to live files (cross-folder same-stamp reuse), beginReview refuses while detail open, no bloom onto a refused drop, detail CRC written back to the column, dispose hides an open detail, banned-vocab reword. Suite 499 passed + 6 pre-existing flaky + 1 skipped. Next: e10-2 (drag CSUM -V output to diff). Earlier: E10-1 STORY CREATED (e10-1-ambient-crc32-column-per-record-detail-view.md, ready-for-dev; epic-e10 → in-progress): full dev guide with csumhost-generated ground-truth vectors EMBEDDED (empty/one-record/short-final/two-identical/text — sibling repo not needed to build), the same-size-edit trap called out (fill pass must run on the snapshot-equal early-return path or stale CRCs persist), keyboard-reachable ⁞ button (readiness carry-in), interim drop-refusal-while-detail-open as the S10.2 boundary, detail footer hint ships [hidden] until S10.2. Sizing: kept as ONE story; split line (T1-T3 vs T4) recorded if dev needs it. Earlier: E10 SPRINT-PLANNED: epic-e10 'Host-side Checksums in the Pull Pane' + 2 stories (e10-1 ambient CRC-32 column + Fletcher-16 detail view; e10-2 drag-CSUM-V-to-diff with pattern diagnosis) added as backlog from epics-csum.md; readiness READY (implementation-readiness-report-2026-07-24.md, 0 critical/0 major, 7 non-blocking notes carried to story time). Earlier: GUARD LOOPHOLE FIXED (E8 action → done) + E9 REVIEW BACKFILLS (E9 action #5 → done): check-story-done-consistency.py now blocks a missing Code Review section outside a closed pre-convention list, accepts h2+h3 headings (h3-only match had skipped every E9 section), and flags the E9 template's 'fill on completion' stub text. The tightened guard surfaced 4 unfilled E9 stubs (S9.1a/S9.2/S9.3/S9.4 — outcomes were in Change Logs/commits, stubs never replaced); all backfilled with dated notes. Guard passes: 29 done stories consistent. Remaining open E8 action: light-ready() spec-pattern doc. Earlier: E8 REVIEW-SECTION BACKFILL DONE (action item → done): e8-1/e8-2/e8-3 story files gained '### Code Review' sections reconstructed from their implementation-commit records (2+5+0 findings), each with a dated backfill note + change-log entry; check-story-done-consistency.py no longer lists E8 as a legacy gap. Remaining open E8 actions: guard-loophole fix, light-ready() spec-pattern doc. Earlier: E8 TWEAKS i–iv IMPLEMENTED + same-day refinement (action item → done): overlay list renders oldest→newest top→bottom (internal state stays newest-first; ↑=older ↓=newer); Enter sends the highlight when edit line is empty OR ↑/↓ moved it since the text last changed (navigated flag; freshly typed text with untouched highlight sends as typed); ←/→ on empty edit line copy highlight as Tab; legend now "↑↓ select · Tab edit · Enter send · Esc cancel"; EXPERIENCE.md/DESIGN.md amended with dated markers; overlay spec 24 tests (5 new). Suite green + Ant hardware-verified ("all is working"). Earlier: E8 RETRO DONE (epic-e8-retro-2026-07-24.md); epic-e8 → done; 4 action items opened (e8 review-section backfill, done-guard legacy-loophole fix, light-ready() spec pattern doc, recall-overlay hardware tweaks i–iv pre-deployment). Hardware verdict from Ant: "basic functionality is solid"; tweaks ii/iii are field-driven FR-16 spec reversals (Enter/arrows act on highlight un-Tabbed), i flips list display order (newest at bottom), iv trims the legend. Earlier: E9 RETRO DONE (epic-e9-retro-2026-07-24.md); epic-e9 → done; 5 action items opened (bot-parity-first, checkpoint-in-story, latent-stub sweep, wire-speed baselines, S9.1a review backfill); backlog seeds: DIR-columnar reassembly, bloom dismissal after drag-out, formatSize dedup. Deployment parked behind E8 retro + next file-transfer planning talk. Earlier: S9.4 → done. Code review (high effort, --fix; commit 8bdb80a): 10 verified findings, all fixed — window-ACK pacing latch (split-ACK first half / console text / line noise could pump the next window before the previous ACK completed, running the sender ahead of the Z80 cadence and uncovering its disk-flush deaf window); reverse-drag stash lifetime (window-level pointerup/pointercancel + refresh skip while a gesture is armed — a stale stash could send an old file on a later unrelated text drag, wrapper left blurred); sendFiles() session guard + recv in isSessionActive (silent no-op confirm modals); polite-fail Chromium detection (Firefox over LAN HTTP got the secure-context page); batch hint preserved across mid-session recv re-entry (chip keeps N/M); isSendActive() export replaces __getStateForTests in the Esc path + chip onCancel; recv hot-loop per-byte wasm crossing trimmed; renderRows() consumes orderedFiles(); refusal diagnostic throttled; throughput formatter purified; leftover first-use-confirm console.info removed; banned-vocabulary rewording. Full suite 472 passed. Next: epic-e9 retro. Earlier: S9.4 → review (dev-story). Manual checkpoint failed design (a) — Chromium's real drag loop strips a JS-constructed File from the drag store (degrades to text/plain filename; proven with instrumented real-drag runs). Took the story's sanctioned fallback shaped to KEEP the native drag: sendFiles() export on file-source.js + main.js injection (recorded deviations), pane-owned guarded wrapper drop handlers reusing the data-drop-target overlay, step-back on real 'Files' drags. Drag→overlay→drop→modal re-verified through the real drag loop; hardware pass confirmed by Ant ("works great"). Then T6 user-requested extension: multi-select (ctrl/shift/plain/empty-area) + multi-file drag, both handoff routes, real-loop verified — supersedes the story's "no multi-file drag" scope line. Then UAT-E9-03 (two rounds): Ant's multi-file hardware send (19+24 KB) failed after file 1 with "Transfer cancelled by peer". Round 1 fixed a real split-ACK classifier drop + made Esc cancel SEND sessions (was recv-only). Retest failed identically → read the REAL slide.asm: the primary defect was the sender pumping ONE frame per ACK while the Z80 ACKs once per 4-frame window — every send ever made crawled on the Z80's retry-NAK crutch (~4-5x slow), and the NAK spam + file 2's frame seq 24 (0x18 = CTRL_CAN) turned any desync into a spurious cancel echo (deterministic for >=24 KB files; the 19 KB file tops out at seq 21). Fixed: window-aligned burst pump matching slide.asm/slide-py cadence — sends now run at wire speed. Bot gained v0.5.2 ACK cadence + split-ACK realism + stall knob; 3 new sender specs, each shown failing before the fix and passing after. Hardware retest PASSED (19+24 KB pair end-to-end, wire speed — all sends incl. OS drags now ~4-5x faster); UAT-E9-02 + UAT-E9-03 recorded pass. Post-pass niggles (UAT-E9-04): send-chip filename wired (Phase 9 leftover), chip rate flip-flop fixed (trim-vs-guard boundary bug); OS-filer-drag rejection closed as Linux-desktop-environmental (Windows Chrome accepts; Linux delivers no drag events to the web contents — tracer-proven, temporary tracer stripped). The Windows tunnel run surfaced+fixed two more: polite-fail now distinguishes insecure-context Chromium from non-Chromium (LAN-HTTP case), and T-12-07 first-use-confirm 30s timeout no longer leaks firstUseConfirmPending (was: all sends refused until reload). Remaining: code review → done. Story created earlier today (ready-for-dev): reverse drag via dataTransfer.items.add(File) — zero transport changes; stash-at-pointerdown for the async getFile; manual checkpoint + sanctioned sendFiles fallback documented. Earlier today: E9 S9.3 → done. Code-review --fix (10 findings), then UAT-E9-01 executed on REAL HARDWARE and passed end-to-end: drag→drop→review→confirm→transfer→land→refresh, incl. DIR-columnar parsing (VPEEK    COM → VPEEK.COM) and pane-content scrolling added during UAT. Hardware surfaced 5 receiver interop bugs vs Z80 SLIDE v0.5.2, all fixed + replay-tested (commit b63217d): wakeup-signature-in-payload false re-entry (pulling SLIDE.COM broke the session), window-retransmit intolerance, EOF-after-gap silent short file, boundary+EOF double-ACK (stray ^D), chip counters (2/1 → 1; pane batch hint gives N/M). Hardware checks answered: (a) space separators WORK, (b) bare SLIDE S resolves from A> and B>, (c) 126-char line accepted. Next: S9.4 (optional reverse drag) or epic-e9 retro. E8 done 2026-07-23; E0–E7 complete (milestone ui-rethink, 2026-07-04).
# retrospective action items (surface open ones during sprint status)
action_items:  # ui-rethink (E0–E7) items all resolved & cleared 2026-07-23; retros append new ones here
  - epic: E8
    action: "Backfill '### Code Review' sections for e8-1/e8-2/e8-3 from commit evidence (ea27a13, 474cf65, c1706d0)"
    owner: "Amelia"
    status: done  # backfilled 2026-07-24 with dated backfill markers; guard now sees all three as filled
  - epic: E8
    action: "check-story-done-consistency.py: missing Code Review section blocks for E8-onward stories; pin legacy allowance to a closed pre-convention list"
    owner: "Amelia"
    status: done  # 2026-07-24: closed LEGACY_NO_SECTION list (10 E0-E2 stories); h2+h3 headings both accepted (h3-only match missed every E9 section); 'fill on completion' added to stub markers; the 4 unfilled E9 stubs it then flagged were backfilled
  - epic: E8
    action: "Record light-ready() spec pattern (wait on wasm encoder only when test sends real keys) in test docs / spec template"
    owner: "Paige"
    status: open
  - epic: E8
    action: "Recall-overlay hardware tweaks i-iv: newest at list bottom; Enter sends highlight un-Tabbed (precedence: edit text wins when non-empty); left/right copies highlight to edit line; drop 'left/right edit' legend text; EXPERIENCE.md + DESIGN.md amended. Pre-deployment."
    owner: "Amelia"
    status: done  # implemented 2026-07-24; overlay render flip + key remap, 3 new specs + 4 amended, suite 657 green
  - epic: E9
    action: "Bot-parity-first rule: any story touching SLIDE transport starts by confirming bot behavior against slide.asm for the paths it exercises"
    owner: "Amelia"
    status: open
  - epic: E9
    action: "Manual platform checkpoints run in the story that raises them; one left open at story end is an explicit carried risk named in the next story's plan"
    owner: "Amelia"
    status: open
  - epic: E9
    action: "Latent-stub sweep: grep the shell for 'until wired' / dormant-hook TODOs (cf. Phase 9 Esc-cancel + chip filename); triage into fix/park/delete, record results"
    owner: "Amelia"
    status: open
  - epic: E9
    action: "Record wire-speed reference throughput (e.g. ~11 s for 19 KB @ 19200 baud) in transport docs so silent slowdowns are measurable"
    owner: "Paige"
    status: open
  - epic: E9
    action: "Backfill S9.1a story file's code-review outcome section"
    owner: "Amelia"
    status: done  # 2026-07-24: backfilled from 20c23f2 (1 finding: async epoch guard) — plus the S9.2/S9.3/S9.4 stubs the tightened guard surfaced
project: beastty
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # ── Epic E0 · Shared UI Primitives (depends on: nothing) ──
  epic-e0: done
  e0-1-focus-retention-helper: done
  e0-2-modal-helper-openmodal-send-modal-refactor: done
  epic-e0-retrospective: done

  # ── Epic E1 · Menu Bar Backbone + View Menu (depends on: E0) ──
  epic-e1: in-progress
  e1-1-menu-bar-shell-dropdown-mechanics: done
  e1-2-keyboard-navigation-esc-passthrough-guard: done
  e1-3-chrome-js-decomposition-boot-order-reset-re-projection: done
  e1-4-view-menu-theme-phosphor: done
  e1-5-view-menu-font-zoom-clear: done
  epic-e1-retrospective: done

  # ── Epic E2 · Connection & Serial Configuration (depends on: E0, E1) ──
  epic-e2: in-progress
  e2-1-connect-disconnect-single-writer-menu-item: done
  e2-2-auto-connect-toggle-choose-microbeast: done
  e2-3-serial-configuration-modal: done
  epic-e2-retrospective: done

  # ── Epic E3 · File, Settings & Transfer Configuration (depends on: E0, E1) ──
  epic-e3: in-progress
  e3-1-file-menu-send-file-download-session-log: done
  e3-2-settings-menu-local-echo-enter-key-sends: done
  e3-3-settings-menu-reset-all-preferences-reserved-ctrl-info: done
  e3-4-slide-file-transfer-modal: done
  epic-e3-retrospective: done

  # ── Epic E4 · Bottom Status Bar (depends on: E1, E2) ──
  epic-e4: in-progress
  e4-1-connection-device-baud-readout: done
  e4-2-build-sha-zoom-readout: done
  e4-3-recent-errors-affordance: done
  epic-e4-retrospective: done

  # ── Epic E5 · Debug Menu & In-Page Debug Panel (depends on: E1) ──
  epic-e5: in-progress
  e5-1-debug-menu-panel-toggle: done
  epic-e5-retrospective: done

  # ── Epic E6 · Help Menu (depends on: E0, E1) ──
  epic-e6: in-progress
  e6-1-keyboard-shortcuts-modal: done
  e6-2-about-beastty-modal: done
  epic-e6-retrospective: done

  # ── Epic E7 · Paste Toast (depends on: E0) ──
  epic-e7: done
  e7-1-paste-progress-large-paste-confirm-toast: done
  epic-e7-retrospective: done

  # ── Epic E8 · Command History (NEW milestone; depends on: finished E0–E7 chrome) ──
  # Story order E8.1 → E8.2 → E8.3, each standalone (default-on ⇒ E8.1+E8.2 work before E8.3).
  epic-e8: done
  e8-1-command-capture-engine-line-mirror-history-store: done
  e8-2-recall-overlay-trigger-filter-edit-send: done
  e8-3-settings-surfaces-enable-size-clear: done
  epic-e8-retrospective: done  # epic-e8-retro-2026-07-24.md

  # ══ NEW MILESTONE — Pull Pane ══
  # ── Epic E9 · Local Folder Pull Pane (depends on: E0–E7 shipped) ──
  # Requirements: planning-artifacts/epics-pull-pane.md (no PRD). UX: ux-beastty-2026-07-01 (pull-pane).
  # Dependency order: S9.1a → S9.1b → S9.2 → S9.3 → S9.4 (optional). E8 Command History merged from ui-rethink 2026-07-23.
  epic-e9: done
  e9-1a-pull-pane-shell-fsa-folder-view: done
  e9-1b-live-refresh-triggers-guards-diff-render: done
  e9-2-selection-to-slide-s-in-pane-review: done
  e9-3-drop-to-pull-fire-land-refresh: done
  e9-4-reverse-drag-pane-to-terminal: done   # optional story — shipped
  epic-e9-retrospective: done  # epic-e9-retro-2026-07-24.md

  # ══ NEW MILESTONE — Host-side Checksums ══
  # ── Epic E10 · Host-side Checksums in the Pull Pane (depends on: E9 shipped) ──
  # Requirements: planning-artifacts/epics-csum.md (no PRD — E9 precedent; external contract: ../beast_csum).
  # Readiness: implementation-readiness-report-2026-07-24.md — READY, 0 critical/0 major, 7 carried notes
  # (keyboard-reachable detail button → S10.1; painted-once Dev Note + aria-live caption → S10.2;
  #  UX-contract dated E10 amendments during implementation; csumhost vectors checked into fixtures).
  # Dependency order: S10.1 → S10.2. S10.1 flagged at upper size bound — split line documented
  # (column+cache vs detail view) if create-story confirms.
  epic-e10: in-progress
  e10-1-ambient-crc32-column-per-record-detail-view: done

  e10-2-drag-csum-v-output-record-diff-diagnosis: review
  epic-e10-retrospective: optional

  # ══ NEW MILESTONE — Beast-to-Beast ══
  # ── Epic E11 · Beast-to-Beast Drag Transfer (depends on: E9 shipped; E10 unaffected) ──
  # Requirements: planning-artifacts/epics-beast-to-beast.md (no PRD — E9/E10 precedent;
  # design settled in the 2026-08-05 session). 15 FRs / 8 NFRs / 7 additional / 7 UX-DRs.
  # Scope: drag a filename out of one beast's terminal onto another beast's terminal in a
  # second tab of the same Chrome window (Split View) — the file is copied A → B. Identity
  # rides in the drag (custom dataTransfer type + session id + single-use nonce); the bytes
  # ride over a same-origin BroadcastChannel. No server, no protocol or firmware change,
  # no pane, no peer list, no nicknames.
  # Out of scope, recorded: non-Split-View drag (Chrome's tab strip delivers no drop to a
  # page; hover-to-reveal would background the source tab mid-pull); a menu handoff (parked);
  # more than two beasts.
  # Dependency order: S11.1 → S11.2 → S11.3. S11.4 depends on nothing but must land before
  # E11 ships — it covers the tab hidden MID-transfer, which S11.3's request-time check cannot.
  # Two of the four are pre-existing defects this design surfaced (e11-1, e11-4).
  epic-e11: in-progress
  e11-1-two-tabs-two-beasts-per-tab-port-identity: review
  e11-2-cross-tab-link-identity-nonce-blob-transport: backlog
  e11-3-drag-filename-onto-the-other-beast: backlog
  e11-4-hidden-tab-never-invents-a-failure: done
  epic-e11-retrospective: optional

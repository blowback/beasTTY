# generated: 2026-07-01
# last_updated: 2026-07-22
# project: beastty
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones
#
# DEPENDENCY ORDER (from EPIC-SPLIT.md — informational, not enforced by status):
# ==============================================================================
#   E0 → E1 → {E2, E3, E5, E6};  E4 depends on E1 + E2;  E7 depends on E0.
#   E1 is the backbone and must land before the other menu/modal epics.

generated: 2026-07-01
last_updated: 2026-08-06  # E11 RETROSPECTIVE (epic-e11-retro-2026-08-06.md) — E11 and E10 both closed. The one item blocking E11 is FIXED (b9827ab): main.js handed wireChrome slide-recv's recv-only isSlideActive plus a recv-only cancel, so the D-13 CTRL_CAN branch never ran for a SEND session and closing the tab mid-send left the Z80 waiting — the only remaining teardown protection since S11.4 made pagehide the sole hide-time trigger. Predicate widened to the composite already used at the pull-pane wiring; cancel now routes by direction as wireSlideChip's onCancel does. New slide-bridge.spec.js case proven red on the recv-only version; its comment states honestly that the cancel-routing half is NOT independently pinned (reverting it alone leaves the case green, because the raw writeSlideFrame(0x18) suffices when the mock bot echoes CTRL_CAN). Suite 819 passed / 1 skipped / 2 flaky = 822 (baseline 821, +1); both flakes are the standing load-sensitive pool in prefs.spec.js, 17/17 green isolated. E10 CLOSED: e10-2 -> done on Ant's confirmation it was implemented and reviewed, with its Code Review section backfilled as a FACT-OF-REVIEW record only — the findings were never written down and no review-labelled commit exists, so E10's review coverage is unevidenced rather than clean. E10 retro SKIPPED by Ant's call. RETRO HEADLINE: the recv-only predicate appeared FOUR times in E11 alone (S11.4's maybeExitRecvMode, S11.2's isBusy, S11.3's hasBoundFolder, and this one) and seven times since E9 — one unrepaired shape every new consumer must independently notice, now the single open E11 action item. Second theme: deliberate mutation testing caught SIX specs passing for the wrong reason, and it is the practice that found all of them. Third: both hardware checkpoints found defects no spec could — S11.1's proved the shipped in-use classifier had never once fired for its own case, S11.3's proved the custom drag type survives the real Chromium protected drag store and caught the modal naming the wrong drive. Ant's verdict on the feature: "worked first time and is impressive in action". THREE STALE ACTION ITEMS LEFT UNTOUCHED pending Ant's call — E9 #3 (latent-stub sweep), E9 #4 (wire-speed reference), E8 #3 (light-ready spec pattern). Next: light bugfixes; no E12 defined.
# Superseded 2026-08-06 (kept for the S11.3 detail): E11-3 -> DONE, and with it every story of E11 (the epic stays in-progress on one carried defect, below). The drag itself: select a filename in one beast's terminal, drop it on the other beast's, and the file crosses - the last story of the epic and the only one a user can see. Implementation fff7137, which also carries the code-review fixes because the tree was still uncommitted when the review ran. New www/input/peer-drop.js owns BOTH halves of the gesture and every user-facing string: the source half (one getPeerStamp thunk handed to selection.js's existing dragstart, stamping a versioned JSON payload of session id + single-use nonce + optional mint timestamp + the parsed filenames under a lowercase custom type, and stamping NOTHING when the selection holds no valid 8.3 name) and the destination half (four handlers on #terminal-wrapper as its THIRD independent owner, this beast's own refusals run BEFORE any request is posted, the new #peer-copy-modal on the existing confirm-transfers pref, and new File([blob], name) at the boundary into sendFiles because processFiles duck-types .name and a bare Blob lands every file in the REJECTED rows without throwing). peer-link.js byte-for-byte unchanged. AC-15 RUN ON HARDWARE by Ant and PASSED - two MicroBeasts in Chrome Split View: single-file drag, multi-file drag, two refusals end to end, source tab hidden mid-transfer. It found one defect the whole automated suite could not, the modal's To row reading A: while the receiving beast was on B: ({X} was slideProgramDrive - where SLIDE.COM lives, not where CP/M writes); Ant's call was to delete the drive letter rather than qualify it, and getDestDrive went with it. CODE REVIEW (--fix, different LLM): 7 findings, 6 fixed, 1 recorded. The two that mattered both sat in pullForPeer's wait and were invisible to the suite for the same reason - the specs run one pull at a time through a directory stub that enumerates in insertion order. FIXED (1) THE ONE THAT COULD CORRUPT FILES: the read-back paired arrived[i] from dirHandle.entries() (DIRECTORY order) with requested[i] (SLIDE COMMAND order), so a two-file drag of ZAP.COM ABC.TXT handed the peer a ZAP.COM containing ABC.TXT's bytes and vice versa, silently, both transfers reporting success; now paired by name (exact, else the BASE~N.EXT shape ensureUnique produces, via a helper mirroring slide-recv.js:588-593). The story's recorded "arrival order = command order" assumption was wrong on the read-back side; the multi-file two-tab pin held only because that fixture's names are alphabetical. (2) onChipLifecycle treated EVERY terminal chip state as end-of-session, but each summary/error/notice arms a 5s auto-hide that emits 'hidden' - so two drags inside 5s had the first transfer's auto-hide settle the SECOND pull's wait early and report pull-failed about a pull running perfectly; only 'hidden' is now conditioned on this pull's session having shown life, every other terminal state stays unconditional because those are emitted by the tab actually entering them (the first, broader version of the fix broke AC-7's never-wakes case and was narrowed until it passed for the right reason). (3) a tailTimer armed by a terminal event was never cancelled when the chip recovered to 'active'. (4) handleDrop checked destination connected/busy BEFORE the confirm modal and never again, and that dialog can sit open indefinitely - unplug the port or start a local send, click Copy, and a full 19200-baud pull comes back to a tab that cannot take it; both checks now re-run after the confirm, as pull-pane.js already does. (5) hasBoundFolder shipped as slideRecvToFolder !== false, which reads a missing or nulled pref as USABLE - reintroducing in the narrow case the same silent stall the widening was written to close; now !!getPrefs()?.slideRecvToFolder, matching the truthiness test slide-recv.js:521 applies. (6) in sentenceForRefusal a torn-down peerLinkRef leaves codes as {}, so every case is undefined, an undefined code matched the FIRST case, and the user was told "This beast isn't connected" about a check nobody ran. (7) __resetForTests left lastPeerPullReason standing, so the very assertion added to stop AC-7 passing for the wrong reason could itself pass on the previous spec's value. RECORDED NOT FIXED: slide-chip.js's enterNotice can clobber a live progress chip when a late pull-failed lands after this tab started its own transfer - fixing it needs a precedence rule between notices and live progress, a UX call outside the diff with the copy frozen by UX-DR7; owner Ant. Also reconfirmed and still out of scope: slide-recv's downloadToFolder returns without throwing when it finds no handle, so onFileLanded fires on the anchor-tray path - the outcome stays honest (pull-failed) but the callback's name is a lie there. Suite after fixes: 581 passed / 1 skipped on chromium and 237 passed on chromium-transport, two pre-existing unrelated flakes green on retry - the standing load-sensitive pool from S11.1's review, not this diff. E11 CHECKPOINTS: S11.4's clamp fix is now hardware-observed and CLOSED, with the limit stated - a 10-second hide exercises the basic clamp but not Chromium's coarser post-~5-minute backgrounding regime. ONE RE-CARRIED AND STILL OPEN, which is why epic-e11 is not done: main.js:458 still hands wireChrome the recv-only isSlideActive, so the pagehide CTRL_CAN teardown never fires for SEND sessions - and since S11.4 made pagehide the sole hide-time trigger, that is the only remaining protection and it has a hole. It is the same recv-only-predicate trap AC-4 guards against, seen from a third angle. chrome.js was on S11.3's explicit no-change list; it needs its own story in E12 or a fix commit before E11 ships. Next: that fix, then the E11 retrospective (optional).
# retrospective action items (surface open ones during sprint status)
action_items:  # ui-rethink (E0–E7) items all resolved & cleared 2026-07-23; retros append new ones here
  - epic: E8
    action: "Backfill '### Code Review' sections for e8-1/e8-2/e8-3 from commit evidence (ea27a13, 474cf65, c1706d0)"
    owner: "Amelia"
    status: done  # backfilled 2026-07-24 with dated backfill markers; guard now sees all three as filled
  - epic: E8
    action: "check-story-done-consistency.py: missing Code Review section blocks for E8-onward stories; pin legacy allowance to a closed pre-convention list"
    owner: "Amelia"
    status: done  # 2026-07-24: closed LEGACY_NO_SECTION list (10 E0-E2 stories); h2+h3 headings both accepted (h3-only match missed every E9 section); 'fill on completion' added to stub markers; the 4 unfilled E9 stubs it then flagged were backfilled
  - epic: E8
    action: "Record light-ready() spec pattern (wait on wasm encoder only when test sends real keys) in test docs / spec template"
    owner: "Paige"
    status: open
  - epic: E8
    action: "Recall-overlay hardware tweaks i-iv: newest at list bottom; Enter sends highlight un-Tabbed (precedence: edit text wins when non-empty); left/right copies highlight to edit line; drop 'left/right edit' legend text; EXPERIENCE.md + DESIGN.md amended. Pre-deployment."
    owner: "Amelia"
    status: done  # implemented 2026-07-24; overlay render flip + key remap, 3 new specs + 4 amended, suite 657 green
  - epic: E9
    action: "Bot-parity-first rule: any story touching SLIDE transport starts by confirming bot behavior against slide.asm for the paths it exercises"
    owner: "Amelia"
    status: in-progress  # E11: APPLIED and it paid — S11.4's AC-10 parity check against respond_to_cancel found the bot silent in its recv role, so no page-initiated send cancel had ever been answered in any spec. Still in-progress because the "done when" criterion is unmet: the rule is practised, not recorded anywhere a future story would find it.
  - epic: E9
    action: "Manual platform checkpoints run in the story that raises them; one left open at story end is an explicit carried risk named in the next story's plan"
    owner: "Amelia"
    status: in-progress  # E11: 2 of 3. S11.1's T5 ran in-story and disproved the shipped classifier's premise; S11.3's AC-15 ran in-story and found the modal's wrong drive letter. S11.4's clamp checkpoint was deferred, named in each following story's plan as the rule requires, and only closed three stories later in S11.3 — and closed with a limit still standing (a 10 s hide does not reach Chromium's post-~5-minute backgrounding regime).
  - epic: E9
    action: "Latent-stub sweep: grep the shell for 'until wired' / dormant-hook TODOs (cf. Phase 9 Esc-cancel + chip filename); triage into fix/park/delete, record results"
    owner: "Amelia"
    status: open
  - epic: E9
    action: "Record wire-speed reference throughput (e.g. ~11 s for 19 KB @ 19200 baud) in transport docs so silent slowdowns are measurable"
    owner: "Paige"
    status: open
  - epic: E9
    action: "Backfill S9.1a story file's code-review outcome section"
    owner: "Amelia"
    status: done  # 2026-07-24: backfilled from 20c23f2 (1 finding: async epoch guard) — plus the S9.2/S9.3/S9.4 stubs the tightened guard surfaced
  - epic: E11
    action: "Fix the recv-only session predicate at source: export ONE composite SLIDE-session predicate and make slide-recv's recv-only isSlideActive module-internal, so a new consumer cannot pick the wrong one. Seven appearances across E9-E11; the last one (main.js wireChrome) is fixed in b9827ab but the shape that produces them is not."
    owner: "Amelia"
    status: open
project: beastty
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # ── Epic E0 · Shared UI Primitives (depends on: nothing) ──
  epic-e0: done
  e0-1-focus-retention-helper: done
  e0-2-modal-helper-openmodal-send-modal-refactor: done
  epic-e0-retrospective: done

  # ── Epic E1 · Menu Bar Backbone + View Menu (depends on: E0) ──
  epic-e1: in-progress
  e1-1-menu-bar-shell-dropdown-mechanics: done
  e1-2-keyboard-navigation-esc-passthrough-guard: done
  e1-3-chrome-js-decomposition-boot-order-reset-re-projection: done
  e1-4-view-menu-theme-phosphor: done
  e1-5-view-menu-font-zoom-clear: done
  epic-e1-retrospective: done

  # ── Epic E2 · Connection & Serial Configuration (depends on: E0, E1) ──
  epic-e2: in-progress
  e2-1-connect-disconnect-single-writer-menu-item: done
  e2-2-auto-connect-toggle-choose-microbeast: done
  e2-3-serial-configuration-modal: done
  epic-e2-retrospective: done

  # ── Epic E3 · File, Settings & Transfer Configuration (depends on: E0, E1) ──
  epic-e3: in-progress
  e3-1-file-menu-send-file-download-session-log: done
  e3-2-settings-menu-local-echo-enter-key-sends: done
  e3-3-settings-menu-reset-all-preferences-reserved-ctrl-info: done
  e3-4-slide-file-transfer-modal: done
  epic-e3-retrospective: done

  # ── Epic E4 · Bottom Status Bar (depends on: E1, E2) ──
  epic-e4: in-progress
  e4-1-connection-device-baud-readout: done
  e4-2-build-sha-zoom-readout: done
  e4-3-recent-errors-affordance: done
  epic-e4-retrospective: done

  # ── Epic E5 · Debug Menu & In-Page Debug Panel (depends on: E1) ──
  epic-e5: in-progress
  e5-1-debug-menu-panel-toggle: done
  epic-e5-retrospective: done

  # ── Epic E6 · Help Menu (depends on: E0, E1) ──
  epic-e6: in-progress
  e6-1-keyboard-shortcuts-modal: done
  e6-2-about-beastty-modal: done
  epic-e6-retrospective: done

  # ── Epic E7 · Paste Toast (depends on: E0) ──
  epic-e7: done
  e7-1-paste-progress-large-paste-confirm-toast: done
  epic-e7-retrospective: done

  # ── Epic E8 · Command History (NEW milestone; depends on: finished E0–E7 chrome) ──
  # Story order E8.1 → E8.2 → E8.3, each standalone (default-on ⇒ E8.1+E8.2 work before E8.3).
  epic-e8: done
  e8-1-command-capture-engine-line-mirror-history-store: done
  e8-2-recall-overlay-trigger-filter-edit-send: done
  e8-3-settings-surfaces-enable-size-clear: done
  epic-e8-retrospective: done  # epic-e8-retro-2026-07-24.md

  # ══ NEW MILESTONE — Pull Pane ══
  # ── Epic E9 · Local Folder Pull Pane (depends on: E0–E7 shipped) ──
  # Requirements: planning-artifacts/epics-pull-pane.md (no PRD). UX: ux-beastty-2026-07-01 (pull-pane).
  # Dependency order: S9.1a → S9.1b → S9.2 → S9.3 → S9.4 (optional). E8 Command History merged from ui-rethink 2026-07-23.
  epic-e9: done
  e9-1a-pull-pane-shell-fsa-folder-view: done
  e9-1b-live-refresh-triggers-guards-diff-render: done
  e9-2-selection-to-slide-s-in-pane-review: done
  e9-3-drop-to-pull-fire-land-refresh: done
  e9-4-reverse-drag-pane-to-terminal: done   # optional story — shipped
  epic-e9-retrospective: done  # epic-e9-retro-2026-07-24.md

  # ══ NEW MILESTONE — Host-side Checksums ══
  # ── Epic E10 · Host-side Checksums in the Pull Pane (depends on: E9 shipped) ──
  # Requirements: planning-artifacts/epics-csum.md (no PRD — E9 precedent; external contract: ../beast_csum).
  # Readiness: implementation-readiness-report-2026-07-24.md — READY, 0 critical/0 major, 7 carried notes
  # (keyboard-reachable detail button → S10.1; painted-once Dev Note + aria-live caption → S10.2;
  #  UX-contract dated E10 amendments during implementation; csumhost vectors checked into fixtures).
  # Dependency order: S10.1 → S10.2. S10.1 flagged at upper size bound — split line documented
  # (column+cache vs detail view) if create-story confirms.
  epic-e10: done
  e10-1-ambient-crc32-column-per-record-detail-view: done

  e10-2-drag-csum-v-output-record-diff-diagnosis: done  # 2026-08-06 — Ant confirmed implemented + reviewed; Code Review section backfilled as a FACT-OF-REVIEW record only (findings were never written down and no review-labelled commit exists to reconstruct them). Treat E10's review coverage as unevidenced, not clean.
  epic-e10-retrospective: optional  # SKIPPED by Ant's call 2026-08-06 — not running an E10 retro.

  # ══ NEW MILESTONE — Beast-to-Beast ══
  # ── Epic E11 · Beast-to-Beast Drag Transfer (depends on: E9 shipped; E10 unaffected) ──
  # Requirements: planning-artifacts/epics-beast-to-beast.md (no PRD — E9/E10 precedent;
  # design settled in the 2026-08-05 session). 15 FRs / 8 NFRs / 7 additional / 7 UX-DRs.
  # Scope: drag a filename out of one beast's terminal onto another beast's terminal in a
  # second tab of the same Chrome window (Split View) — the file is copied A → B. Identity
  # rides in the drag (custom dataTransfer type + session id + single-use nonce); the bytes
  # ride over a same-origin BroadcastChannel. No server, no protocol or firmware change,
  # no pane, no peer list, no nicknames.
  # Out of scope, recorded: non-Split-View drag (Chrome's tab strip delivers no drop to a
  # page; hover-to-reveal would background the source tab mid-pull); a menu handoff (parked);
  # more than two beasts.
  # Dependency order: S11.1 → S11.2 → S11.3. S11.4 depends on nothing but must land before
  # E11 ships — it covers the tab hidden MID-transfer, which S11.3's request-time check cannot.
  # Two of the four are pre-existing defects this design surfaced (e11-1, e11-4).
  epic-e11: done
  e11-1-two-tabs-two-beasts-per-tab-port-identity: done
  e11-2-cross-tab-link-identity-nonce-blob-transport: done
  e11-3-drag-filename-onto-the-other-beast: done
  e11-4-hidden-tab-never-invents-a-failure: done
  epic-e11-retrospective: done  # epic-e11-retro-2026-08-06.md

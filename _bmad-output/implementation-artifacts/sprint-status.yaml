# generated: 2026-07-01
# last_updated: 2026-07-01
# project: beastty
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones
#
# DEPENDENCY ORDER (from EPIC-SPLIT.md — informational, not enforced by status):
# ==============================================================================
#   E0 → E1 → {E2, E3, E5, E6};  E4 depends on E1 + E2;  E7 depends on E0.
#   E1 is the backbone and must land before the other menu/modal epics.

generated: 2026-07-01
last_updated: 2026-07-04  # E7.1 done — paste toast + dual-chrome formal sweep + whole-branch review; epic-e7 done; five-epic carry (e1#5/e2#4/e3#4/e4#4/e5#2 + e6#1/#2/#3/#4) closed. Milestone ui-rethink complete.
# retrospective action items (surface open ones during sprint status)
action_items:
  - epic: "e0"
    action: "Establish a flaky-test protocol: serialize/quarantine the parallel-load transport specs, OR codify 'judge regressions by named oracles in isolation (--workers=1)' as a standing convention"
    owner: "Amelia (Dev)"
    status: done   # resolved 2026-07-02 by the E1 flake fix (playwright.config.js transport project + retry)
  - epic: "e0"
    action: "Decide openModal returnValue-reset policy for future new modals and document it in modal.js"
    owner: "Winston + Amelia"
    status: done   # 2026-07-02 — policy block added to modal.js header; clear-scrollback modal audited
  - epic: "e0"
    action: "Note in E1.3 plan that chrome.js already has zero inline focus sites (migrated in E0.1) so the work is not re-planned"
    owner: "Amelia"
    status: done
  - epic: "e1"
    action: "Land the real flake fix (serialize/quarantine transport connect-handshake specs in playwright.config.js, or bounded retry) so the full parallel suite is trustworthy without per-story --workers=1 re-diagnosis"
    owner: "Amelia (Dev)"
    status: done   # 2026-07-02 — transport isolated to a non-fully-parallel project + retries:1; verified 397 passed / 0 failed at full parallelism
  - epic: "e1"
    action: "Ratify or flip the Font shown-but-disabled-off-CRT decision (dev resolved architecture-authoritative per AD-7/AD-9 against silent FR-9; flip is a one-line syncFontDisabled removal)"
    owner: "Ant + Amelia"
    status: done   # 2026-07-02 — Ant confirmed: Font selection disabled when not in CRT mode is correct
  - epic: "e1"
    action: "Decide and document the openModal returnValue-reset policy in modal.js before E2.3, retro-auditing the E1.5 #clear-scrollback-confirm modal"
    owner: "Winston + Amelia"
    status: done   # 2026-07-02 — policy block in modal.js; clear-scrollback modal conforms
  - epic: "e1"
    action: "Codify menu-driven testing idioms (window.__menuBar.open, force:true on aria-disabled, snap-to-bottom as scrollback-flush fingerprint) for E2/E3/E6"
    owner: "Amelia"
    status: done   # 2026-07-04 (E5 retro) — codified-idioms carry closed as intentionally per-story (E5.1 Q3); zero-sweep Q1 mechanism removed the extraction trigger
  - epic: "e1"
    action: "Track the #top-bar/<details> coexistence variance to its E7 close-out so dual-chrome is never shipped mid-migration and the neutral-shell pin retires with it"
    owner: "Amelia"
    status: done   # 2026-07-04 (E7.1) — dual-chrome formal sweep shipped in cbfcd35: #top-bar + <details connection|settings> + neutral-shell pins + [data-theme=crt] chrome override removed; debug pane kept; whole-branch review run + recorded. Carry closed.
  - epic: "e2"
    action: "Promote the per-story codified menu-test idioms into one shared location (TESTING doc or test helper) so E3 authors inherit them instead of re-deriving them each story (closes carried-forward E1 action #4)"
    owner: "Amelia"
    status: done   # 2026-07-04 (E5 retro) — same carry as E1 #4; closed as intentionally per-story (E5.1 Q3)
  - epic: "e2"
    action: "Capture code-review outcomes in the story file (one line: N findings, fixed in <sha>) so reviews are visible to retro/audit and not only in git history"
    owner: "Amelia + reviewer"
    status: done   # 2026-07-04 (E5 retro) — root fixed by E4 #2 (scripts/check-story-done-consistency.py); E5.1's Code Review section is recorded
  - epic: "e2"
    action: "Decide the flake endgame once, in writing: accept retries:1 as the permanent mask and stop per-story re-diagnosis, OR fix the root wasm-boot-under-parallelism contention (boot barrier / quarantine boot-race specs)"
    owner: "Amelia"
    status: done   # 2026-07-04 (E5 retro) — Ant DECIDED: accept retries:1 + chromium-transport as the permanent mask; stop per-story re-diagnosis (see E5 action #1 — record in playwright.config.js)
  - epic: "e2"
    action: "Keep the E7 dual-chrome retirement checklist current as E3 adds surfaces so nothing ships mid-migration (continues E1 action #5)"
    owner: "Amelia"
    status: done   # 2026-07-04 (E7.1) — dual-chrome formal sweep shipped in cbfcd35: #top-bar + <details connection|settings> + neutral-shell pins + [data-theme=crt] chrome override removed; debug pane kept; whole-branch review run + recorded. Carry closed.
  - epic: "e2"
    action: "Reconcile story-file Status front-matter with sprint-status on completion, or designate one as authoritative (E2 stories read review while sprint-status reads done)"
    owner: "Amelia"
    status: done   # 2026-07-03 (E3 retro) — all four E3 front-matters read done, matching sprint-status
  - epic: "e3"
    action: "Resolve the codified-idioms carry once and for all (3 epics old): extract the menu-test idioms block into a shared TESTING doc or test helper before E4, OR formally close the action as intentionally per-story. Stop carrying it unresolved. Closes carried-forward E1 #4 / E2 #1."
    owner: "Amelia"
    status: done   # 2026-07-04 (E5 retro) — closed as intentionally per-story (E5.1 Q3); same carry as E1 #4 / E2 #1
  - epic: "e3"
    action: "Run and record code review for E3.1 and E3.4 (both shipped done with a Pending review section), and make review-section-filled a done-gate so it cannot recur. Closes the half-adopted E2 #2."
    owner: "Amelia + reviewer"
    status: done   # 2026-07-04 (E5 retro) — done-gate is now scripts/check-story-done-consistency.py (E4 #2); E5.1 review recorded
  - epic: "e3"
    action: "Frame E4 planning as build-new, not relocate: write acceptance criteria against the spec (subscription + imperative push, holds no independent truth), not against a v1.1 incumbent — the preservation premise that anchored E0-E3 ends here."
    owner: "Amelia"
    status: done   # 2026-07-03 (E4 retro) — E4.1 Context section drops the incumbent anchor; acceptance spec-based (FR-26/AD-6), carried through E4.2/E4.3
  - epic: "e3"
    action: "Carry the E7 dual-chrome close-out forward: settings-slide sub-pane is gone and details#settings is a local-echo/crlf-only vestige; keep the retirement checklist current through E4/E5/E6 so E7 is a clean sweep. Continues E1 #5 / E2 #4."
    owner: "Amelia"
    status: done   # 2026-07-04 (E7.1) — dual-chrome formal sweep shipped in cbfcd35: #top-bar + <details connection|settings> + neutral-shell pins + [data-theme=crt] chrome override removed; debug pane kept; whole-branch review run + recorded. Carry closed.
  - epic: "e4"
    action: "Backfill the three E4 Code Review sections with the reviews that ALREADY ran (findings count + severity + fix sha, or 0 findings; E4.3 fixes are in 8d2795e). Reviews happened — only the record is missing. Owner has the outcomes."
    owner: "Ant + Amelia"
    status: done   # 2026-07-03 — E4.1 clean / E4.2 clean / E4.3 fixes in 8d2795e recorded in each story's Code Review section
  - epic: "e4"
    action: "Fold record-the-review-outcome into the mark-story-done step so it can't recur. Root cause: mark-done flips status but never fills the Code Review section, leaving the dev-story template stub in place — which retros then mis-read as review-skipped. Closes the E2 #2 / E3 #2 ghost at its actual root."
    owner: "Amelia"
    status: done   # 2026-07-04 — scripts/check-story-done-consistency.py asserts every done story's front-matter + Code Review section agree; run on-demand per the mark-story-done convention (manual, no git hook — Ant's call). Fixed drift found on first run: E1.2 front-matter, E3.1 stub.
  - epic: "e4"
    action: "Kill or extract the codified-idioms carry — last call (5 epics): before E5.1 re-derives the block a fifth time, extract it into a shared TESTING doc/test helper, OR formally close it as intentionally per-story and stop listing it. Closes E1 #4 / E2 #1 / E3 #1."
    owner: "Amelia"
    status: done   # 2026-07-04 (E5 retro) — E5.1 Q3 formally closed as intentionally per-story; the block is re-embedded per story by design
  - epic: "e4"
    action: "Carry the E7 dual-chrome close-out forward WITH the E5 exception noted: keep the retirement checklist current through E5/E6, but record that the debug panel is the one pane that does not retire — it relocates behind the Debug menu and stays. Continues E1 #5 / E2 #4 / E3 #4."
    owner: "Amelia"
    status: done   # 2026-07-04 (E7.1) — dual-chrome formal sweep shipped in cbfcd35: #top-bar + <details connection|settings> + neutral-shell pins + [data-theme=crt] chrome override removed; debug pane kept; whole-branch review run + recorded. Carry closed.
  - epic: "e4"
    action: "Watch the status-bar setter surface: the imperative-push API grew a writer per E4 story (setConnectionInfo/setBuild/setZoom/setErrorCount). Before adding more, confirm the fed-never-owned contract still holds and the module isn't becoming a setter grab-bag."
    owner: "Amelia"
    status: open   # 2026-07-04 (E5 retro) — E5 didn't touch status-bar.js; standing watch carries into E6
  - epic: "e5"
    action: "Ratify retries:1 as the permanent flake policy at the source: add a one-line comment in playwright.config.js recording the decision (accept chromium-transport + retries:1; no per-story --workers=1 re-diagnosis) so it is never re-litigated. Closes E2 #3."
    owner: "Amelia"
    status: done   # 2026-07-04 (E6 retro) — verified landed at playwright.config.js:20-27 (policy comment + retries:1); no per-story --workers=1 re-diagnosis in E6. Closes E2 #3.
  - epic: "e5"
    action: "Carry the E7 dual-chrome close-out forward WITH the debug-panel exception recorded: the debug pane is relocated behind the Debug menu and STAYS (the one pane that does not retire). Keep the E7 retirement checklist current through E6; formal sweep is E7. Continues E1 #5 / E2 #4 / E3 #4 / E4 #4."
    owner: "Amelia"
    status: done   # 2026-07-04 (E7.1) — dual-chrome formal sweep shipped in cbfcd35: #top-bar + <details connection|settings> + neutral-shell pins + [data-theme=crt] chrome override removed; debug pane kept; whole-branch review run + recorded. Carry closed.
  - epic: "e5"
    action: "E6 prep: confirm the About modal's build source (window.__buildInfo, kept alive by E4.2, single-sourced with #status-build) and apply the clean-modal aesthetic (aligned rows + info tooltips per key-screen-chrome.html, not verbose panels); verify the source-link TBD placeholder and the verbatim privacy string."
    owner: "Amelia"
    status: done   # 2026-07-04 (E6 retro) — E6.2 read window.__buildInfo (single-sourced with #status-build), aligned-row aesthetic, verbatim privacy string, literal TBD source, Chromium note — all as specified.
  - epic: "e6"
    action: "E7 = the dual-chrome formal sweep. Plan E7 to close the retirement checklist to zero alongside the paste-toast feature: #top-bar removal is the sweep's last act, so verify no dual-chrome ships, retire the neutral-shell pin, and confirm the debug panel is the one recorded pane that stays. Closes the five-epic carry (e1 #5 / e2 #4 / e3 #4 / e4 #4 / e5 #2)."
    owner: "Amelia"
    status: done   # 2026-07-04 (E7.1) — dual-chrome formal sweep shipped in cbfcd35: #top-bar + <details connection|settings> + neutral-shell pins + [data-theme=crt] chrome override removed; debug pane kept; whole-branch review run + recorded. Carry closed.
  - epic: "e6"
    action: "Run the whole-branch review on E7 as the milestone's final sweep. E6 proved trivial stories retire adjacent debt when the review scope is the branch not the diff (12 findings off two clone stories); E7 is the last story, so treat its whole-branch review as the last natural pass over the entire ui-rethink tree before milestone close."
    owner: "Amelia + reviewer"
    status: done   # 2026-07-04 (E7.1) — dual-chrome formal sweep shipped in cbfcd35: #top-bar + <details connection|settings> + neutral-shell pins + [data-theme=crt] chrome override removed; debug pane kept; whole-branch review run + recorded. Carry closed.
  - epic: "e6"
    action: "Guard the must-agree surface before it drifts. Five modals + three build-stamp readers + the conn-dot/BUILD_UNKNOWN_SHA single-sources now hold cross-surface truth. Before E7 adds a toast that reads any shared state, confirm it consumes an existing single-source (do not add a sixth duplicated constant). Continues the setter watch (e4 #5) into its likely close."
    owner: "Amelia"
    status: done   # 2026-07-04 (E7.1) — dual-chrome formal sweep shipped in cbfcd35: #top-bar + <details connection|settings> + neutral-shell pins + [data-theme=crt] chrome override removed; debug pane kept; whole-branch review run + recorded. Carry closed.
  - epic: "e6"
    action: "Follow slide-chip.js for the toast, input/shortcuts.js for any derived content. E7's toast should clone the transient-chip precedent (no new mechanic); if it surfaces any handler-derived text, derive it from a registry like E6.2's shortcuts registry rather than static strings."
    owner: "Amelia"
    status: done   # 2026-07-04 (E7.1) — dual-chrome formal sweep shipped in cbfcd35: #top-bar + <details connection|settings> + neutral-shell pins + [data-theme=crt] chrome override removed; debug pane kept; whole-branch review run + recorded. Carry closed.
project: beastty
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # ── Epic E0 · Shared UI Primitives (depends on: nothing) ──
  epic-e0: done
  e0-1-focus-retention-helper: done
  e0-2-modal-helper-openmodal-send-modal-refactor: done
  epic-e0-retrospective: done

  # ── Epic E1 · Menu Bar Backbone + View Menu (depends on: E0) ──
  epic-e1: in-progress
  e1-1-menu-bar-shell-dropdown-mechanics: done
  e1-2-keyboard-navigation-esc-passthrough-guard: done
  e1-3-chrome-js-decomposition-boot-order-reset-re-projection: done
  e1-4-view-menu-theme-phosphor: done
  e1-5-view-menu-font-zoom-clear: done
  epic-e1-retrospective: done

  # ── Epic E2 · Connection & Serial Configuration (depends on: E0, E1) ──
  epic-e2: in-progress
  e2-1-connect-disconnect-single-writer-menu-item: done
  e2-2-auto-connect-toggle-choose-microbeast: done
  e2-3-serial-configuration-modal: done
  epic-e2-retrospective: done

  # ── Epic E3 · File, Settings & Transfer Configuration (depends on: E0, E1) ──
  epic-e3: in-progress
  e3-1-file-menu-send-file-download-session-log: done
  e3-2-settings-menu-local-echo-enter-key-sends: done
  e3-3-settings-menu-reset-all-preferences-reserved-ctrl-info: done
  e3-4-slide-file-transfer-modal: done
  epic-e3-retrospective: done

  # ── Epic E4 · Bottom Status Bar (depends on: E1, E2) ──
  epic-e4: in-progress
  e4-1-connection-device-baud-readout: done
  e4-2-build-sha-zoom-readout: done
  e4-3-recent-errors-affordance: done
  epic-e4-retrospective: done

  # ── Epic E5 · Debug Menu & In-Page Debug Panel (depends on: E1) ──
  epic-e5: in-progress
  e5-1-debug-menu-panel-toggle: done
  epic-e5-retrospective: done

  # ── Epic E6 · Help Menu (depends on: E0, E1) ──
  epic-e6: in-progress
  e6-1-keyboard-shortcuts-modal: done
  e6-2-about-beastty-modal: done
  epic-e6-retrospective: done

  # ── Epic E7 · Paste Toast (depends on: E0) ──
  epic-e7: done
  e7-1-paste-progress-large-paste-confirm-toast: done
  epic-e7-retrospective: optional

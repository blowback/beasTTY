---
phase: 11
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - www/tests/transport/slide-chip.spec.js
  - www/tests/transport/slide-bridge.spec.js
  - www/tests/transport/slide-compatibility.spec.js
  - www/tests/transport/slide-prefs.spec.js
  - www/tests/transport/mock-serial-slide-bot.js
  - www/state/prefs.js
autonomous: true
requirements: [SLIDE-11, SLIDE-14, SLIDE-25, SLIDE-26, SLIDE-28, SLIDE-31, SLIDE-32, SLIDE-33, SLIDE-35, SLIDE-37, SLIDE-39]

must_haves:
  truths:
    - "All four new Playwright spec files exist with stub `test.skip` declarations covering every SLIDE-* requirement landing in Phase 11"
    - "mock-serial-slide-bot.js exposes `setWakeupDelay(ms)` for the timeout-chip tests"
    - "prefs.js DEFAULTS object contains the three new SLIDE keys"
    - "test:fast and full suite remain green (existing tests unaffected; new stubs registered as skipped)"
  artifacts:
    - path: "www/tests/transport/slide-chip.spec.js"
      provides: "RED-gate stubs for SLIDE-25/26/28 chip lifecycle tests"
      min_lines: 30
    - path: "www/tests/transport/slide-bridge.spec.js"
      provides: "RED-gate stubs for SLIDE-11/14/31/32/33"
      min_lines: 30
    - path: "www/tests/transport/slide-compatibility.spec.js"
      provides: "RED-gate stubs for SLIDE-35/39 Compatibility-mode behavior"
      min_lines: 30
    - path: "www/tests/transport/slide-prefs.spec.js"
      provides: "RED-gate stubs for SLIDE-37/39 prefs persistence"
      min_lines: 20
    - path: "www/tests/transport/mock-serial-slide-bot.js"
      provides: "setWakeupDelay(ms) method + bot.send.wakeupDelayMs field"
      contains: "setWakeupDelay"
    - path: "www/state/prefs.js"
      provides: "DEFAULTS keys slideAutoSendCommand / slideShowSummary / slideCompatibilityMode"
      contains: "slideAutoSendCommand"
  key_links:
    - from: "www/tests/transport/slide-chip.spec.js"
      to: "www/tests/transport/mock-serial-slide-bot.js"
      via: "setWakeupDelay(ms) consumption in awaiting-wakeup-timeout test stub"
      pattern: "setWakeupDelay"
    - from: "www/state/prefs.js"
      to: "DEFAULTS Object.freeze literal"
      via: "three new key/value pairs appended to existing frozen object"
      pattern: "slideAutoSendCommand:"
---

<objective>
Land Wave 0 RED-gate scaffolding for Phase 11: four new Playwright spec
files with `test.skip` stubs covering every SLIDE-* requirement that
Phase 11 ships, the mock-bot extension `setWakeupDelay(ms)` that the
Compatibility-mode timeout tests depend on, and the three new prefs
DEFAULTS keys (`slideAutoSendCommand`, `slideShowSummary`,
`slideCompatibilityMode`).

Purpose: This plan is purely additive — no behavior changes. It pins
the test surface BEFORE any production code lands so that Wave 1-3
plans can drive against named RED gates, and Wave 4 fills the stubs.
This mirrors the Wave 0 pattern that Plans 04-01, 05-01, 06-01,
08-01, 10-01 used successfully (every prior phase's Playwright tests
were authored as `test.skip` stubs first, then filled).

Output: 4 new spec files (~30-50 lines each), one extended mock-bot
file (3-5 lines added), one extended prefs file (3 lines added).
test:fast green; full suite green; new stubs registered as skipped.
</objective>

<execution_context>
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/workflows/execute-plan.md
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-slide-js-bridge-v1-0-integration/11-CONTEXT.md
@.planning/phases/11-slide-js-bridge-v1-0-integration/11-VALIDATION.md
@.planning/phases/11-slide-js-bridge-v1-0-integration/11-PATTERNS.md
@www/tests/transport/slide-cancel.spec.js
@www/tests/transport/slide-recv-settings.spec.js
@www/tests/transport/mock-serial-slide-bot.js
@www/state/prefs.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 4 Playwright RED-gate spec stub files</name>
  <files>
    www/tests/transport/slide-chip.spec.js (NEW),
    www/tests/transport/slide-bridge.spec.js (NEW),
    www/tests/transport/slide-compatibility.spec.js (NEW),
    www/tests/transport/slide-prefs.spec.js (NEW)
  </files>
  <read_first>
    - www/tests/transport/slide-cancel.spec.js (analog setup helper at lines 13-35; commonReset at 25-35; ctrlCanInWriterLog at 61-67; enterMidStream at 41-59 — Plan 11-04 reuses this helper)
    - www/tests/transport/slide-recv-settings.spec.js (analog Settings-driven test scaffolding at 80-104; PICKER_STUB usage)
    - .planning/phases/11-slide-js-bridge-v1-0-integration/11-VALIDATION.md (Per-Task Verification Map — every Wave 1-3 task references a `-g` test name from these stubs)
    - .planning/phases/11-slide-js-bridge-v1-0-integration/11-CONTEXT.md (D-01..D-16 lock the chip text strings each test will eventually assert against)
    - .planning/phases/11-slide-js-bridge-v1-0-integration/11-PATTERNS.md §Pattern Assignments — each NEW spec file (chip / bridge / compatibility / prefs) has explicit analog + adaptation notes
  </read_first>
  <action>
    Create 4 new files with `test.skip` stubs. Use this skeleton verbatim
    in every file (matching the slide-cancel.spec.js + slide-recv-settings.spec.js
    idiom established in Phases 8-10):

    ```js
    import { test } from '@playwright/test';

    // Phase 11 RED-gate stubs (Plan 11-01). Filled by Plan 11-05 with real assertions
    // against the chip + lifecycle wiring landed in Plans 11-02 / 11-03 / 11-04.

    test.describe('<short suite name>', () => {
        test.skip('<test name>', async () => {});
        // ...
    });
    ```

    File 1: `www/tests/transport/slide-chip.spec.js` — chip lifecycle.
    test.describe blocks:
    - `slide-chip — active layout` (SLIDE-25): stubs covering token order
      `↑ MY-DOC.TXT  2/3  47%  482 KB  12.3 KB/s  [Cancel]` (D-01 verbatim);
      direction-arrow swap `↑` send / `↓` recv (D-01); 8.3 filename verbatim
      (D-03); inline [Cancel] click hands off to cancelSlideRecv (D-04).
    - `slide-chip — throughput` (SLIDE-26): stubs covering `—` for first 2 s
      (D-02 first-window rule); auto-scaled units `847 B/s` / `12.3 KB/s` /
      `1.4 MB/s` after first 2 s; 2-second sliding window calculation
      (D-02 sample ring formula).
    - `slide-chip — cancelled summary` (SLIDE-28): stub covering
      `Cancelled — N of M files transferred` text + 5-second auto-hide
      (D-08 says SLIDE-28 always shows regardless of slideShowSummary).

    Minimum 5 stub `test.skip` declarations.

    File 2: `www/tests/transport/slide-bridge.spec.js` — session-log pause +
    paste-pump gate + visibilitychange + port-lost + drop rejected + swallow-echo.
    test.describe blocks:
    - `slide-bridge — session-log pause` (SLIDE-33): stub covering
      session-log-append-paused-while-active (D-11 wraps call site with
      `if (!isSlideActive())`); resumes after session ends.
    - `slide-bridge — paste-pump gate` (SLIDE-33): stub covering
      `pastePump.cancelPaste()` called at wakeup match completion (D-12);
      enqueuePaste no-ops while session active.
    - `slide-bridge — visibilitychange` (SLIDE-31): stub covering
      `visibilitychange` + `pagehide` emit single-byte CTRL_CAN [0x18]
      via writeSlideFrame when active (D-13).
    - `slide-bridge — port lost` (SLIDE-32): stub covering
      slidePumpOnPortLost called from teardown / handleReadError /
      onNavSerialDisconnect (D-14); chip enters error state.
    - `slide-bridge — drop rejected` (SLIDE-11): stub covering chip
      flashDropRejected on dragenter/drop during active session (D-10);
      3-second flash with `Transfer in progress — cancel first` text.
    - `slide-bridge — swallow-echo` (SLIDE-14): stub covering byte-for-byte
      auto-typed-command echo swallow (~500 ms; C-03); mismatch flushes
      remaining buffer to term.feed.

    Minimum 6 stub `test.skip` declarations.

    File 3: `www/tests/transport/slide-compatibility.spec.js` — 3-way Compat mode.
    test.describe blocks:
    - `slide-compatibility — Auto timeout` (SLIDE-35, D-15, D-16): stub covering
      Auto mode arms 3-second timer; chip transitions to awaiting-timeout
      state with text `Z80 didn't respond.  [Retry]  [Cancel]  [Force start]`.
    - `slide-compatibility — Wakeup-required` (SLIDE-39, D-07, D-16): stub covering
      Wakeup-required mode: no timer arms; chip stays in awaiting-wakeup
      indefinitely (asserts no timeout-chip text after 5+ seconds).
    - `slide-compatibility — Force-start` (SLIDE-39, D-07, D-16): stub covering
      Force-start mode: skip wakeup wait entirely; chip enters active state
      immediately on auto-type completion.
    - `slide-compatibility — Retry button` (SLIDE-35): stub covering
      [Retry] click re-emits auto-type and restarts the 3-second timer.
    - `slide-compatibility — Cancel button` (SLIDE-35): stub covering
      [Cancel] click clears pendingSendSession and hides the chip.
    - `slide-compatibility — Force start button` (SLIDE-35): stub covering
      [Force start] click jumps directly into send mode
      (enterSendModeInternal) without waiting for wakeup.

    Minimum 6 stub `test.skip` declarations.

    File 4: `www/tests/transport/slide-prefs.spec.js` — Settings persistence.
    test.describe blocks:
    - `slide-prefs — Settings layout` (SLIDE-39): stub covering DOM presence
      of `<details class="reserved" id="settings-slide">`; summary text
      "SLIDE file transfer" verbatim (D-05); 4 rows in correct order
      (Save-to-folder / Auto-send / Show-summary / Compatibility).
    - `slide-prefs — auto-send command` (SLIDE-37): stub covering
      `<input id="slide-auto-send-input">` default value `B:SLIDE R` (D-06);
      typing + dispatching `change` writes `slideAutoSendCommand: '<value>\\r'`
      to localStorage `bestialitty.prefs` after 250 ms debounce (D-06, C-06).
    - `slide-prefs — show summary` (SLIDE-39): stub covering
      `<input id="slide-show-summary">` default `checked` (D-08); toggle
      persists `slideShowSummary` boolean to localStorage.
    - `slide-prefs — Compatibility mode` (SLIDE-39): stub covering
      `<select id="slide-compat-select">` default value `auto` (D-07);
      changing to `wakeup-required` / `force-start` persists.

    Minimum 4 stub `test.skip` declarations.

    For each `test.skip` declaration, the test name should match the
    `-g` filter in 11-VALIDATION.md Per-Task Verification Map exactly
    (e.g. `'shows two-em-dash separator after first 2 seconds'` matches
    11-VALIDATION row 11-02-02 `npx playwright test ... -g "throughput"`).

    Use `test.skip(...)` form with empty `async () => {}` body — these
    register as skipped (exit 0) and Plan 11-05 fills the bodies.

    Do NOT add any test setup helpers (no setup() / commonReset() / etc.)
    to the stubs. Plan 11-05 will add the helpers at fill time, copied
    from slide-cancel.spec.js per PATTERNS.md.
  </action>
  <verify>
    <automated>cd www && npx playwright test transport/slide-chip.spec.js transport/slide-bridge.spec.js transport/slide-compatibility.spec.js transport/slide-prefs.spec.js --list 2>&amp;1 | grep -E '(skipped|tests in)' | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `www/tests/transport/slide-chip.spec.js` exists
    - File `www/tests/transport/slide-bridge.spec.js` exists
    - File `www/tests/transport/slide-compatibility.spec.js` exists
    - File `www/tests/transport/slide-prefs.spec.js` exists
    - `grep -c 'test.skip' www/tests/transport/slide-chip.spec.js` returns >= 5
    - `grep -c 'test.skip' www/tests/transport/slide-bridge.spec.js` returns >= 6
    - `grep -c 'test.skip' www/tests/transport/slide-compatibility.spec.js` returns >= 6
    - `grep -c 'test.skip' www/tests/transport/slide-prefs.spec.js` returns >= 4
    - `cd www && npx playwright test transport/slide-chip.spec.js transport/slide-bridge.spec.js transport/slide-compatibility.spec.js transport/slide-prefs.spec.js --list` exits 0
    - Total stubs across all 4 files: >= 21 (covers all 11 SLIDE-* requirement IDs)
  </acceptance_criteria>
  <done>
    Four new spec files exist with at least 21 combined `test.skip`
    declarations registered in playwright. Test names match the
    `-g` filters in 11-VALIDATION.md so Plan 11-05 can fill them
    by name. test:fast remains green (no stubs run actual code).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend mock-serial-slide-bot.js with setWakeupDelay(ms)</name>
  <files>
    www/tests/transport/mock-serial-slide-bot.js
  </files>
  <read_first>
    - www/tests/transport/mock-serial-slide-bot.js (existing bot state at lines 121-244; existing `pushSlideHostWakeup` method that currently emits wakeup synchronously; existing API extension pattern at lines 197-235 — `setRole`, `enable`, `setInjectNakOnSeq`, etc.)
    - .planning/phases/11-slide-js-bridge-v1-0-integration/11-PATTERNS.md §`mock-serial-slide-bot.js` adaptation block (line numbers + verbatim code excerpt for the setWakeupDelay extension)
    - .planning/phases/11-slide-js-bridge-v1-0-integration/11-CONTEXT.md D-15 (3 s wakeup timeout — this mock extension is what enables Plan 11-05 timeout-chip tests)
  </read_first>
  <action>
    Add `wakeupDelayMs: 0` field to the existing `bot.send` state object
    (currently around line 140-162). Extend the bot's reset() method
    (around line 193) to clear the delay back to 0.

    Add `setWakeupDelay(ms)` to the bot's public API (alongside `setRole`,
    `enable`, `setInjectNakOnSeq`, etc. at lines 197-235):

    ```js
    setWakeupDelay(ms) { bot.send.wakeupDelayMs = ms | 0; },
    ```

    Modify `pushSlideHostWakeup` (lines ~228-235) to honour the delay:

    ```js
    pushSlideHostWakeup() {
        const wakeBytes = new Uint8Array([0x1B, 0x5E, 0x53, 0x4C, 0x49, 0x44, 0x45]);
        if (bot.send.wakeupDelayMs > 0) {
            setTimeout(() => window.__mockReaderPush(wakeBytes), bot.send.wakeupDelayMs);
        } else {
            window.__mockReaderPush(wakeBytes);
        }
    },
    ```

    Pre-existing `pushSlideWakeup` (recv-direction; no host-initiated send)
    can be left UNCHANGED. The delay applies only to the send-direction
    wakeup that Phase 11 D-15 / SLIDE-35 timeout-chip behavior depends on.

    No other API changes. The existing `bot.reset()` clears
    `bot.send.wakeupDelayMs = 0` so test isolation is preserved.

    The added field becomes part of the introspection-pattern surface but
    does NOT become test introspection itself — Plan 11-05 calls
    `setWakeupDelay(ms)` directly to control test timing.
  </action>
  <verify>
    <automated>grep -n 'setWakeupDelay\|wakeupDelayMs' www/tests/transport/mock-serial-slide-bot.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'setWakeupDelay' www/tests/transport/mock-serial-slide-bot.js` returns >= 1
    - `grep -c 'wakeupDelayMs' www/tests/transport/mock-serial-slide-bot.js` returns >= 3 (field declaration + reset clear + push consumer)
    - Existing `pushSlideHostWakeup` method still exists (no removal)
    - Existing `setRole`, `enable`, `pushSlideWakeup` methods still exist
    - `cd www && npm run test:fast` exits 0 (existing tests use bot without setWakeupDelay; default of 0 preserves prior behavior)
  </acceptance_criteria>
  <done>
    Mock bot exposes `setWakeupDelay(ms)` for Plan 11-05 timeout-chip
    tests. Default delay is 0 so existing Phase 9/10 tests are
    unaffected. test:fast green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add three new prefs DEFAULTS keys</name>
  <files>
    www/state/prefs.js
  </files>
  <read_first>
    - www/state/prefs.js (existing DEFAULTS Object.freeze at lines 18-30; existing CURRENT_VERSION constant; existing defensive merge at line 66; existing IDB_ONLY_FIELDS at line 40; debounced savePrefs at line 86)
    - .planning/phases/11-slide-js-bridge-v1-0-integration/11-CONTEXT.md D-09 (the three exact key/value pairs)
    - .planning/phases/11-slide-js-bridge-v1-0-integration/11-PATTERNS.md §`www/state/prefs.js` adaptation block (verbatim insertion for the three new keys)
    - .planning/phases/06-daily-driver-polish-session-deployment/06-CONTEXT.md D-32 (defensive-merge pattern that fills missing DEFAULTS fields without a CURRENT_VERSION bump — the contract this task relies on)
  </read_first>
  <action>
    In `www/state/prefs.js`, append three keys to the `DEFAULTS = Object.freeze({...})`
    literal at lines 18-30, immediately AFTER the existing `slideRecvToFolder: false`
    line (which is the most-recent precedent — Phase 10):

    ```js
    slideAutoSendCommand: 'B:SLIDE R\r',          // Phase 11 — D-09 (SLIDE-37)
    slideShowSummary: true,                       // Phase 11 — D-09 (D-08 default ON)
    slideCompatibilityMode: 'auto',               // Phase 11 — D-09 ('auto' | 'wakeup-required' | 'force-start')
    ```

    Use the EXACT default values from D-09:
    - `slideAutoSendCommand`: string literal `'B:SLIDE R\r'` (with embedded
      `\r` carriage return — JS string escape, becomes 0x0D byte at consume time).
    - `slideShowSummary`: boolean `true`.
    - `slideCompatibilityMode`: string literal `'auto'` (one of three values
      `'auto'` | `'wakeup-required'` | `'force-start'` per D-07).

    Do NOT bump `CURRENT_VERSION`. The Phase 6 D-32 defensive merge at
    line 66 (`cached = { ...DEFAULTS, ...parsed, serial: { ...DEFAULTS.serial, ...(parsed.serial || {}) } }`)
    fills missing fields from DEFAULTS on load. Existing users whose
    localStorage has the older blob get the new defaults transparently
    on next load.

    Do NOT add the keys to `IDB_ONLY_FIELDS` at line 40. These three
    keys persist via the localStorage `bestialitty.prefs` blob, not
    via IndexedDB.

    Do NOT add a migration step. The defensive-merge pattern is the
    migration.
  </action>
  <verify>
    <automated>grep -n 'slideAutoSendCommand\|slideShowSummary\|slideCompatibilityMode' www/state/prefs.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "slideAutoSendCommand: 'B:SLIDE R" www/state/prefs.js` returns 1
    - `grep -c 'slideShowSummary: true' www/state/prefs.js` returns 1
    - `grep -c "slideCompatibilityMode: 'auto'" www/state/prefs.js` returns 1
    - File still contains `Object.freeze` (DEFAULTS structure preserved)
    - `grep -c 'CURRENT_VERSION = 1' www/state/prefs.js` returns 1 (NOT bumped to 2)
    - `cd www && npm run test:fast` exits 0 (existing prefs tests unaffected by additive keys)
    - `cd www && npx playwright test state/prefs.spec.js` exits 0 if the file exists; otherwise verify via `npm run test:fast` baseline
  </acceptance_criteria>
  <done>
    DEFAULTS contains the three Phase 11 keys with the exact D-09
    values. No version bump. Existing tests green. Wave 1 plans can
    consume `prefs.slideAutoSendCommand`, `prefs.slideShowSummary`,
    and `prefs.slideCompatibilityMode` at boot.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → production code | Wave 0 stubs do not call into production code; bot extensions execute only inside Playwright pages |
| localStorage → JS shell | DEFAULTS extension exposes three keys to whatever consumes prefs.js — Plan 11-03 wires them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-stub-collision | Tampering | Spec stubs | accept | Stubs are `test.skip` only; no production code modified; new test names won't collide with existing because new spec files use new names |
| T-11-01-prefs-injection | Tampering | DEFAULTS literal | accept | `slideAutoSendCommand` stores arbitrary user input; Phase 11 ships without validation per CONTEXT D-09 (Phase 12 SLIDE-38 adds alphanumeric+colon+\\r-only validation as known gap) |
| T-11-01-bot-timing | Denial of Service | Mock-bot setWakeupDelay | accept | Test-only path; setWakeupDelay defaults to 0 so existing tests preserve behavior; Plan 11-05 only sets values < 5000 ms in timeout-chip tests |

## Phase 11 Defaults Acknowledged Gap (CONTEXT D-09)

T-11-prefs-injection: Auto-send command stores arbitrary user input — Phase 11
ships without safety validation; Phase 12 SLIDE-38 adds validation
(alphanumeric + `:` + `\r` only, rejects `;`, pipes, non-`\r` controls).
This is documented as a known gap in CONTEXT D-09. Plan 11-01 simply
makes the storage path exist; the gap is owned by Phase 12.
</threat_model>

<verification>
After all 3 tasks complete:

1. `cd www && npx playwright test transport/slide-chip.spec.js transport/slide-bridge.spec.js transport/slide-compatibility.spec.js transport/slide-prefs.spec.js --list` exits 0 with >= 21 skipped tests reported
2. `cd www && npm run test:fast` exits 0 (full test:fast suite green)
3. `grep -E 'slideAutoSendCommand|slideShowSummary|slideCompatibilityMode' www/state/prefs.js` shows all three keys present in DEFAULTS
4. `grep -E 'setWakeupDelay|wakeupDelayMs' www/tests/transport/mock-serial-slide-bot.js` shows the mock extension is wired
</verification>

<success_criteria>
1. **4 new spec files exist** with at least 21 combined `test.skip` declarations
2. **mock-serial-slide-bot.js** exposes `setWakeupDelay(ms)` and pushSlideHostWakeup honors the delay
3. **prefs.js DEFAULTS** contains the 3 new SLIDE keys with exact D-09 values
4. **test:fast green** — no production behavior change; stubs are skipped
5. **Wave 1 / 2 / 3 plans unblocked** — they can drive against named RED gates and consume the new prefs keys
</success_criteria>

<output>
After completion, create `.planning/phases/11-slide-js-bridge-v1-0-integration/11-01-SUMMARY.md`.
</output>

---
phase: 11
plan: 05
type: execute
wave: 4
depends_on: [11-01, 11-02, 11-03, 11-04]
files_modified:
  - www/tests/transport/slide-chip.spec.js
  - www/tests/transport/slide-bridge.spec.js
  - www/tests/transport/slide-compatibility.spec.js
  - www/tests/transport/slide-prefs.spec.js
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SLIDE-11, SLIDE-14, SLIDE-25, SLIDE-26, SLIDE-28, SLIDE-31, SLIDE-32, SLIDE-33, SLIDE-35, SLIDE-37, SLIDE-39]

must_haves:
  truths:
    - "All Wave 0 stub `test.skip` declarations are replaced with real `test(...)` bodies and assertions"
    - "Chip lifecycle assertions (active layout / throughput / cancelled-summary) match the Plan 11-02/11-03/11-04 implementation"
    - "Bridge assertions (session-log pause / paste-pump gate / visibilitychange / port-lost / drop-rejected / swallow-echo) all pass"
    - "Compatibility-mode 3-way assertions (auto / wakeup-required / force-start + Retry/Cancel/Force-start buttons) all pass"
    - "Prefs persistence assertions (Settings layout + auto-send + show-summary + Compatibility) all pass"
    - "11 SLIDE-* requirement IDs flip from Pending to Complete in REQUIREMENTS.md (top-level checkboxes + traceability table)"
    - "Full Playwright suite green (cd www && npm test)"
    - "cargo test --workspace remains 283/283 (Phase 11 makes zero Rust changes — strict invariant)"
  artifacts:
    - path: "www/tests/transport/slide-chip.spec.js"
      provides: "Real Playwright assertions covering SLIDE-25, SLIDE-26, SLIDE-28"
      min_lines: 200
    - path: "www/tests/transport/slide-bridge.spec.js"
      provides: "Real Playwright assertions covering SLIDE-11, SLIDE-14, SLIDE-31, SLIDE-32, SLIDE-33"
      min_lines: 250
    - path: "www/tests/transport/slide-compatibility.spec.js"
      provides: "Real Playwright assertions covering SLIDE-35, SLIDE-39 Compatibility-mode behaviors"
      min_lines: 200
    - path: "www/tests/transport/slide-prefs.spec.js"
      provides: "Real Playwright assertions covering SLIDE-37, SLIDE-39 prefs persistence"
      min_lines: 100
    - path: ".planning/REQUIREMENTS.md"
      provides: "11 SLIDE-* requirement IDs flipped Pending → Complete"
      contains: "Phase 11 | Complete"
  key_links:
    - from: "www/tests/transport/slide-chip.spec.js"
      to: "www/renderer/slide-chip.js"
      via: "window.__slideChip introspection accessor + chip DOM polling via Playwright"
      pattern: "window.__slideChip"
    - from: "www/tests/transport/slide-bridge.spec.js"
      to: "www/transport/slide-recv.js + www/transport/echo-swallow.js"
      via: "isSlideActive predicate + writeSlideFrame log inspection + swallow filter introspection"
      pattern: "writeSlideFrame|isSlideActive"
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 11 traceability rows"
      via: "11 SLIDE-* requirement IDs flipped to Complete"
      pattern: "Phase 11 | Complete"
---

<objective>
Wave 4 GREEN gate — fill every Wave 0 RED-gate stub from Plan 11-01
with real Playwright assertions. Verify the chip lifecycle, bridge
wiring, Compatibility-mode 3-way branch, and Settings persistence
all behave per CONTEXT.md decisions. Flip the 11 Phase 11 SLIDE-*
requirement IDs from Pending to Complete in REQUIREMENTS.md.

Test patterns mirror the Phase 8/9/10 verification phases verbatim:
mock-bot setup helper (slide-cancel.spec.js:13-35), commonReset
(slide-cancel.spec.js:25-35), enterMidStream (slide-cancel.spec.js:41-59),
ctrlCanInWriterLog helper (slide-cancel.spec.js:61-67), Settings-driven
test scaffolding (slide-recv-settings.spec.js:80-104).

Output: 4 spec files filled with real assertions, REQUIREMENTS.md
flipped, full suite green. After this plan ships, Phase 11 is ready
for /gsd-verify-phase.
</objective>

<execution_context>
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/workflows/execute-plan.md
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-slide-js-bridge-v1-0-integration/11-CONTEXT.md
@.planning/phases/11-slide-js-bridge-v1-0-integration/11-VALIDATION.md
@.planning/phases/11-slide-js-bridge-v1-0-integration/11-PATTERNS.md
@.planning/phases/11-slide-js-bridge-v1-0-integration/11-01-SUMMARY.md
@.planning/phases/11-slide-js-bridge-v1-0-integration/11-02-SUMMARY.md
@.planning/phases/11-slide-js-bridge-v1-0-integration/11-03-SUMMARY.md
@.planning/phases/11-slide-js-bridge-v1-0-integration/11-04-SUMMARY.md
@www/tests/transport/slide-cancel.spec.js
@www/tests/transport/slide-recv-settings.spec.js
@www/tests/transport/slide-chip.spec.js
@www/tests/transport/slide-bridge.spec.js
@www/tests/transport/slide-compatibility.spec.js
@www/tests/transport/slide-prefs.spec.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fill slide-chip.spec.js + slide-prefs.spec.js with real assertions</name>
  <files>
    www/tests/transport/slide-chip.spec.js,
    www/tests/transport/slide-prefs.spec.js
  </files>
  <read_first>
    - www/tests/transport/slide-cancel.spec.js (verbatim setup() + commonReset() + enterMidStream() + ctrlCanInWriterLog() helpers — copy verbatim into both spec files)
    - www/tests/transport/slide-recv-settings.spec.js (verbatim Settings-driven scaffolding pattern; localStorage assertion pattern at the persistence test)
    - www/tests/transport/slide-chip.spec.js (Plan 11-01 stubs — replace test.skip bodies with real test bodies; preserve test names so the -g filters in 11-VALIDATION.md continue to match)
    - www/tests/transport/slide-prefs.spec.js (Plan 11-01 stubs — same fill pattern)
    - .planning/phases/11-slide-js-bridge-v1-0-integration/11-VALIDATION.md (per-task verification map — every test name must match its `-g` filter exactly)
    - .planning/phases/11-slide-js-bridge-v1-0-integration/11-CONTEXT.md (D-01 layout / D-02 throughput / D-08 summary copy / D-06 \r append-at-save / D-07 Compatibility 3-way / D-09 prefs DEFAULTS)
    - .planning/phases/11-slide-js-bridge-v1-0-integration/11-PATTERNS.md §slide-chip.spec.js + §slide-prefs.spec.js — adaptation notes
  </read_first>
  <action>
    **Replace Plan 11-01 test.skip stubs with real test bodies.** Use
    setup helpers copied verbatim from slide-cancel.spec.js / slide-recv-settings.spec.js
    (do NOT re-export them; copy as private helpers in each spec file).

    **slide-chip.spec.js:** Common helpers at top of file:
    ```js
    import { test, expect } from '@playwright/test';
    import { SERIAL_MOCK } from './mock-serial.js';
    import { MOCK_SERIAL_SLIDE_BOT } from './mock-serial-slide-bot.js';

    async function setup(page) {
        await page.addInitScript(SERIAL_MOCK);
        await page.addInitScript(MOCK_SERIAL_SLIDE_BOT);
        await page.goto('/');
        await page.locator('#terminal-wrapper').focus();
        await page.waitForFunction(() => document.getElementById('terminal').width > 0);
        await page.locator('#connection').evaluate((el) => { el.open = true; });
    }

    async function commonReset(page) {
        await page.locator('#connect-button').click();
        await expect.poll(
            () => page.evaluate(() => Boolean(navigator.serial._grantedPorts[0]?._reader)),
            { timeout: 5000 },
        ).toBe(true);
        await page.evaluate(() => {
            window.__slide.__resetForTests();
            window.__slideRecv.__resetForTests();
            window.__slideChip.__resetForTests();
            window.__mockWriterLog.length = 0;
            window.__mockSlideBot.reset();
            window.__mockSlideBot.setRole('send');
        });
    }

    async function enterMidStream(page, fileSize) {
        await page.evaluate((size) => {
            const bytes = new Uint8Array(size);
            for (let i = 0; i < size; i++) bytes[i] = i & 0xFF;
            window.__mockSlideBot.send.pauseAfterFirstWindow = true;
            window.__mockSlideBot.queueSendFiles([{ name: 'MY-DOC.TXT', bytes }]);
            window.__mockSlideBot.pushSlideHostWakeup();
        }, fileSize);
        await expect.poll(
            () => page.evaluate(() => window.__slide.__getStateForTests().mode),
            { timeout: 5000 },
        ).toBe('recv');
        await page.evaluate(() => window.__mockSlideBot.startSendSession());
        await expect.poll(
            () => page.evaluate(() => window.__slideRecv.__getStateForTests().bytesInFileDone > 0),
            { timeout: 5000 },
        ).toBe(true);
    }
    ```

    **slide-chip — active layout (SLIDE-25):** Real test:
    ```js
    test.describe('slide-chip — active layout', () => {
        test.beforeEach(async ({ page }) => { await setup(page); });

        test('renders dense single-line tokens with two-space separators', async ({ page }) => {
            await commonReset(page);
            await enterMidStream(page, 4096);
            await expect.poll(
                () => page.evaluate(() => window.__slideChip.__getStateForTests().lifecycle),
                { timeout: 5000 },
            ).toBe('active');
            const text = await page.locator('#slide-chip-text').textContent();
            // D-01 token order — direction arrow (recv ↓) + filename + N/M + percent + bytes + throughput + Cancel.
            expect(text).toMatch(/^↓ MY-DOC\.TXT  \d+\/\d+  \d+%/);
            expect(text).toMatch(/\[Cancel\]\s*$/);
        });

        test('inline Cancel click hands off to cancelSlideRecv', async ({ page }) => {
            await commonReset(page);
            await enterMidStream(page, 4096);
            await expect.poll(
                () => page.evaluate(() => window.__slideChip.__getStateForTests().lifecycle),
                { timeout: 5000 },
            ).toBe('active');
            await page.locator('#slide-chip button[data-action="cancel"]').click();
            // Phase 10 5-step CTRL_CAN sequence emits 0x18 within ~200 ms.
            await expect.poll(
                () => page.evaluate(() => window.__mockWriterLog.some((e) =>
                    e.bytes && e.bytes.some((b) => b === 0x18))),
                { timeout: 2000 },
            ).toBe(true);
        });
    });
    ```

    **slide-chip — throughput (SLIDE-26):** Real tests:
    ```js
    test.describe('slide-chip — throughput', () => {
        test.beforeEach(async ({ page }) => { await setup(page); });

        test('shows em-dash for first 2 seconds', async ({ page }) => {
            await commonReset(page);
            await enterMidStream(page, 4096);
            await expect.poll(
                () => page.evaluate(() => window.__slideChip.__getStateForTests().lifecycle),
                { timeout: 5000 },
            ).toBe('active');
            // First poll within 1 second — chip text contains the em-dash placeholder per D-02.
            const text = await page.locator('#slide-chip-text').textContent();
            expect(text).toContain('—');
        });

        test('auto-scales to KB/s after 2 seconds', async ({ page }) => {
            await commonReset(page);
            await enterMidStream(page, 16384);
            // Wait 2.5 s for the sliding window to populate.
            await page.waitForTimeout(2500);
            await expect.poll(
                () => page.evaluate(() => {
                    const t = document.getElementById('slide-chip-text').textContent;
                    return /\b\d+\.?\d* (B|KB|MB)\/s\b/.test(t);
                }),
                { timeout: 3000 },
            ).toBe(true);
        });
    });
    ```

    **slide-chip — cancelled summary (SLIDE-28):** Real test:
    ```js
    test.describe('slide-chip — cancelled summary', () => {
        test.beforeEach(async ({ page }) => { await setup(page); });

        test('shows Cancelled — N of M files transferred for 5 seconds', async ({ page }) => {
            await commonReset(page);
            await enterMidStream(page, 4096);
            await expect.poll(
                () => page.evaluate(() => window.__slideChip.__getStateForTests().lifecycle),
                { timeout: 5000 },
            ).toBe('active');
            // Programmatically trigger cancelled-summary state (mirrors what cancelSlideRecv does at session end).
            await page.evaluate(() => {
                window.__slideChip.enterCancelledSummary({ done: 0, total: 1 });
            });
            const text = await page.locator('#slide-chip-text').textContent();
            expect(text).toBe('Cancelled — 0 of 1 files transferred');
            // Auto-hide kicks in within 5.5 s (5s timer + slack).
            await expect.poll(
                () => page.evaluate(() => document.getElementById('slide-chip').hasAttribute('hidden')),
                { timeout: 6000 },
            ).toBe(true);
        });
    });
    ```

    **slide-prefs.spec.js:** Common setup helper (no mock-bot needed for
    pure persistence tests):
    ```js
    import { test, expect } from '@playwright/test';
    import { SERIAL_MOCK } from './mock-serial.js';

    async function setup(page) {
        await page.addInitScript(SERIAL_MOCK);
        await page.goto('/');
        await page.locator('#terminal-wrapper').focus();
        await page.waitForFunction(() => document.getElementById('terminal').width > 0);
        await page.locator('#settings').evaluate((el) => { el.open = true; });
        await page.locator('#settings-slide').evaluate((el) => { el.open = true; });
    }
    ```

    **slide-prefs — Settings layout (SLIDE-39):** Real test:
    ```js
    test('Settings layout — nested details + 4 rows', async ({ page }) => {
        await setup(page);
        await expect(page.locator('#settings-slide')).toHaveCount(1);
        await expect(page.locator('#settings-slide > summary')).toHaveText('SLIDE file transfer');
        // Row 1: Save-to-folder (carry-forward).
        await expect(page.locator('#slide-recv-folder-row')).toHaveCount(1);
        // Row 2-4: new Phase 11 rows.
        await expect(page.locator('#slide-auto-send-input')).toHaveCount(1);
        await expect(page.locator('#slide-show-summary')).toHaveCount(1);
        await expect(page.locator('#slide-compat-select')).toHaveCount(1);
    });
    ```

    **slide-prefs — auto-send command (SLIDE-37):** Real test:
    ```js
    test('auto-send command — default value B:SLIDE R', async ({ page }) => {
        await setup(page);
        await expect(page.locator('#slide-auto-send-input')).toHaveValue('B:SLIDE R');
    });

    test('auto-send command — change appends \\\\r at save time', async ({ page }) => {
        await setup(page);
        await page.locator('#slide-auto-send-input').fill('A:RUN PROG.COM');
        await page.locator('#slide-auto-send-input').dispatchEvent('change');
        await page.waitForTimeout(300);   // 250 ms debounce + slack
        const blob = await page.evaluate(() => JSON.parse(localStorage.getItem('bestialitty.prefs')));
        expect(blob.slideAutoSendCommand).toBe('A:RUN PROG.COM\r');
    });
    ```

    **slide-prefs — show summary (SLIDE-39):** Real test:
    ```js
    test('show summary — default ON; toggle persists', async ({ page }) => {
        await setup(page);
        await expect(page.locator('#slide-show-summary')).toBeChecked();
        await page.locator('#slide-show-summary').uncheck();
        await page.waitForTimeout(300);
        const blob = await page.evaluate(() => JSON.parse(localStorage.getItem('bestialitty.prefs')));
        expect(blob.slideShowSummary).toBe(false);
    });
    ```

    **slide-prefs — Compatibility mode (SLIDE-39):** Real test:
    ```js
    test('Compatibility mode — default auto; 3-way persists', async ({ page }) => {
        await setup(page);
        await expect(page.locator('#slide-compat-select')).toHaveValue('auto');
        for (const mode of ['wakeup-required', 'force-start', 'auto']) {
            await page.locator('#slide-compat-select').selectOption(mode);
            await page.waitForTimeout(300);
            const blob = await page.evaluate(() => JSON.parse(localStorage.getItem('bestialitty.prefs')));
            expect(blob.slideCompatibilityMode).toBe(mode);
        }
    });
    ```

    Replace EVERY `test.skip(...)` from Plan 11-01 with `test(...)` and
    a real body that asserts the corresponding D-* decision.
  </action>
  <verify>
    <automated>cd www && npx playwright test transport/slide-chip.spec.js transport/slide-prefs.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'test.skip' www/tests/transport/slide-chip.spec.js` returns 0 (all stubs filled)
    - `grep -c 'test.skip' www/tests/transport/slide-prefs.spec.js` returns 0 (all stubs filled)
    - `grep -c "test('" www/tests/transport/slide-chip.spec.js` returns >= 5
    - `grep -c "test('" www/tests/transport/slide-prefs.spec.js` returns >= 4
    - `cd www && npx playwright test transport/slide-chip.spec.js` exits 0
    - `cd www && npx playwright test transport/slide-prefs.spec.js` exits 0
    - All test names match the `-g` filters in 11-VALIDATION.md (e.g. 'active layout', 'throughput', 'cancelled summary', 'auto-send command', 'show summary', 'Compatibility mode')
  </acceptance_criteria>
  <done>
    slide-chip.spec.js and slide-prefs.spec.js have all stubs replaced
    with real assertions. Tests run green. Plan 11-02/11-03 chip
    lifecycle and Settings persistence are verified end-to-end.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fill slide-bridge.spec.js + slide-compatibility.spec.js with real assertions</name>
  <files>
    www/tests/transport/slide-bridge.spec.js,
    www/tests/transport/slide-compatibility.spec.js
  </files>
  <read_first>
    - www/tests/transport/slide-bridge.spec.js (Plan 11-01 stubs — replace bodies)
    - www/tests/transport/slide-compatibility.spec.js (Plan 11-01 stubs — replace bodies)
    - www/tests/transport/slide-cancel.spec.js (port-lost teardown pattern; ctrlCanInWriterLog helper)
    - www/tests/transport/paste.spec.js (paste-pump cancel pattern)
    - www/tests/transport/slide-recv-settings.spec.js (Settings-driven scaffolding for the Compatibility-mode 3-way tests)
    - www/tests/transport/mock-serial-slide-bot.js (setWakeupDelay extension from Plan 11-01)
    - www/tests/transport/slide-chip.spec.js (Plan 11-05 Task 1 — verify the helper-copy pattern; do NOT import from there, copy verbatim per Phase 8/9/10 precedent)
    - .planning/phases/11-slide-js-bridge-v1-0-integration/11-VALIDATION.md (test names + -g filters)
    - .planning/phases/11-slide-js-bridge-v1-0-integration/11-CONTEXT.md (D-11 / D-12 / D-13 / D-14 / D-15 / D-16 / C-03 — every test asserts a specific decision)
  </read_first>
  <action>
    **slide-bridge.spec.js:** Copy the same setup() / commonReset() /
    enterMidStream() / ctrlCanInWriterLog() helpers verbatim from
    slide-cancel.spec.js. Then fill stubs:

    **slide-bridge — session-log pause (SLIDE-33):**
    ```js
    test('session-log paused while SLIDE active; resumes after', async ({ page }) => {
        await commonReset(page);
        // Snapshot session-log size BEFORE SLIDE active.
        const before = await page.evaluate(() => window.__sessionLog.getCurrentBytes().length);
        await enterMidStream(page, 4096);
        // Drive multi-KB through during active session.
        await page.waitForTimeout(500);
        const during = await page.evaluate(() => window.__sessionLog.getCurrentBytes().length);
        expect(during).toBe(before);   // D-11: log paused — bytes match snapshot
        // End session and verify log resumes.
        await page.evaluate(() => window.__slide.cancelRecv());
        await page.evaluate(() => window.__mockReaderPush(new Uint8Array([0x68, 0x69])));   // 'hi'
        await page.waitForTimeout(200);
        const after = await page.evaluate(() => window.__sessionLog.getCurrentBytes().length);
        expect(after).toBeGreaterThan(during);   // log resumed
    });
    ```

    **slide-bridge — paste-pump gate (SLIDE-33):**
    ```js
    test('paste-pump cancelled at SLIDE wakeup; enqueuePaste no-ops while active', async ({ page }) => {
        await commonReset(page);
        // Queue a paste.
        await page.evaluate(() => {
            window.__pastePump.enqueuePaste(new TextEncoder().encode('Z'.repeat(2000)));
        });
        await expect.poll(
            () => page.evaluate(() => window.__pastePump.__getStateForTests().active),
            { timeout: 1000 },
        ).toBe(true);
        // Trigger SLIDE wakeup; D-12 should cancel the paste.
        await enterMidStream(page, 1024);
        await expect.poll(
            () => page.evaluate(() => window.__pastePump.__getStateForTests().active),
            { timeout: 2000 },
        ).toBe(false);
        // Subsequent enqueuePaste no-ops.
        const writerLogBefore = await page.evaluate(() => window.__mockWriterLog.length);
        await page.evaluate(() => window.__pastePump.enqueuePaste(new Uint8Array([0x41, 0x42])));
        await page.waitForTimeout(200);
        const writerLogAfter = await page.evaluate(() => window.__mockWriterLog.length);
        // No new writer entries from the paste — paste was no-op'd.
        // (Some writer activity from the SLIDE session itself is allowed; we just verify the paste didn't add writes for 0x41 0x42.)
        const newPasteBytes = await page.evaluate(() =>
            window.__mockWriterLog.slice(-5).some((e) => e.bytes && e.bytes.some((b) => b === 0x41 || b === 0x42)));
        expect(newPasteBytes).toBe(false);
    });
    ```

    **slide-bridge — visibilitychange (SLIDE-31):**
    ```js
    test('visibilitychange emits CTRL_CAN when active', async ({ page }) => {
        await commonReset(page);
        await enterMidStream(page, 4096);
        await page.evaluate(() => window.__mockWriterLog.length = 0);   // reset writer log
        // Synthesize visibilitychange to hidden.
        await page.evaluate(() => {
            Object.defineProperty(document, 'visibilityState', { value: 'hidden', configurable: true });
            Object.defineProperty(document, 'hidden', { value: true, configurable: true });
            document.dispatchEvent(new Event('visibilitychange'));
        });
        await expect.poll(
            () => page.evaluate(() =>
                window.__mockWriterLog.some((e) => e.bytes && e.bytes.length === 1 && e.bytes[0] === 0x18)),
            { timeout: 2000 },
        ).toBe(true);
    });

    test('pagehide emits CTRL_CAN when active', async ({ page }) => {
        await commonReset(page);
        await enterMidStream(page, 4096);
        await page.evaluate(() => window.__mockWriterLog.length = 0);
        await page.evaluate(() => {
            window.dispatchEvent(new Event('pagehide'));
        });
        await expect.poll(
            () => page.evaluate(() =>
                window.__mockWriterLog.some((e) => e.bytes && e.bytes.length === 1 && e.bytes[0] === 0x18)),
            { timeout: 2000 },
        ).toBe(true);
    });
    ```

    **slide-bridge — port lost (SLIDE-32):**
    ```js
    test('slidePumpOnPortLost called from teardown; chip enters error', async ({ page }) => {
        await commonReset(page);
        await enterMidStream(page, 4096);
        // Simulate port disconnect.
        await page.evaluate(() => {
            const port = navigator.serial._grantedPorts[0];
            if (port && port._reader) {
                port._reader.releaseLock();
            }
            navigator.serial.dispatchEvent(new CustomEvent('disconnect', { detail: { port } }));
        });
        // Chip should transition to error state.
        await expect.poll(
            () => page.evaluate(() => window.__slideChip.__getStateForTests().lifecycle),
            { timeout: 3000 },
        ).toBe('error');
    });
    ```

    **slide-bridge — drop rejected (SLIDE-11):**
    ```js
    test('drag-drop during active SLIDE flashes chip with rejection text', async ({ page }) => {
        await commonReset(page);
        await enterMidStream(page, 4096);
        // Synthesize dragenter on terminal-wrapper.
        await page.evaluate(() => {
            const dt = new DataTransfer();
            dt.items.add(new File(['x'], 'x.txt', { type: 'text/plain' }));
            const ev = new DragEvent('dragenter', { dataTransfer: dt, bubbles: true });
            document.getElementById('terminal-wrapper').dispatchEvent(ev);
        });
        const text = await page.locator('#slide-chip-text').textContent();
        expect(text).toContain('Transfer in progress — cancel first');
        // After 3 seconds + tick, reverts to active state.
        await page.waitForTimeout(3500);
        const after = await page.locator('#slide-chip-text').textContent();
        expect(after).not.toContain('Transfer in progress — cancel first');
    });
    ```

    **slide-bridge — swallow-echo (SLIDE-14):**
    ```js
    test('auto-typed echo swallowed byte-for-byte within 500 ms', async ({ page }) => {
        await commonReset(page);
        // Trigger send-mode entry which auto-types B:SLIDE R\r.
        await page.evaluate(() => {
            const file = new File([new Uint8Array([0x41])], 'A.TXT');
            window.__slide.enterSendMode({ files: [file] });
        });
        await page.waitForTimeout(50);
        // Push CP/M echo bytes back via the mock reader.
        await page.evaluate(() => {
            window.__mockReaderPush(new TextEncoder().encode('B:SLIDE R\r'));
        });
        await page.waitForTimeout(100);
        // Bytes should be swallowed; terminal grid should NOT contain a duplicate "B:SLIDE R" line.
        const gridText = await page.evaluate(() => window.__testGridView ? window.__testGridView() : '');
        // Phase 4 D-12 local-echo OR Phase 5 read loop already painted; the SWALLOWED echo means no second copy on the canvas.
        // Assertion: count of "B:SLIDE R" occurrences is at most 1 (local echo only).
        const matches = (gridText.match(/B:SLIDE R/g) || []).length;
        expect(matches).toBeLessThanOrEqual(1);
    });
    ```

    **slide-compatibility.spec.js:** Use Settings-driven setup helper.

    **slide-compatibility — Auto timeout (SLIDE-35):**
    ```js
    test('Auto mode arms 3 s timer; chip enters awaiting-timeout state', async ({ page }) => {
        await commonReset(page);
        // Set Compatibility mode to Auto (default).
        await page.evaluate(() => {
            window.__mockSlideBot.setWakeupDelay(5000);   // > 3 s, exceeds timeout
        });
        await page.evaluate(() => {
            const file = new File([new Uint8Array([0x41])], 'A.TXT');
            window.__slide.enterSendMode({ files: [file] });
        });
        // Wait for chip to enter awaiting-timeout state (3 s + 250 ms slack).
        await expect.poll(
            () => page.evaluate(() => window.__slideChip.__getStateForTests().lifecycle),
            { timeout: 4000 },
        ).toBe('awaiting-timeout');
        const text = await page.locator('#slide-chip-text').textContent();
        expect(text).toContain("Z80 didn't respond.");
        expect(text).toContain('[Retry]');
        expect(text).toContain('[Cancel]');
        expect(text).toContain('[Force start]');
    });
    ```

    **slide-compatibility — Wakeup-required (SLIDE-39):**
    ```js
    test('Wakeup-required mode does not arm timer', async ({ page }) => {
        await setup(page);
        await page.locator('#slide-compat-select').selectOption('wakeup-required');
        await page.waitForTimeout(300);
        await commonReset(page);
        await page.evaluate(() => {
            window.__mockSlideBot.setWakeupDelay(5000);
        });
        await page.evaluate(() => {
            const file = new File([new Uint8Array([0x41])], 'A.TXT');
            window.__slide.enterSendMode({ files: [file] });
        });
        // Wait 4 s — should still be in awaiting-wakeup, not awaiting-timeout.
        await page.waitForTimeout(4000);
        const lifecycle = await page.evaluate(() => window.__slideChip.__getStateForTests().lifecycle);
        expect(lifecycle).toBe('awaiting-wakeup');
        const hasTimer = await page.evaluate(() => window.__slideChip.__getStateForTests().hasWakeupTimer);
        expect(hasTimer).toBe(false);
    });
    ```

    **slide-compatibility — Force-start (SLIDE-39):**
    ```js
    test('Force-start mode skips wakeup wait; jumps to send mode', async ({ page }) => {
        await setup(page);
        await page.locator('#slide-compat-select').selectOption('force-start');
        await page.waitForTimeout(300);
        await commonReset(page);
        await page.evaluate(() => {
            window.__mockSlideBot.setWakeupDelay(10000);   // would never arrive in test window
        });
        await page.evaluate(() => {
            const file = new File([new Uint8Array([0x41])], 'A.TXT');
            window.__slide.enterSendMode({ files: [file] });
        });
        // Force-start should jump immediately into send mode.
        await expect.poll(
            () => page.evaluate(() => window.__slide.__getStateForTests().mode),
            { timeout: 1000 },
        ).toBe('send');
    });
    ```

    **slide-compatibility — Retry button (SLIDE-35):**
    ```js
    test('[Retry] click re-emits auto-type and restarts 3 s timer', async ({ page }) => {
        await commonReset(page);
        await page.evaluate(() => window.__mockSlideBot.setWakeupDelay(5000));
        await page.evaluate(() => {
            const file = new File([new Uint8Array([0x41])], 'A.TXT');
            window.__slide.enterSendMode({ files: [file] });
        });
        await expect.poll(
            () => page.evaluate(() => window.__slideChip.__getStateForTests().lifecycle),
            { timeout: 4000 },
        ).toBe('awaiting-timeout');
        const writeCountBefore = await page.evaluate(() => window.__mockWriterLog.length);
        await page.locator('#slide-chip button[data-action="retry"]').click();
        await page.waitForTimeout(200);
        const writeCountAfter = await page.evaluate(() => window.__mockWriterLog.length);
        expect(writeCountAfter).toBeGreaterThan(writeCountBefore);   // auto-type re-emitted
        const lifecycle = await page.evaluate(() => window.__slideChip.__getStateForTests().lifecycle);
        expect(lifecycle).toBe('awaiting-wakeup');   // back to awaiting state
    });
    ```

    **slide-compatibility — Cancel button (SLIDE-35):**
    ```js
    test('[Cancel] click clears pendingSendSession; chip hides', async ({ page }) => {
        await commonReset(page);
        await page.evaluate(() => window.__mockSlideBot.setWakeupDelay(5000));
        await page.evaluate(() => {
            const file = new File([new Uint8Array([0x41])], 'A.TXT');
            window.__slide.enterSendMode({ files: [file] });
        });
        await expect.poll(
            () => page.evaluate(() => window.__slideChip.__getStateForTests().lifecycle),
            { timeout: 4000 },
        ).toBe('awaiting-timeout');
        await page.locator('#slide-chip button[data-action="cancel"]').click();
        await expect.poll(
            () => page.evaluate(() => window.__slideChip.__getStateForTests().lifecycle),
            { timeout: 1000 },
        ).toBe('hidden');
    });
    ```

    **slide-compatibility — Force start button (SLIDE-35):**
    ```js
    test('[Force start] click enters send mode immediately', async ({ page }) => {
        await commonReset(page);
        await page.evaluate(() => window.__mockSlideBot.setWakeupDelay(5000));
        await page.evaluate(() => {
            const file = new File([new Uint8Array([0x41])], 'A.TXT');
            window.__slide.enterSendMode({ files: [file] });
        });
        await expect.poll(
            () => page.evaluate(() => window.__slideChip.__getStateForTests().lifecycle),
            { timeout: 4000 },
        ).toBe('awaiting-timeout');
        await page.locator('#slide-chip button[data-action="force-start"]').click();
        await expect.poll(
            () => page.evaluate(() => window.__slide.__getStateForTests().mode),
            { timeout: 1000 },
        ).toBe('send');
    });
    ```

    Replace EVERY test.skip stub with a real test. Test names match
    the `-g` filters in 11-VALIDATION.md.
  </action>
  <verify>
    <automated>cd www && npx playwright test transport/slide-bridge.spec.js transport/slide-compatibility.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'test.skip' www/tests/transport/slide-bridge.spec.js` returns 0
    - `grep -c 'test.skip' www/tests/transport/slide-compatibility.spec.js` returns 0
    - `grep -c "test('" www/tests/transport/slide-bridge.spec.js` returns >= 6
    - `grep -c "test('" www/tests/transport/slide-compatibility.spec.js` returns >= 6
    - `cd www && npx playwright test transport/slide-bridge.spec.js` exits 0
    - `cd www && npx playwright test transport/slide-compatibility.spec.js` exits 0
    - Each test has at least one `expect(...)` assertion (no empty bodies remaining)
  </acceptance_criteria>
  <done>
    Bridge wiring (D-11/D-12/D-13/D-14) and Compatibility-mode behavior
    (D-07/D-15/D-16) are verified end-to-end. All Wave 0 stubs filled.
    Plan 11-04's awaiting-timeout transition + Retry/Cancel/Force-start
    handlers prove out.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Flip 11 SLIDE-* requirement IDs in REQUIREMENTS.md + run full suite</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (top-level checkboxes for SLIDE-11/14/25/26/28/31/32/33/35/37/39 — currently `[ ]`); §Traceability table rows for the same IDs (currently `Phase 11 | Pending`)
    - .planning/phases/11-slide-js-bridge-v1-0-integration/11-VALIDATION.md (per-task verification map confirming every SLIDE-* ID has a passing test)
    - .planning/phases/10-slide-receiver-cancellation/10-05-SUMMARY.md (precedent for the bulk Pending → Complete flip pattern — 11 rows flipped at end of Phase 10 verification)
  </read_first>
  <action>
    Run the full Playwright suite and confirm green:

    ```bash
    cd www && npm test
    ```

    Then update REQUIREMENTS.md by editing TWO sections per requirement
    ID:

    **§v1.1 Requirements top-level checkboxes** — flip `[ ]` to `[x]`
    for every Phase 11 SLIDE-* ID:

    - `[ ] **SLIDE-11**` → `[x] **SLIDE-11**`
    - `[ ] **SLIDE-14**` → `[x] **SLIDE-14**`
    - `[ ] **SLIDE-25**` → `[x] **SLIDE-25**`
    - `[ ] **SLIDE-26**` → `[x] **SLIDE-26**`
    - `[ ] **SLIDE-28**` → `[x] **SLIDE-28**`
    - `[ ] **SLIDE-31**` → `[x] **SLIDE-31**`
    - `[ ] **SLIDE-32**` → `[x] **SLIDE-32**`
    - `[ ] **SLIDE-33**` → `[x] **SLIDE-33**`
    - `[ ] **SLIDE-35**` → `[x] **SLIDE-35**`
    - `[ ] **SLIDE-37**` → `[x] **SLIDE-37**`
    - `[ ] **SLIDE-39**` → `[x] **SLIDE-39**`

    **§Traceability table rows** — flip status for every Phase 11 ID:

    - `| SLIDE-11 | Phase 11 | Pending |` → `| SLIDE-11 | Phase 11 | Complete |`
    - `| SLIDE-14 | Phase 11 | Pending |` → `| SLIDE-14 | Phase 11 | Complete |`
    - `| SLIDE-25 | Phase 11 | Pending |` → `| SLIDE-25 | Phase 11 | Complete |`
    - `| SLIDE-26 | Phase 11 | Pending |` → `| SLIDE-26 | Phase 11 | Complete |`
    - `| SLIDE-28 | Phase 11 | Pending |` → `| SLIDE-28 | Phase 11 | Complete |`
    - `| SLIDE-31 | Phase 11 | Pending |` → `| SLIDE-31 | Phase 11 | Complete |`
    - `| SLIDE-32 | Phase 11 | Pending |` → `| SLIDE-32 | Phase 11 | Complete |`
    - `| SLIDE-33 | Phase 11 | Pending |` → `| SLIDE-33 | Phase 11 | Complete |`
    - `| SLIDE-35 | Phase 11 | Pending |` → `| SLIDE-35 | Phase 11 | Complete |`
    - `| SLIDE-37 | Phase 11 | Pending |` → `| SLIDE-37 | Phase 11 | Complete |`
    - `| SLIDE-39 | Phase 11 | Pending |` → `| SLIDE-39 | Phase 11 | Complete |`

    Do NOT flip Phase 12 IDs (SLIDE-12, SLIDE-36, SLIDE-38, SLIDE-40,
    SLIDE-41, SLIDE-42) — those land in Phase 12.

    Confirm cargo workspace remains green (Phase 11 makes zero Rust
    changes — strict invariant):

    ```bash
    cargo test --workspace
    ```

    Confirm slide_wasm_boundary_shape.rs still passes (boundary is
    locked from Phase 10):

    ```bash
    cargo test --test slide_wasm_boundary_shape
    ```

    Confirm core_02_no_browser_deps.rs invariant (no std::time, no
    web_sys, no js_sys, no wasm_bindgen leakage in core):

    ```bash
    cargo test --test core_02_no_browser_deps
    ```

    Confirm `bash scripts/build.sh` exits 0.
  </action>
  <verify>
    <automated>cd www && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[x\] \*\*SLIDE-11\*\*' .planning/REQUIREMENTS.md` returns 1
    - `grep -c '\[x\] \*\*SLIDE-14\*\*' .planning/REQUIREMENTS.md` returns 1
    - `grep -c '\[x\] \*\*SLIDE-25\*\*' .planning/REQUIREMENTS.md` returns 1
    - `grep -c '\[x\] \*\*SLIDE-26\*\*' .planning/REQUIREMENTS.md` returns 1
    - `grep -c '\[x\] \*\*SLIDE-28\*\*' .planning/REQUIREMENTS.md` returns 1
    - `grep -c '\[x\] \*\*SLIDE-31\*\*' .planning/REQUIREMENTS.md` returns 1
    - `grep -c '\[x\] \*\*SLIDE-32\*\*' .planning/REQUIREMENTS.md` returns 1
    - `grep -c '\[x\] \*\*SLIDE-33\*\*' .planning/REQUIREMENTS.md` returns 1
    - `grep -c '\[x\] \*\*SLIDE-35\*\*' .planning/REQUIREMENTS.md` returns 1
    - `grep -c '\[x\] \*\*SLIDE-37\*\*' .planning/REQUIREMENTS.md` returns 1
    - `grep -c '\[x\] \*\*SLIDE-39\*\*' .planning/REQUIREMENTS.md` returns 1
    - `grep -c 'SLIDE-11 | Phase 11 | Complete' .planning/REQUIREMENTS.md` returns 1
    - `grep -c 'Phase 11 | Pending' .planning/REQUIREMENTS.md` returns 0 (all 11 Phase 11 rows flipped)
    - `cd www && npm test` exits 0 (full Playwright suite green)
    - `cargo test --workspace` exits 0 with the same test count as Phase 10 close (283/283 — Phase 11 makes ZERO Rust changes)
    - `bash scripts/build.sh` exits 0
  </acceptance_criteria>
  <done>
    All 11 SLIDE-* requirement IDs flipped Pending → Complete.
    Full Playwright suite green. Cargo workspace baseline preserved.
    Phase 11 ready for /gsd-verify-phase.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → production code | Tests run inside Playwright pages with the same JS shell as production; no production code modified by Plan 11-05 |
| REQUIREMENTS.md flip → traceability | Each ID flip claims a passing test; if any test red, the ID stays Pending |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-flake | Denial of Service | Playwright timing-sensitive tests (3 s wakeup timeout, 5 s auto-hide, 250 ms throughput tick) | mitigate | Use `expect.poll(..., { timeout: ... })` with conservative timeouts (4-5 s for 3 s timer, 6 s for 5 s auto-hide); avoid hard `waitForTimeout` where polling is feasible |
| T-11-05-test-coupling | Tampering | Tests reading window.__slideChip introspection during state transitions | accept | Plan 11-02 designed introspection to be read-only; race conditions surface as flake (fixed via expect.poll) not as production bugs |
| T-11-05-rust-drift | Tampering | Phase 11 inadvertently touching crates/ | mitigate | Acceptance criteria require `cargo test --workspace` exits 0 with same test count (283/283) — proves zero Rust drift |
</threat_model>

<verification>
After all 3 tasks complete:

1. `cd www && npm test` exits 0 (full Playwright suite green)
2. `cargo test --workspace` exits 0 with 283/283 (no Rust changes)
3. `cargo test --test slide_wasm_boundary_shape` exits 0 (boundary locked from Phase 10)
4. `cargo test --test core_02_no_browser_deps` exits 0 (invariant preserved)
5. `bash scripts/build.sh` exits 0
6. `grep -c 'Phase 11 | Pending' .planning/REQUIREMENTS.md` returns 0 (all Phase 11 rows flipped)
7. ROADMAP.md Phase 11 main checkbox can be flipped to `[x]` by /gsd-verify-phase
</verification>

<success_criteria>
1. **All 4 spec files filled** — zero `test.skip` declarations remain across slide-chip / slide-bridge / slide-compatibility / slide-prefs
2. **All Wave 0 stubs replaced** with real assertions matching the `-g` filters in 11-VALIDATION.md
3. **Full Playwright suite green** — `cd www && npm test` exits 0
4. **Cargo workspace baseline preserved** — 283/283 tests; Phase 11 made zero Rust changes (strict invariant)
5. **REQUIREMENTS.md flipped** — 11 SLIDE-* requirement IDs marked Complete in both top checkbox section and traceability table
6. **Phase 11 ready for /gsd-verify-phase** — every success criterion in ROADMAP §Phase 11 has a passing test
</success_criteria>

<output>
After completion, create `.planning/phases/11-slide-js-bridge-v1-0-integration/11-05-SUMMARY.md`.
</output>

---
phase: 07-slide-rust-core-framer-crc-state-machine
plan: 05
type: execute
wave: 4
depends_on: [07-03]
files_modified:
  - .planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md
  - crates/bestialitty-core/tests/core_02_no_browser_deps.rs
autonomous: true
requirements: [SLIDE-04]
requirements_addressed: [SLIDE-04]
tags: [slide, adr, hardening, no-time-invariant]

must_haves:
  truths:
    - ".planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md exists with Nygard sections (Status / Date / Phase / Deciders / Context / Decision / Consequences / Rejected Alternatives / Cross-link / References)"
    - "ADR-003 documents bidirectional CAN echo contract (D-05)"
    - "ADR-003 documents idempotent cancel + force_idle escape hatch (D-06)"
    - "ADR-003 documents silent-drain semantics in CancelPending (D-07)"
    - "ADR-003 records CTRL_CAN exact wire format (raw byte 0x18, NOT framed) with cited line references from slide-rs/protocol.rs"
    - "ADR-003 cross-links to upstream github.com/blowback/slide PR (Phase 12 dependency)"
    - "ADR-003 cross-links to ADR-001 / ADR-002 / 07-CONTEXT.md / PITFALLS.md §5 / ARCHITECTURE.md §7"
    - "core_02_no_browser_deps.rs FORBIDDEN_TOKENS_WITH_EXEMPTIONS extended with `(\"std::time\", &[])` to gate the no-time invariant"
    - "Existing tests/core_02_no_browser_deps.rs still passes after the extension"
  artifacts:
    - path: ".planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md"
      provides: "Nygard ADR formalising D-05/D-06/D-07; deliverable per CONTEXT D-08"
      min_lines: 80
      contains: "## Decision"
    - path: "crates/bestialitty-core/tests/core_02_no_browser_deps.rs"
      provides: "FORBIDDEN_TOKENS extension to include std::time"
      contains: "(\"std::time\", &[])"
  key_links:
    - from: ".planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md"
      to: "crates/bestialitty-core/src/slide/state.rs (cancel + force_idle implementation)"
      via: "ADR Decision section cites Plan 07-03 implementation"
      pattern: "slide/state.rs"
    - from: "crates/bestialitty-core/tests/core_02_no_browser_deps.rs"
      to: "crates/bestialitty-core/src/slide/ (every file in the module)"
      via: "FORBIDDEN_TOKENS file walker"
      pattern: "std::time"
---

<objective>
Wave 4 hardening: deliver the two non-code-changing artifacts that close the
Phase 7 deliverable list. ADR-003 (D-08, mandatory) formally documents the
v0.2.1 CAN-bidirectional amendment that Plans 07-02 / 07-03 implemented —
mirroring ADR-001 / ADR-002 Nygard structure verbatim. The
`core_02_no_browser_deps.rs` extension adds `std::time` to the forbidden-token
table so the "no time logic in Rust core" invariant (PITFALLS §1, ARCHITECTURE.md
anti-pattern 4, CONTEXT D-06/D-07) is enforced by the test walker rather than by
convention alone (RESEARCH §Open Questions #2 recommendation).

This plan runs in parallel with Plan 07-04 (integration tests) — no shared
files between the two; both depend only on Plan 07-03's behaviour being shipped
(CAN echo + cancel + force_idle live; the ADR can describe what Phase 7 actually
delivers, not what it intends to deliver).

Output: 1 new ADR file; 1 modified test file.
</objective>

<execution_context>
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/workflows/execute-plan.md
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-slide-rust-core-framer-crc-state-machine/07-CONTEXT.md
@.planning/phases/07-slide-rust-core-framer-crc-state-machine/07-RESEARCH.md
@.planning/phases/07-slide-rust-core-framer-crc-state-machine/07-PATTERNS.md
@.planning/phases/07-slide-rust-core-framer-crc-state-machine/07-03-SUMMARY.md
@.planning/decisions/ADR-001-parser-strategy.md
@.planning/decisions/ADR-002-wasm-gating.md
@crates/bestialitty-core/tests/core_02_no_browser_deps.rs
@crates/bestialitty-core/src/slide/state.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ADR-003 (D-08 deliverable)</name>
  <files>.planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md</files>
  <read_first>
    - .planning/decisions/ADR-001-parser-strategy.md (analog: Nygard structure — Status / Date / Phase / Deciders / Context / Decision / Consequences / Rejected Alternatives / References)
    - .planning/decisions/ADR-002-wasm-gating.md (analog: same Nygard structure for the wasm-gating decision)
    - .planning/phases/07-slide-rust-core-framer-crc-state-machine/07-CONTEXT.md §D-05, §D-06, §D-07, §D-08 (the source decisions ADR-003 formalises)
    - .planning/phases/07-slide-rust-core-framer-crc-state-machine/07-RESEARCH.md §CTRL_CAN Wire Format Resolution (lines 698-725) — raw byte 0x18 + slide-rs cited evidence
    - .planning/phases/07-slide-rust-core-framer-crc-state-machine/07-RESEARCH.md §Sliding-Window State Machine §Cross-cutting (lines 686-694)
    - .planning/research/PITFALLS.md (referenced — §5 cancellation race + v0.2.1 amendment rationale)
    - .planning/research/ARCHITECTURE.md (referenced — §7 cancellation propagation)
    - crates/bestialitty-core/src/slide/state.rs (Plan 07-03 — `pub fn cancel` + `pub fn force_idle` impls; ADR cites this as the implementation site)
  </read_first>
  <action>
    Create `.planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md` with the
    full Nygard structure (mirror ADR-001 / ADR-002):

    ```markdown
    # ADR-003: SLIDE v0.2.1 CAN-Bidirectional Amendment

    **Status:** Accepted
    **Date:** 2026-05-06
    **Phase:** 07-slide-rust-core-framer-crc-state-machine
    **Deciders:** ant (project author)

    ## Context

    The SLIDE v0.2 spec at `/home/ant/src/microbeast/SLIDE/SPEC-v0.2.md` defines
    `CTRL_CAN = 0x18` as a cancellation signal but specifies an asymmetric
    contract: only the receiver (Z80) emits CAN. The reference implementations
    `/home/ant/src/microbeast/SLIDE/slide-rs/src/protocol.rs:104` (`recv_control`
    consumes 0x18) and `/home/ant/src/microbeast/SLIDE/slide-py/slide/common.py:64-71`
    (Python receive path) accept inbound CAN; neither implementation has a
    sender-emits-CAN path.

    BestialiTTY v1.1 needs PC-initiated cancellation: the user clicks the
    floating chip's Cancel button (Phase 11) or hits Esc mid-transfer
    (REQUIREMENTS.md SLIDE-27, SLIDE-30). The PC is the sender during host →
    Z80 transfers (Phase 9) and the receiver during Z80 → PC transfers
    (Phase 10); cancellation must work in both directions, neutrally leaving
    the wire ready for the next session without `reader.cancel()` /
    `port.close()` (PITFALLS §5, BLOCKING).

    A naive PC-side `port.write(&[0x18])` without a documented Z80 contract
    leaves the wire desync'd: the Z80 may not know whether to abort its
    current state machine, may finish writing the current frame, or may
    interpret 0x18 as in-band data. PITFALLS §5 calls this BLOCKING for
    cancellation correctness.

    07-CONTEXT.md decisions D-05 / D-06 / D-07 lock the resolution at the
    discuss-phase: strict bidirectional echo, idempotent `cancel()` /
    `force_idle()` API, silent-drain semantics in CancelPending. D-08
    requires this ADR document the resulting amendment formally so that
    `docs/SLIDE_Z80_REQUIREMENT.md` (Phase 12 deliverable) and the upstream
    `github.com/blowback/slide` PR (Phase 12 dependency) can both reference it.

    ## Decision

    BestialiTTY adopts the **SLIDE v0.2.1 CAN-bidirectional amendment**:

    1. **Strict bidirectional echo (D-05).** Either side MAY initiate
       `CTRL_CAN`. The other side MUST echo `CTRL_CAN` back. Both sides then
       drain the wire and return to idle. The amendment makes CAN symmetric;
       previously only the receiver could emit it.

    2. **CTRL_CAN wire format: raw single byte 0x18, NOT a wrapped frame.**
       Cited evidence:
       - `slide-rs/protocol.rs:104` — `CTRL_CAN => return Ok(Control::Can),` —
         `recv_control` reads a single byte and immediately returns Can.
         No SOF, no SEQ, no LEN, no CRC follow.
       - `slide-rs/protocol.rs:199-206` — `send_control` writes `&[ctrl]`
         (and optionally a seq for ACK/NAK only). For CAN, `seq: None`, so
         `port.write_all(&[0x18])` — a single byte.
       - `slide-py/common.py:64-71` — Python reference: `ctrl in (CTRL_RDY,
         CTRL_CAN, CTRL_FIN): return (ctrl, None)` — same single-byte shape.
       - **Symmetry argument:** RDY (0x11) and FIN (0x04) are both raw single
         bytes. CAN sits in the same family. Wrapping CAN in a frame envelope
         would (a) require the Z80 to mid-state-machine-pause to validate CRC,
         (b) add ~5 bytes of latency to a critical-path cancel signal,
         (c) diverge from RDY/FIN/ACK shape conventions, and (d) break wire
         compatibility with stock unmodified slide.com (which receives
         single-byte CAN per `slide-rs/protocol.rs:104`).

       The amendment adds a *behaviour* (Z80 must echo back when it sees CAN;
       v0.2.1 sender now sends CAN, where v0.2 only ever received it), not
       a new wire shape.

    3. **Idempotent host-initiated cancel API (D-06).** The Rust SM exposes:

       ```rust
       pub fn cancel(&mut self);       // builds CTRL_CAN, transitions to CancelPending; idempotent
       pub fn force_idle(&mut self);   // forcibly transitions to Done (JS escape hatch)
       ```

       Implemented in `crates/bestialitty-core/src/slide/state.rs`. `cancel()`
       is fire-and-set-state: it pushes `CTRL_CAN = 0x18` onto `outbound_buf`
       and transitions `sm_state` to `CancelPending`. Calling `cancel()` while
       already in `CancelPending` (or `Done` / `Error`) is a no-op — the
       second call does NOT push another CTRL_CAN. JS owns timing windows
       (200 ms in-flight settle / 500 ms echo wait / 100 ms drain / 2 s
       absolute timeout per PITFALLS §5); Rust SM is purely event-driven.
       `force_idle()` is the JS escape hatch after the absolute timeout: it
       drops `CancelPending` and transitions to `Done` so JS can construct
       a fresh `Slide::new()` for the next session.

    4. **Silent-drain semantics in CancelPending (D-07).** While the SM is
       in `CancelPending`, `feed_byte` / `feed_chunk` silently consume
       incoming bytes (no events emitted) until a CAN echo is recognised
       → transition to `Done`. JS owns the post-echo drain window via its
       own read loop (~100 ms keeps reading + feeding, then stops). No
       byte-count threshold or quiescence detector in Rust; no time logic
       in Rust (CORE-02 invariant + ARCHITECTURE.md anti-pattern 4).

    The amendment is exercised in unit tests in `crates/bestialitty-core/src/slide/state.rs`
    (`cancel_idempotent`, `peer_can_during_data_phase_echoes_and_transitions`,
    `force_idle_transitions_to_done`, `cancel_pending_silent_drains_non_can_bytes`,
    `cancel_pending_can_completes_session`) and in the integration corpus
    `crates/bestialitty-core/tests/slide_idempotent_reentry.rs` (6 re-entry
    test cases re1..re6).

    ## Consequences

    **Positive:**
    - Cancellation is symmetric and recoverable from either side. Both PC and
      Z80 can initiate; both must echo; both drain to a neutral wire.
    - Phase 10's hard-fail recovery is strictly stronger than v0.2 because
      the receiver now has an authoritative way to terminate a session
      (the v0.2 spec only allowed the sender to terminate via FIN; receivers
      had to wait it out).
    - The `force_idle()` escape hatch tolerates Z80 firmware that doesn't yet
      support the amendment: after JS's 2 s timeout, the session terminates
      cleanly client-side even if the Z80 never echoes back.
    - All timing logic stays in JS — Rust SM is event-driven, no `std::time`,
      satisfying the CORE-02 invariant (`tests/core_02_no_browser_deps.rs`
      enforces this; Plan 07-05 Task 2 hardens it by adding `std::time` to the
      forbidden-token list).
    - Idempotent `cancel()` tolerates user double-clicks on the chip's Cancel
      button without sending two CTRL_CAN bytes that could confuse the Z80.

    **Negative:**
    - Adds an upstream-divergent contract. Stock slide.com (running v0.2 only)
      will receive CAN from BestialiTTY but NOT echo back. The `force_idle()`
      escape hatch absorbs this — the user-visible result is a 2 s wait
      followed by a clean reset — but the wire-level behaviour is non-ideal
      until the Z80 PR lands.
    - Phase 12 must coordinate the `github.com/blowback/slide` PR. Until the
      PR is accepted and the user has updated slide.com on their Z80,
      cancellation in BestialiTTY relies on the escape hatch rather than the
      echo. Phase 12's `docs/SLIDE_Z80_REQUIREMENT.md` (REQUIREMENTS.md
      SLIDE-40) will document this clearly.
    - Maintaining the two-impl matrix (BestialiTTY-side amended vs slide.com-side
      stock) is fragile if Z80 firmware drift: stale slide.com installs may
      look like a hardware fault rather than a "please update slide.com"
      situation. The Phase 11 chip's "Z80 didn't respond" timeout (SLIDE-35)
      surfaces this gracefully with a Compatibility-mode option.

    ## Rejected Alternatives

    - **"Initiator-only no echo."** Whichever side first sends CAN simply
      transitions to a terminal state without waiting. Rejected because
      Phase 10's hard-fail recovery is strictly weaker (the non-initiator
      doesn't know the session is dead until its next read times out, and a
      multi-second fenced-off wire kills daily-driver responsiveness).
    - **"PC-initiated only."** Only the PC may emit CAN; Z80 may not.
      Rejected because it conflicts with REQUIREMENTS.md SLIDE-04's
      "bidirectional" wording AND because slide-rs / slide-py / spec already
      define Z80-initiated CAN — the v0.2 spec is symmetric on receive,
      asymmetric on send; v0.2.1 closes that gap.
    - **"CTRL_CAN as a wrapped frame."** Wrap 0x18 in `[SOF, SEQ, 0x00, 0x01,
      0x18, CRC_H, CRC_L]` to give it CRC integrity. Rejected per the four
      evidence points cited in the Decision section (slide-rs / slide-py
      both consume 0x18 raw, and RDY/FIN are also raw — single-byte family).

    ## Cross-link

    - **Upstream PR target:** `github.com/blowback/slide` (Phase 12 dependency
      per REQUIREMENTS.md SLIDE-40).
    - 07-CONTEXT.md §D-05 (strict bidirectional)
    - 07-CONTEXT.md §D-06 (idempotent cancel + force_idle)
    - 07-CONTEXT.md §D-07 (drain semantics in CancelPending)
    - 07-CONTEXT.md §D-08 (this ADR is the deliverable)
    - 07-RESEARCH.md §CTRL_CAN Wire Format Resolution (lines 698-725 — cited evidence)
    - .planning/research/PITFALLS.md §5 (cancellation race + v0.2.1 amendment rationale)
    - .planning/research/ARCHITECTURE.md §7 (cancellation propagation)
    - .planning/research/ARCHITECTURE.md anti-pattern 4 (no `std::time` in core)
    - REQUIREMENTS.md SLIDE-04, SLIDE-27, SLIDE-30, SLIDE-40
    - `crates/bestialitty-core/src/slide/state.rs` — `pub fn cancel`, `pub fn force_idle`, CancelPending state arm
    - `crates/bestialitty-core/tests/slide_idempotent_reentry.rs` — re1..re6 corpus

    ## References

    - `/home/ant/src/microbeast/SLIDE/SPEC-v0.2.md` — authoritative v0.2 spec
      (frame format, control bytes, sliding window, NO bidirectional CAN)
    - `/home/ant/src/microbeast/SLIDE/slide-rs/src/protocol.rs:16-30` — CRC source
      copied verbatim per CONTEXT D-01
    - `/home/ant/src/microbeast/SLIDE/slide-rs/src/protocol.rs:104` — `recv_control`
      single-byte CAN consumption
    - `/home/ant/src/microbeast/SLIDE/slide-rs/src/protocol.rs:199-206` —
      `send_control` single-byte CAN emission
    - `/home/ant/src/microbeast/SLIDE/slide-py/slide/common.py:64-71` — Python
      single-byte CAN consumption (cross-validation)
    - ADR-001 (Parser strategy) — same Nygard structure used here
    - ADR-002 (Wasm gating) — same Nygard structure used here
    - Greg Cook CRC catalogue (https://reveng.sourceforge.io/crc-catalogue/all.htm)
      — confirms CRC variant CCITT-FALSE / IBM-3740 used by SLIDE
    ```

    Note: write the ADR ONCE in the canonical Nygard structure. Do NOT add
    section numbers or slugs that don't appear in ADR-001/ADR-002. The
    document is plain prose with code blocks where they aid clarity.
  </action>
  <verify>
    <automated>test -f .planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md && grep -q '## Decision' .planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md && grep -q '## Consequences' .planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md && grep -q '## Rejected Alternatives' .planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md && grep -q '## Cross-link' .planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md && grep -q '## References' .planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md && grep -q 'Strict bidirectional echo' .planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md && grep -q 'github.com/blowback/slide' .planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md` exists
    - File contains the literal string `# ADR-003: SLIDE v0.2.1 CAN-Bidirectional Amendment`
    - File contains the literal string `**Status:** Accepted`
    - File contains the literal string `**Phase:** 07-slide-rust-core-framer-crc-state-machine`
    - File contains the heading `## Context`
    - File contains the heading `## Decision`
    - File contains the heading `## Consequences`
    - File contains the heading `## Rejected Alternatives`
    - File contains the heading `## Cross-link`
    - File contains the heading `## References`
    - File contains the literal string `Strict bidirectional echo` (D-05 verbatim)
    - File contains the literal string `Idempotent` (D-06 verbatim — the cancel contract)
    - File contains the literal string `silent-drain` or `silent drain` (D-07)
    - File contains the literal string `raw single byte 0x18` or `raw byte 0x18` (CTRL_CAN wire format resolution)
    - File contains the literal string `github.com/blowback/slide` (upstream PR cross-link)
    - File contains the literal string `slide-rs/src/protocol.rs:104` (cited evidence for raw-byte CAN)
    - File contains the literal string `force_idle` (escape-hatch API name)
    - File cross-references `ADR-001` and `ADR-002`
    - File is at least 80 lines long (Nygard structure with full prose)
  </acceptance_criteria>
  <done>
    - ADR-003 exists with full Nygard structure
    - All four CONTEXT.md decisions D-05/D-06/D-07/D-08 formalised
    - CTRL_CAN raw-byte wire format resolved with cited slide-rs evidence
    - Cross-links to ADR-001 / ADR-002 / 07-CONTEXT.md / PITFALLS / ARCHITECTURE / REQUIREMENTS
    - Upstream PR target documented for Phase 12 coordination
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden core_02_no_browser_deps.rs to forbid std::time in slide/</name>
  <files>crates/bestialitty-core/tests/core_02_no_browser_deps.rs</files>
  <read_first>
    - crates/bestialitty-core/tests/core_02_no_browser_deps.rs (current state — FORBIDDEN_TOKENS_WITH_EXEMPTIONS at lines 55-67)
    - crates/bestialitty-core/src/slide/ (Plans 07-01..07-03 outputs — must NOT contain `std::time`; the test will scan these files after this Task lands)
    - .planning/phases/07-slide-rust-core-framer-crc-state-machine/07-RESEARCH.md §Open Questions #2 (lines 1447-1454) — recommendation to add std::time
    - .planning/phases/07-slide-rust-core-framer-crc-state-machine/07-CONTEXT.md (no-time invariant in D-06 / D-07 + ARCHITECTURE.md anti-pattern 4)
  </read_first>
  <behavior>
    - After this change, the existing dependency_graph_excludes_browser_crates test still passes
    - After this change, the existing source_files_contain_no_wasm_attrs test still passes
    - After this change, the FORBIDDEN_TOKENS_WITH_EXEMPTIONS table includes `("std::time", &[])` (no exemptions for any file)
    - If a future commit adds `use std::time` to ANY src/**/*.rs file, the test fails
  </behavior>
  <action>
    Edit `crates/bestialitty-core/tests/core_02_no_browser_deps.rs`. The current
    `FORBIDDEN_TOKENS_WITH_EXEMPTIONS` constant (lines 63-67) is:

    ```rust
    const FORBIDDEN_TOKENS_WITH_EXEMPTIONS: &[(&str, &[&str])] = &[
        ("wasm_bindgen", &["lib.rs"]),
        ("web_sys", &[]),
        ("js_sys", &[]),
    ];
    ```

    Edit to add `("std::time", &[])` as a fourth row, with an updated
    explanatory comment ABOVE the constant noting Phase 7's hardening:

    ```rust
    /// Forbidden tokens in `crates/bestialitty-core/src/**/*.rs` source files.
    /// Tuple: (token, list of file-relative paths exempt FROM THAT SPECIFIC token).
    /// Every file is subject to every token unless explicitly exempted for that token.
    ///
    /// D-07: Phase 2 permits `wasm_bindgen` ONLY in `src/lib.rs`. `web_sys` and
    /// `js_sys` remain forbidden in every file, including `lib.rs`. Do NOT replace
    /// this with a path-based early-continue — that would regress D-07 by also
    /// exempting lib.rs from `web_sys` / `js_sys`.
    ///
    /// Phase 7 (07-CONTEXT.md D-06/D-07 + ARCHITECTURE.md anti-pattern 4):
    /// `std::time` is forbidden everywhere in the core crate. All timing logic
    /// lives in JS; Rust SM is purely event-driven. `slide/` has no exemption;
    /// `lib.rs` does not need one either (Phase 1+2 don't import std::time).
    /// See ADR-003 (`.planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md`)
    /// for the full rationale.
    const FORBIDDEN_TOKENS_WITH_EXEMPTIONS: &[(&str, &[&str])] = &[
        ("wasm_bindgen", &["lib.rs"]),
        ("web_sys", &[]),
        ("js_sys", &[]),
        ("std::time", &[]),
    ];
    ```

    Make NO other change to the file. The token-walker logic at lines 114+ uses
    a substring match — `std::time` is specific enough not to collide with any
    legitimate Phase 1+2 source. (If it ever did collide, the test would fail
    immediately and the responsible source file would need to be cleaned up.)

    Sanity check: before committing, grep all `crates/bestialitty-core/src/**/*.rs`
    for `std::time`. If any file contains it, the test will fail; the source
    must be cleaned up first. Plans 07-01..07-03 explicitly forbade `std::time`
    in their `<read_first>` and `<acceptance_criteria>` so this should be a no-op
    in steady state — but the grep verification is defensive.
  </action>
  <verify>
    <automated>cargo test -p bestialitty-core --test core_02_no_browser_deps</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/bestialitty-core/tests/core_02_no_browser_deps.rs` contains the literal string `("std::time", &[])`
    - File `crates/bestialitty-core/tests/core_02_no_browser_deps.rs` `FORBIDDEN_TOKENS_WITH_EXEMPTIONS` table has exactly 4 entries (wasm_bindgen + web_sys + js_sys + std::time)
    - The doc-comment above `FORBIDDEN_TOKENS_WITH_EXEMPTIONS` references `Phase 7` and `ADR-003`
    - `cargo test -p bestialitty-core --test core_02_no_browser_deps dependency_graph_excludes_browser_crates` exits 0
    - `cargo test -p bestialitty-core --test core_02_no_browser_deps source_files_contain_no_wasm_attrs` exits 0
    - `cargo test -p bestialitty-core --test core_02_no_browser_deps` exits 0 overall
    - Whole-crate `cargo test -p bestialitty-core` still passes (no Phase 1+2 source contains `std::time`)
  </acceptance_criteria>
  <done>
    - core_02_no_browser_deps now enforces no-std::time invariant by file walker
    - All Phase 1+2 source files still pass (none contain std::time)
    - All Phase 7 source files still pass (none contain std::time per Plans 07-01..07-03 acceptance criteria)
    - Doc comment references the Phase 7 rationale and ADR-003
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation ↔ implementation | ADR-003 must accurately describe what Plan 07-03 implemented (cancel + force_idle behaviour). Drift would mislead Phase 8/9/10 implementers and the Phase 12 Z80 PR. |
| Architectural invariant ↔ test enforcement | The "no time logic in Rust core" invariant was enforced by convention; this plan promotes it to a file-walker test gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Cancellation race / contract drift | Documentation drift between ADR-003 and the implementation in `slide/state.rs` could lead Phase 8/9/10 implementers astray | mitigate | ADR-003 explicitly cites `crates/bestialitty-core/src/slide/state.rs` as the implementation site and lists the unit tests that exercise each clause (D-05/D-06/D-07). Anyone changing the implementation must also update ADR-003. |
| T-07-01 | Tampering / Information Disclosure | Future contributor introduces `std::time::Instant` into core (e.g., copy-pasting from slide-rs CLI), regressing the wasm-free / event-driven invariant | mitigate | Task 2 hardens `core_02_no_browser_deps.rs` to fail compilation/test on `std::time` import. PITFALLS §1 BLOCKING; ARCHITECTURE.md anti-pattern 4. |
</threat_model>

<verification>
End of plan, run all of:

```bash
# Test invariant gate (now includes std::time)
cargo test -p bestialitty-core --test core_02_no_browser_deps

# Whole crate green (no source file has std::time; Plan 07-01..03 already verified)
cargo test -p bestialitty-core

# ADR file presence + structural completeness
test -f .planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md
grep -q "## Decision" .planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md
grep -q "## Consequences" .planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md
grep -q "github.com/blowback/slide" .planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md

# Native build succeeds (D-20)
cargo build -p bestialitty-core --target x86_64-unknown-linux-gnu
```

All MUST exit 0.
</verification>

<success_criteria>
- [ ] `.planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md` exists with full Nygard structure (Status / Date / Phase / Deciders / Context / Decision / Consequences / Rejected Alternatives / Cross-link / References)
- [ ] ADR-003 documents D-05 (strict bidirectional echo), D-06 (idempotent cancel + force_idle), D-07 (silent drain), and the CTRL_CAN raw-byte wire format with cited slide-rs/protocol.rs line references
- [ ] ADR-003 cross-links to upstream `github.com/blowback/slide` PR for Phase 12 coordination
- [ ] ADR-003 cites the implementation site (`slide/state.rs`) and the test files (`tests/slide_idempotent_reentry.rs`)
- [ ] `crates/bestialitty-core/tests/core_02_no_browser_deps.rs` `FORBIDDEN_TOKENS_WITH_EXEMPTIONS` includes `("std::time", &[])`
- [ ] `cargo test -p bestialitty-core --test core_02_no_browser_deps` exits 0 (no Phase 1+2 nor Phase 7 source contains `std::time`)
- [ ] Whole-crate `cargo test -p bestialitty-core` passes (T-07-01 mitigation extended)
</success_criteria>

<output>
After completion, create `.planning/phases/07-slide-rust-core-framer-crc-state-machine/07-05-SUMMARY.md`
following the template at `.claude/get-shit-done/templates/summary.md`. Capture:
- Files created (ADR-003) and modified (core_02_no_browser_deps.rs)
- ADR-003 ratifies D-05/D-06/D-07/D-08 with cited slide-rs evidence
- Phase 12 cross-link recorded (Z80 PR coordination)
- No-`std::time` invariant promoted from convention to file-walker enforcement
- Phase 7 deliverable list closed:
  - 11 modified/new code files (Plans 01-04)
  - 21+ integration tests (Plans 02 + 04)
  - 30+ unit tests (Plans 01 + 02 + 03)
  - 1 new ADR (Plan 05)
- Unblocks: Phase 8 (wasm boundary wrapper consumes the pinned Slide surface)
</output>

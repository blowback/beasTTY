---
phase: 12
plan: 03
type: execute
wave: 2
depends_on: [12-02]
files_modified:
  - www/state/prefs.js
  - www/transport/slide.js
  - www/renderer/slide-chip.js
  - www/index.html
  - www/main.js
  - www/tests/transport/slide-autosend-safety.spec.js
autonomous: true
requirements: [SLIDE-38]
must_haves:
  truths:
    - "Auto-send command is validated against /^[A-Za-z0-9:]*\\r$/ at use time before any bytes hit the wire (empty string bypasses regex per SLIDE-13 disabled sentinel)"
    - "Unsafe values (semicolons, pipes, LF, missing CR, multiple CR, control chars) cause auto-type to silently disable; visible chip + Settings hint surface explain why"
    - "Settings input flips data-invalid='true' on save when value is unsafe (visual cue only — does NOT block savePrefs per Anti-Patterns)"
    - "First-use confirmation chip appears when user has set a non-default value that does not match prefs.slideAutoSendCommandConfirmed (key = exact-string match)"
    - "Confirmation chip shows [Confirm] [Reset to default] inline buttons; [Confirm] saves slideAutoSendCommandConfirmed = current value and proceeds; [Reset to default] restores the DEFAULTS literal"
    - "Confirmation flag re-arms automatically when user changes the auto-send command in Settings"
    - "First-use chip surfaces ONLY at session START — never overlays an in-flight active-session chip (Pitfall 6)"
  artifacts:
    - path: "www/state/prefs.js"
      provides: "isAutoSendSafe(cmd) pure helper export + slideAutoSendCommandConfirmed: '' added to DEFAULTS"
      exports: ["isAutoSendSafe"]
    - path: "www/transport/slide.js"
      provides: "Use-time safety gate in readAutoSendCommandBytes + first-use confirmation gate before pushTxBytes(autoSendBytes)"
      contains: "isAutoSendSafe"
    - path: "www/renderer/slide-chip.js"
      provides: "first-use-confirm lifecycle state + enterFirstUseConfirm public API + 30s defensive auto-hide"
      contains: "first-use-confirm"
    - path: "www/index.html"
      provides: "validation-hint sub-row inside #slide-auto-send-row + invalid-state CSS"
      contains: "slide-auto-send-validation-hint"
    - path: "www/main.js"
      provides: "Settings input handler that toggles data-invalid + re-arms slideAutoSendCommandConfirmed; chip first-use lifecycle wiring"
      contains: "slideAutoSendCommandConfirmed"
    - path: "www/tests/transport/slide-autosend-safety.spec.js"
      provides: "15 Playwright tests covering SAFE/UNSAFE cases + use-time gate + first-use confirm chip + flag re-arm"
      min_lines: 250
  key_links:
    - from: "www/transport/slide.js readAutoSendCommandBytes"
      to: "www/state/prefs.js isAutoSendSafe"
      via: "import + use-time call before TextEncoder.encode"
      pattern: "isAutoSendSafe"
    - from: "www/transport/slide.js enterSendMode"
      to: "www/renderer/slide-chip.js enterFirstUseConfirm"
      via: "Promise-wrapped call before pushTxBytes(autoSendBytes)"
      pattern: "enterFirstUseConfirm"
    - from: "www/main.js Settings input handler"
      to: "www/state/prefs.js savePrefs"
      via: "savePrefs({slideAutoSendCommand, slideAutoSendCommandConfirmed: ''})"
      pattern: "slideAutoSendCommandConfirmed"
---

<objective>
Close SLIDE-38 — auto-send command safety validation. Per CONTEXT Claude's Discretion (validate at use, defense-in-depth visual cue at save) and 12-UI-SPEC.md sections C/D/E (locked):

1. Lock the safety regex `/^[A-Za-z0-9:]*\r$/` at use time in slide.js as the wire-safety hard gate.
2. Add a save-time visual cue (`data-invalid` attribute on the Settings input + `.validation-hint` row) that does NOT block save (per Anti-Patterns).
3. Add a first-use confirmation chip lifecycle state for non-default values, with sticky `slideAutoSendCommandConfirmed` flag keyed to the exact string.

Purpose: Closes Pitfall 17 (auto-send injection vector — a hostile config or accidentally-pasted command containing `;`, pipes, or non-`\r` control chars cannot reach the Z80 wire). Defends against hostile-config injection where a user is tricked into using a non-default command — the first-use chip provides visible confirmation.

Output:
- prefs.js gains the `isAutoSendSafe` pure helper export + `slideAutoSendCommandConfirmed: ''` DEFAULTS key.
- slide.js gains use-time gate + first-use confirmation gate before `pushTxBytes(autoSendBytes)`.
- slide-chip.js gains `first-use-confirm` lifecycle state + `enterFirstUseConfirm({value, onConfirm, onReset})` API + 30 s defensive auto-hide.
- index.html gains a validation-hint row inside `#slide-auto-send-row` + invalid-state CSS on the input.
- main.js Settings input handler sets `data-invalid` and re-arms confirmation flag.
- New `www/tests/transport/slide-autosend-safety.spec.js` with 15 tests covering all SLIDE-38 success criteria.

Depends on Plan 12-02: index.html footer + file-source.js modal extensions land first; this plan touches a different part of index.html (Settings sub-block) but waits for 12-02 to stabilize the file.
</objective>

<execution_context>
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/workflows/execute-plan.md
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-CONTEXT.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-RESEARCH.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-UI-SPEC.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-PATTERNS.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-VALIDATION.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-02-SUMMARY.md

@www/state/prefs.js
@www/transport/slide.js
@www/renderer/slide-chip.js
@www/index.html
@www/main.js
@www/tests/transport/slide-prefs.spec.js
@www/tests/transport/slide-chip.spec.js

<interfaces>
Phase 12 NEW pure helper export in prefs.js (signature locked):

    export function isAutoSendSafe(cmd) { ... }
        // Returns true if cmd is the empty string (SLIDE-13 disabled sentinel)
        //   OR cmd matches /^[A-Za-z0-9:]*\r$/.
        // Returns false otherwise (including non-string inputs).

Phase 12 NEW DEFAULTS key in prefs.js (additive — defensive merge handles existing users):

    slideAutoSendCommandConfirmed: ''   // empty string = never confirmed

Phase 12 NEW chip lifecycle state in slide-chip.js (additive — extends Phase 11 state machine):

    'first-use-confirm'   // appended to existing union 'hidden' / 'awaiting-wakeup' /
                          //   'awaiting-timeout' / 'active' / 'cancelled-summary' /
                          //   'sent-summary' / 'received-summary' / 'error' /
                          //   'drop-rejected-flash'

Phase 12 NEW chip API in slide-chip.js (additive — exported from wireSlideChip return):

    enterFirstUseConfirm({ value, onConfirm, onReset })
        // value: the user's typed auto-send command (rendered in chip text;
        //        \r rendered as the literal two-character sequence \r per UI-SPEC).
        // onConfirm: callback fired when user clicks [Confirm].
        // onReset: callback fired when user clicks [Reset to default].
        // 30 s defensive auto-hide if neither button is clicked.

Existing readAutoSendCommandBytes shape (slide.js around line 183-196, current Phase 11 form):

    function readAutoSendCommandBytes() {
        let cmd;
        if (prefsRef) {
            cmd = prefsRef.slideAutoSendCommand;
            if (cmd === undefined || cmd === null) cmd = AUTO_SEND_DEFAULT;
        } else {
            cmd = AUTO_SEND_DEFAULT;
        }
        if (cmd.length === 0) return new Uint8Array(0);
        return new TextEncoder().encode(cmd);
    }

Existing #slide-auto-send-row markup (find via grep slide-auto-send-row in www/index.html). Phase 11 shape is roughly:

    <div class="settings-row" id="slide-auto-send-row">
      <label for="slide-auto-send-input">Auto-send command:</label>
      <input type="text" id="slide-auto-send-input" value="B:SLIDE R" autocomplete="off" spellcheck="false">
      <div class="hint">\r appended automatically</div>
    </div>

Window introspection hooks (Phase 11 precedent — already exposed):
- window.__slideChip.__getStateForTests().lifecycle
- window.__slide.__getStateForTests()
- window.__prefs.live (direct ref to live prefs object — Phase 11 Plan 11-05 D-3 deviation)
- window.__mockWriterLog (writer-log array — Phase 5 mock-serial)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add isAutoSendSafe + slideAutoSendCommandConfirmed in prefs.js + use-time gate in slide.js</name>
  <read_first>
    - www/state/prefs.js IN FULL — verify CURRENT_VERSION + DEFAULTS structure + savePrefs debounce contract + existing exports
    - www/transport/slide.js — focus on lines 160-220 around readAutoSendCommandBytes, prefsRef destructure, slideChipRef destructure, AUTO_SEND_DEFAULT constant
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-RESEARCH.md section Pitfall 5 — the regex must use `*` not `+` so bare `\r` is valid
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-RESEARCH.md section "SLIDE-38: Auto-send safety regex + use-time validation" Code Examples — verbatim source
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-UI-SPEC.md section "SLIDE-38 — First-use-confirm chip & Settings input flow" — locked SAFE_AUTO_SEND_RE + 12-test pass/fail table
    - www/transport/slide.js existing `__getStateForTests` exposure pattern (search for `__getStateForTests` in slide.js)
  </read_first>
  <files>
    www/state/prefs.js
    www/transport/slide.js
  </files>
  <behavior>
    isAutoSendSafe pass cases (empty string disabled sentinel, default, drive switch, no-R variant, alternate command, bare CR):
    - isAutoSendSafe('') returns true
    - isAutoSendSafe('B:SLIDE R\r') returns true
    - isAutoSendSafe('A:SLIDE R\r') returns true
    - isAutoSendSafe('B:SLIDE\r') returns true
    - isAutoSendSafe('B:DIR\r') returns true
    - isAutoSendSafe('\r') returns true (bare CR — `*` not `+` admits empty body)

    isAutoSendSafe fail cases (missing CR, LF, semicolon, multiple CR, control char, backslash):
    - isAutoSendSafe('B:SLIDE R') returns false
    - isAutoSendSafe('B:SLIDE R\n') returns false
    - isAutoSendSafe('B:SLIDE R; rm -rf /\r') returns false
    - isAutoSendSafe('B:SLIDE R\rB:DIR\r') returns false
    - isAutoSendSafe('B:SLIDE\x07R\r') returns false (BEL — control char)
    - isAutoSendSafe('B:SLIDE\\R\r') returns false (backslash)

    Defensive cases:
    - isAutoSendSafe(undefined) returns false
    - isAutoSendSafe(null) returns false
    - isAutoSendSafe(123) returns false

    prefs.js DEFAULTS gains slideAutoSendCommandConfirmed: '' (CURRENT_VERSION NOT bumped — defensive merge per Phase 6 D-32).

    readAutoSendCommandBytes in slide.js calls isAutoSendSafe AFTER reading prefsRef.slideAutoSendCommand and BEFORE TextEncoder.encode; on failure returns new Uint8Array(0), logs console.error, fires slideChipRef.enterError if available, sets data-invalid + unhides validation hint on the Settings DOM.
  </behavior>
  <action>
    Step 1 — Modify www/state/prefs.js.

    Add the new DEFAULTS key. Find the DEFAULTS literal (around line 18-33) and append slideAutoSendCommandConfirmed AFTER slideCompatibilityMode:

        slideAutoSendCommandConfirmed: '',   // Phase 12 SLIDE-38: exact-match flag,
                                             //   keyed to the value last confirmed.
                                             //   Empty string = never confirmed.

    DO NOT bump CURRENT_VERSION. The Phase 6 D-32 defensive merge fills the field for existing users transparently (Phase 10/11 precedent).

    Add the new pure helper export ANYWHERE in the file alongside the existing loadPrefs/savePrefs/subscribe/getPrefs exports. Verbatim from 12-PATTERNS.md and 12-RESEARCH.md Pitfall 5:

        const SAFE_AUTO_SEND_RE = /^[A-Za-z0-9:]*\r$/;

        /**
         * SLIDE-38: Validate auto-send command for wire safety.
         * Empty string = SLIDE-13 disabled sentinel (bypasses regex).
         * Pitfall 5: regex body uses `*` (not `+`) so bare \r is admitted (harmless on Z80).
         */
        export function isAutoSendSafe(cmd) {
            if (typeof cmd !== 'string') return false;
            if (cmd.length === 0) return true;
            return SAFE_AUTO_SEND_RE.test(cmd);
        }

    Constraints:
    - Regex MUST use `*` (zero-or-more), NOT `+` (Pitfall 5).
    - Empty-string short-circuit MUST come BEFORE the regex test.
    - typeof guard MUST come BEFORE the length check (defensive against undefined/null).
    - DO NOT add aria-invalid mirror logic in prefs.js — that lives in main.js (Task 3 / 4).

    Step 2 — Modify www/transport/slide.js — use-time safety gate.

    Locate readAutoSendCommandBytes (line ~183-196). Import isAutoSendSafe from prefs.js at the top of the file:

        import { isAutoSendSafe } from '../state/prefs.js';

    If a similar import line for prefs already exists, add isAutoSendSafe to its named imports.

    Modify readAutoSendCommandBytes per 12-PATTERNS.md "Phase 12 SLIDE-38 modification" + 12-RESEARCH.md Code Examples:

        function readAutoSendCommandBytes() {
            let cmd;
            if (prefsRef) {
                cmd = prefsRef.slideAutoSendCommand;
                if (cmd === undefined || cmd === null) cmd = AUTO_SEND_DEFAULT;
            } else {
                cmd = AUTO_SEND_DEFAULT;
            }
            if (cmd.length === 0) return new Uint8Array(0);
            // SLIDE-38 use-time hard gate: validate before placing on the wire.
            if (!isAutoSendSafe(cmd)) {
                console.error('[slide] Auto-send command failed safety check; auto-type skipped:',
                              JSON.stringify(cmd));
                if (slideChipRef && typeof slideChipRef.enterError === 'function') {
                    try { slideChipRef.enterError('auto-send command unsafe — fix in Settings'); } catch {}
                }
                try {
                    const inputEl = document.getElementById('slide-auto-send-input');
                    if (inputEl) inputEl.setAttribute('data-invalid', 'true');
                    const hintEl = document.getElementById('slide-auto-send-validation-hint');
                    if (hintEl) hintEl.hidden = false;
                } catch { /* ignore — DOM may not exist in tests */ }
                return new Uint8Array(0);
            }
            return new TextEncoder().encode(cmd);
        }

    Constraints:
    - DO NOT block save anywhere — that's the Settings handler in Task 4.
    - DO NOT throw — return zero-length Uint8Array on safety failure (matches SLIDE-13 disabled).
    - DOM mutations MUST be wrapped in try/catch (test contexts may not have full DOM).
    - DO NOT modify the rest of enterSendMode in this task — Task 2 owns the first-use confirm gate.

    Add a test introspection export. Find the existing __getStateForTests assignment site for window.__slide (search for `window.__slide` in slide.js — typically inside wireSlideDispatcher). Add alongside:

        window.__slide.__isAutoSendSafeForTests = isAutoSendSafe;

    This lets the safety spec call `await page.evaluate((s) => window.__slide.__isAutoSendSafeForTests(s), input)` for the unit-style tests.
  </action>
  <verify>
    <automated>grep -q "export function isAutoSendSafe" www/state/prefs.js; r1=$?; grep -q "slideAutoSendCommandConfirmed" www/state/prefs.js; r2=$?; grep -q "isAutoSendSafe" www/transport/slide.js; r3=$?; grep -q "auto-send command unsafe" www/transport/slide.js; r4=$?; cargo test --workspace --quiet; r5=$?; cd www && npm run test:fast; r6=$?; test $r1 -eq 0 && test $r2 -eq 0 && test $r3 -eq 0 && test $r4 -eq 0 && test $r5 -eq 0 && test $r6 -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function isAutoSendSafe" www/state/prefs.js succeeds.
    - grep -q "SAFE_AUTO_SEND_RE" www/state/prefs.js succeeds.
    - grep -q "slideAutoSendCommandConfirmed" www/state/prefs.js succeeds (default value present in DEFAULTS).
    - CURRENT_VERSION is NOT modified (`git diff www/state/prefs.js | grep '^[+-].*CURRENT_VERSION' | wc -l` returns 0 — no version bump).
    - grep -q "isAutoSendSafe" www/transport/slide.js succeeds (use-time gate present).
    - grep -q "auto-send command unsafe" www/transport/slide.js succeeds (chip error message wired).
    - grep -q "__isAutoSendSafeForTests" www/transport/slide.js succeeds (test introspection export).
    - cargo test --workspace --quiet exits 0 with 283/283 passing (Phase 12 hard invariant — zero Rust changes).
    - cd www && npm run test:fast exits 0 (no Phase 4-11 regressions).
  </acceptance_criteria>
  <done>
    isAutoSendSafe pure helper exported from prefs.js with locked regex; slideAutoSendCommandConfirmed: '' present in DEFAULTS without CURRENT_VERSION bump; slide.js readAutoSendCommandBytes hard-gates unsafe values + fires chip enterError + sets data-invalid + unhides validation hint; test introspection export added; cargo + npm test:fast baselines preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add first-use-confirm lifecycle state + enterFirstUseConfirm API in slide-chip.js + first-use gate in slide.js</name>
  <read_first>
    - www/renderer/slide-chip.js IN FULL — verify lifecycle state machine + clearAutoHide + clearWakeupTimer + refreshChip + handleInlineAction + cancelButtonHtml/retryButtonHtml/forceStartButtonHtml helpers + onStateChange registration + the enterAwaitingWakeup template
    - www/transport/slide.js around line 378-410 (enterSendMode entry — the call site for first-use gate before pushTxBytes(autoSendBytes))
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-PATTERNS.md section "www/renderer/slide-chip.js" — verbatim enterFirstUseConfirm shape + helper additions + handleInlineAction extension
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-UI-SPEC.md section C "First-use-confirm chip lifecycle state" — locked transitions + 30s defensive timeout + copy
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-RESEARCH.md section Pitfall 6 — first-use chip MUST surface ONLY at session START, never overlay an in-flight active-session chip
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-RESEARCH.md section "SLIDE-38: First-use confirmation gate" Code Examples — surfaceFirstUseConfirm shape
  </read_first>
  <files>
    www/renderer/slide-chip.js
    www/transport/slide.js
  </files>
  <behavior>
    slide-chip.js gains:
    - 'first-use-confirm' added to the lifecycle union (one comment-line edit).
    - confirmButtonHtml() and resetButtonHtml() helper functions (mirror of cancelButtonHtml shape).
    - FIRST_USE_CONFIRM_TIMEOUT_MS = 30000 module-scope constant.
    - firstUseConfirmHandle and firstUseConfirmCallbacks module-scope state.
    - enterFirstUseConfirm({ value, onConfirm, onReset }) public API exported in wireSlideChip return.
    - handleInlineAction extended to handle 'confirm' and 'reset' actions (calling firstUseConfirmCallbacks.onConfirm / .onReset, clearing the timeout, transitioning chip).
    - refreshChip extended to render the first-use-confirm state body per UI-SPEC copy.
    - __getStateForTests includes lifecycle string covering the new state.

    slide.js enterSendMode gains:
    - Before pushTxBytes(autoSendBytes), check shouldSurfaceFirstUseConfirm(cmd):
      - Returns false if cmd === AUTO_SEND_DEFAULT.
      - Returns false if prefsRef.slideAutoSendCommandConfirmed === cmd.
      - Returns true otherwise.
    - If true, fire surfaceFirstUseConfirm(cmd) which returns Promise<boolean>; await it.
      - On confirm: savePrefs({slideAutoSendCommandConfirmed: cmd}); proceed with pushTxBytes.
      - On reset: savePrefs({slideAutoSendCommand: AUTO_SEND_DEFAULT, slideAutoSendCommandConfirmed: AUTO_SEND_DEFAULT}); abort the send (return early — user must click Send file again).
    - Pitfall 6: This gate runs ONLY at session START (entry to enterSendMode), never during a Settings input change.
  </behavior>
  <action>
    Step 1 — Modify www/renderer/slide-chip.js.

    Add the new lifecycle state to the union comment (line ~40-42):

        let lifecycle = 'hidden';   // 'hidden' | 'awaiting-wakeup' | 'awaiting-timeout'
                                    // | 'active' | 'cancelled-summary' | 'sent-summary'
                                    // | 'received-summary' | 'error' | 'drop-rejected-flash'
                                    // | 'first-use-confirm' (Phase 12 SLIDE-38)

    Add module-scope state near the existing wakeupTimeoutHandle declaration:

        const FIRST_USE_CONFIRM_TIMEOUT_MS = 30000;   // 30 s defensive auto-hide
        let firstUseConfirmHandle = null;
        let firstUseConfirmCallbacks = null;          // { onConfirm, onReset, value } | null

    Add the two new inline-button HTML helpers next to the existing cancelButtonHtml / retryButtonHtml / forceStartButtonHtml helpers:

        function confirmButtonHtml() {
            return '<button type="button" class="slide-inline" data-action="confirm">[Confirm]</button>';
        }
        function resetButtonHtml() {
            return '<button type="button" class="slide-inline" data-action="reset">[Reset to default]</button>';
        }

    Add the new exported state-transition method enterFirstUseConfirm. Place near enterAwaitingWakeup (lines ~292-317) using the same shape:

        export function enterFirstUseConfirm({ value, onConfirm, onReset }) {
            clearAutoHide();
            clearWakeupTimer();
            lifecycle = 'first-use-confirm';
            firstUseConfirmCallbacks = { onConfirm, onReset, value };
            samples.length = 0;
            refreshChip();
            firstUseConfirmHandle = setTimeout(() => {
                firstUseConfirmHandle = null;
                if (firstUseConfirmCallbacks) {
                    firstUseConfirmCallbacks = null;
                    hide();
                }
            }, FIRST_USE_CONFIRM_TIMEOUT_MS);
        }

    Find the refreshChip rendering switch (it dispatches on lifecycle and produces the chip text + inline buttons HTML for each state). Add a new branch for 'first-use-confirm' that renders the chip body per 12-UI-SPEC.md section "First-use-confirm chip copy" verbatim:

        Confirm auto-send: {VALUE}  [Confirm]  [Reset to default]

    The {VALUE} token is rendered as the literal user value, with `\r` rendered as the literal two-character sequence `\r` (backslash + r). UI-SPEC locks this — DO NOT escape to a CR pictograph and DO NOT send the actual control character through innerHTML.

    Implementation (escape-by-construction — XSS mitigation per T-12-05 inheritance):

        if (lifecycle === 'first-use-confirm') {
            const v = firstUseConfirmCallbacks?.value || '';
            // Render \r as the literal two-character sequence \r (UI-SPEC lock).
            const visibleValue = v.replace(/\r/g, '\\r').replace(/\n/g, '\\n');
            // escapeHtml is the existing chip-side helper — verify it exists; if not,
            // use document.createTextNode pattern via a temp <span>.
            const safeValue = escapeHtml(visibleValue);
            const text = `Confirm auto-send: ${safeValue}  ${confirmButtonHtml()}  ${resetButtonHtml()}`;
            chipTextElRef.innerHTML = text;
            chipElRef.setAttribute('aria-label', 'Confirm auto-send command before first use');
            chipElRef.hidden = false;
            return;
        }

    If escapeHtml is not pre-existing in slide-chip.js, replicate the inline pattern used by Phase 11 lifecycle states (search for innerHTML in slide-chip.js to find the existing escape-by-construction style). The existing handlers for active / awaiting-wakeup / etc. already establish the pattern.

    Extend handleInlineAction (lines ~277-288) to handle 'confirm' and 'reset':

        function handleInlineAction(action) {
            if (action === 'cancel') {
                if (onCancelFn) try { onCancelFn(); } catch {}
            } else if (action === 'confirm' && firstUseConfirmCallbacks) {
                const cb = firstUseConfirmCallbacks;
                firstUseConfirmCallbacks = null;
                if (firstUseConfirmHandle) { clearTimeout(firstUseConfirmHandle); firstUseConfirmHandle = null; }
                try { cb.onConfirm?.(); } catch {}
            } else if (action === 'reset' && firstUseConfirmCallbacks) {
                const cb = firstUseConfirmCallbacks;
                firstUseConfirmCallbacks = null;
                if (firstUseConfirmHandle) { clearTimeout(firstUseConfirmHandle); firstUseConfirmHandle = null; }
                hide();
                try { cb.onReset?.(); } catch {}
            }
            // Phase 11 D-15 retry / cancel / force-start branches via stateChangeObservers.
            for (const fn of stateChangeObservers) {
                try { fn({ kind: 'inline-action', action }); } catch {}
            }
        }

    Export enterFirstUseConfirm in the wireSlideChip return object next to enterAwaitingWakeup:

        return {
            enterAwaitingWakeup,
            enterFirstUseConfirm,    // Phase 12 SLIDE-38
            enterActive,
            // ... existing exports unchanged
        };

    Verify __getStateForTests already returns lifecycle as a string. If not, add lifecycle to its return object.

    Step 2 — Modify www/transport/slide.js — first-use gate at session start.

    Locate enterSendMode (entry point around line 378-410). Read the existing function carefully — note where readAutoSendCommandBytes is called and where pushTxBytes(autoSendBytes) fires.

    Add the helper function shouldSurfaceFirstUseConfirm near the top of slide.js (next to AUTO_SEND_DEFAULT):

        function shouldSurfaceFirstUseConfirm(cmd) {
            if (cmd === AUTO_SEND_DEFAULT) return false;
            if (!prefsRef) return false;
            if (prefsRef.slideAutoSendCommandConfirmed === cmd) return false;
            return true;
        }

    Add surfaceFirstUseConfirm helper:

        function surfaceFirstUseConfirm(cmd) {
            return new Promise((resolve) => {
                if (!slideChipRef || typeof slideChipRef.enterFirstUseConfirm !== 'function') {
                    // Fail-open in tests / when chip isn't wired — proceed without confirm.
                    resolve(true);
                    return;
                }
                slideChipRef.enterFirstUseConfirm({
                    value: cmd,
                    onConfirm: () => resolve(true),
                    onReset: () => resolve(false),
                });
            });
        }

    Modify enterSendMode. Current shape calls readAutoSendCommandBytes then pushTxBytes. After reading the bytes (and the cmd string), insert the gate:

        async function enterSendMode({ files }) {
            // ... existing pendingSendSession setup ...
            const autoSendBytes = readAutoSendCommandBytes();
            const cmd = (prefsRef?.slideAutoSendCommand) ?? AUTO_SEND_DEFAULT;

            // SLIDE-38 first-use confirmation gate — Pitfall 6: only at session START.
            if (autoSendBytes.length > 0 && shouldSurfaceFirstUseConfirm(cmd)) {
                const confirmed = await surfaceFirstUseConfirm(cmd);
                if (!confirmed) {
                    // User clicked [Reset to default] — slide.js resets the prefs + aborts the send.
                    try {
                        if (typeof savePrefsRef === 'function') {
                            savePrefsRef({
                                slideAutoSendCommand: AUTO_SEND_DEFAULT,
                                slideAutoSendCommandConfirmed: AUTO_SEND_DEFAULT,
                            });
                        }
                    } catch {}
                    return;   // user must click Send file again to proceed with the default
                }
                // User clicked [Confirm] — record the confirmation against the exact string.
                try {
                    if (typeof savePrefsRef === 'function') {
                        savePrefsRef({ slideAutoSendCommandConfirmed: cmd });
                    }
                } catch {}
            }

            // ... existing pushTxBytes(autoSendBytes) + pendingSendSession assignment unchanged
        }

    The savePrefsRef is a new dependency injected through wireSlideDispatcher opts (or accessed via the existing prefs singleton — read existing slide.js to determine which pattern). If wireSlideDispatcher already takes savePrefs in opts, use that; otherwise add savePrefs to the destructure block:

        export function wireSlideDispatcher(opts) {
            const { ..., savePrefs, ... } = opts;
            savePrefsRef = savePrefs;
            // ... existing
        }

    main.js Task 4 will pass `savePrefs` (the existing prefs.js export) through the wiring.

    Constraints:
    - The first-use gate MUST run AFTER readAutoSendCommandBytes (so the safety check has already happened — unsafe values get rejected before we even consider asking for confirmation).
    - The gate MUST short-circuit when autoSendBytes.length === 0 (SLIDE-13 disabled OR safety-rejected — neither path needs confirmation).
    - DO NOT surface during a Settings input change event (Pitfall 6 — first-use chip is session-start-only).
    - DO NOT surface during an in-flight active-session chip (the lifecycle clearAutoHide + clearWakeupTimer in enterFirstUseConfirm preempts other states; if a session is already active when enterSendMode is somehow re-entered, the existing pendingSendSession guard handles it — DO NOT add new active-session detection here).
  </action>
  <verify>
    <automated>grep -q "first-use-confirm" www/renderer/slide-chip.js; r1=$?; grep -q "enterFirstUseConfirm" www/renderer/slide-chip.js; r2=$?; grep -q "FIRST_USE_CONFIRM_TIMEOUT_MS" www/renderer/slide-chip.js; r3=$?; grep -q "shouldSurfaceFirstUseConfirm" www/transport/slide.js; r4=$?; grep -q "surfaceFirstUseConfirm" www/transport/slide.js; r5=$?; cd www && npm run test:fast; r6=$?; test $r1 -eq 0 && test $r2 -eq 0 && test $r3 -eq 0 && test $r4 -eq 0 && test $r5 -eq 0 && test $r6 -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "first-use-confirm" www/renderer/slide-chip.js succeeds (lifecycle state added).
    - grep -q "enterFirstUseConfirm" www/renderer/slide-chip.js succeeds (API exported).
    - grep -q "FIRST_USE_CONFIRM_TIMEOUT_MS = 30000" www/renderer/slide-chip.js succeeds (30s defensive timeout).
    - grep -q "data-action=\"confirm\"" www/renderer/slide-chip.js succeeds (confirmButtonHtml).
    - grep -q "data-action=\"reset\"" www/renderer/slide-chip.js succeeds (resetButtonHtml).
    - grep -q "shouldSurfaceFirstUseConfirm" www/transport/slide.js succeeds.
    - grep -q "surfaceFirstUseConfirm" www/transport/slide.js succeeds.
    - grep -q "Confirm auto-send" www/renderer/slide-chip.js succeeds (chip text per UI-SPEC §C).
    - cargo test --workspace --quiet exits 0 with 283/283 (Phase 12 hard invariant).
    - cd www && npm run test:fast exits 0 (no Phase 4-11 regressions; Phase 11 chip lifecycle tests still green).
  </acceptance_criteria>
  <done>
    Chip module gains first-use-confirm lifecycle state with [Confirm] / [Reset to default] inline buttons + 30s defensive auto-hide; slide.js enterSendMode gates pushTxBytes(autoSendBytes) on first-use confirmation when value is non-default and not already confirmed; shouldSurfaceFirstUseConfirm short-circuits for the default value and for previously-confirmed values; cargo + npm test:fast baselines preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add validation-hint markup + invalid-state CSS in index.html + main.js Settings handler wiring</name>
  <read_first>
    - www/index.html — search for slide-auto-send-row to locate the existing Settings row markup
    - www/main.js — locate the existing Settings input change handler for #slide-auto-send-input (search for slide-auto-send-input in main.js)
    - www/main.js — locate wireSlideDispatcher boot wiring + how prefs reference is currently passed
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-UI-SPEC.md sections D and E — locked DOM + CSS
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-PATTERNS.md section "www/index.html" — verbatim markup + CSS
  </read_first>
  <files>
    www/index.html
    www/main.js
  </files>
  <behavior>
    index.html gains:
    - A new <div class="hint validation-hint" id="slide-auto-send-validation-hint" hidden>Auto-send command unsafe — using disabled.</div> as a sibling of the existing "\r appended automatically" hint inside #slide-auto-send-row.
    - CSS rule #slide-auto-send-input[data-invalid="true"] { border-color: rgba(255,255,255,0.6); }
    - CSS rule #slide-auto-send-row .validation-hint { padding-top: 4px; }
    - CSS rule #slide-auto-send-row .validation-hint[hidden] { display: none; }

    main.js Settings handler:
    - On input.value change (or input event), compute `value + '\r'` then call savePrefs.
    - Set/clear data-invalid based on isAutoSendSafe(value + '\r').
    - Re-arm slideAutoSendCommandConfirmed by writing the new value alongside the auto-send command.
    - When user fixes the input back to a safe value, clear data-invalid and hide the validation-hint.

    main.js wires savePrefs into wireSlideDispatcher opts (used by Task 2's first-use confirmation reset).
  </behavior>
  <action>
    Step 1 — Modify www/index.html.

    Find the existing #slide-auto-send-row block. Add the validation-hint sub-row immediately AFTER the existing "\r appended automatically" .hint div:

        <div class="settings-row" id="slide-auto-send-row">
          <label for="slide-auto-send-input">Auto-send command:</label>
          <input type="text" id="slide-auto-send-input" value="B:SLIDE R" autocomplete="off" spellcheck="false">
          <div class="hint">\r appended automatically</div>
          <div class="hint validation-hint" id="slide-auto-send-validation-hint" hidden>Auto-send command unsafe — using disabled.</div>
        </div>

    The hint copy `Auto-send command unsafe — using disabled.` is locked verbatim per 12-UI-SPEC.md section "Settings auto-send validation hint" — em-dash U+2014, trailing period.

    Find the existing <style> block that contains the Phase 11 SLIDE-related CSS rules (e.g., near #slide-auto-send-row or #settings-slide). Add the Phase 12 CSS additions verbatim from 12-UI-SPEC.md section E:

        /* ==== Phase 12 SLIDE-38 settings auto-send invalid state ==== */

        #slide-auto-send-input[data-invalid="true"] {
          /* Muted border to indicate validation rejection without destructive red.
             Visual cue only — does NOT block savePrefs. */
          border-color: rgba(255, 255, 255, 0.6);
        }

        #slide-auto-send-row .validation-hint {
          /* Inherits .hint typography (12px, muted, line-height 1.5).
             Adds 4px top padding to visually separate from the always-on
             "\r appended automatically" hint above. */
          padding-top: 4px;
        }

        #slide-auto-send-row .validation-hint[hidden] {
          display: none;
        }

    Constraints:
    - DO NOT add any new color hex values (Phase 12 hard invariant — zero new colors).
    - DO NOT use destructive red — muted rgba(255,255,255,0.6) only (12-UI-SPEC §Destructive policy).
    - DO NOT modify the existing .hint rule — the .validation-hint class extends it.

    Step 2 — Modify www/main.js — Settings input handler extension.

    Locate the existing handler for #slide-auto-send-input. The Phase 11 shape is roughly:

        const slideAutoSendInput = document.getElementById('slide-auto-send-input');
        slideAutoSendInput?.addEventListener('change', () => {
            const value = slideAutoSendInput.value;
            savePrefs({ slideAutoSendCommand: value + '\r' });
        });

    Replace with the Phase 12 extended handler that:
    1. Imports isAutoSendSafe from prefs.js (alongside the existing savePrefs / loadPrefs imports).
    2. Computes `cmdWithCr = value + '\r'`.
    3. Toggles data-invalid + hint visibility based on isAutoSendSafe(cmdWithCr).
    4. Re-arms slideAutoSendCommandConfirmed by writing it alongside the auto-send command (so any change re-arms first-use confirmation per 12-UI-SPEC §"Auto-send first-use detection rule").

    Verbatim:

        // Phase 12 SLIDE-38: extended Settings handler.
        const slideAutoSendInput = document.getElementById('slide-auto-send-input');
        const slideAutoSendValidationHint = document.getElementById('slide-auto-send-validation-hint');
        slideAutoSendInput?.addEventListener('change', () => {
            const value = slideAutoSendInput.value;
            const cmdWithCr = value + '\r';
            const safe = isAutoSendSafe(cmdWithCr);

            // Visual cue only — does NOT block savePrefs (per UI-SPEC §Anti-Patterns).
            if (safe) {
                slideAutoSendInput.removeAttribute('data-invalid');
                slideAutoSendInput.removeAttribute('aria-invalid');
                if (slideAutoSendValidationHint) slideAutoSendValidationHint.hidden = true;
            } else {
                slideAutoSendInput.setAttribute('data-invalid', 'true');
                slideAutoSendInput.setAttribute('aria-invalid', 'true');
                // Hint stays hidden until use-time gate fires (per UI-SPEC §"Settings input invalid-state visual lifecycle").
            }

            // Persist + re-arm first-use confirmation (any change resets the confirmation flag).
            savePrefs({
                slideAutoSendCommand: cmdWithCr,
                slideAutoSendCommandConfirmed: '',   // re-arm
            });
        });

    Step 3 — Modify www/main.js — pass savePrefs through wireSlideDispatcher opts.

    Locate the existing wireSlideDispatcher({...}) call. Add savePrefs to the opts:

        wireSlideDispatcher({
            // ... existing opts unchanged ...
            prefs: prefsLive,
            savePrefs: savePrefs,           // Phase 12 SLIDE-38 — for first-use confirmation reset
            // ... rest unchanged ...
        });

    The savePrefs reference is the existing import from www/state/prefs.js used elsewhere in main.js. If main.js currently imports it as part of a barrel import, add it; if savePrefs is already in scope, reuse the binding.

    Step 4 — Boot-time data-invalid sync.

    After loadPrefs runs and before the user touches anything, sync the data-invalid attribute to match the loaded value (so a previously-saved unsafe value shows the cue on reload):

        // Phase 12 SLIDE-38: boot-time data-invalid sync.
        try {
            const liveCmd = prefsLive.slideAutoSendCommand;
            if (slideAutoSendInput && !isAutoSendSafe(liveCmd)) {
                slideAutoSendInput.setAttribute('data-invalid', 'true');
                slideAutoSendInput.setAttribute('aria-invalid', 'true');
            }
        } catch {}

    Place this block AFTER the existing prefs hydration and BEFORE wireSlideChip / wireSlideDispatcher boot.
  </action>
  <verify>
    <automated>grep -q "slide-auto-send-validation-hint" www/index.html; r1=$?; grep -q "Auto-send command unsafe" www/index.html; r2=$?; grep -q "data-invalid" www/index.html; r3=$?; grep -q "isAutoSendSafe" www/main.js; r4=$?; grep -q "slideAutoSendCommandConfirmed" www/main.js; r5=$?; bash scripts/build.sh; r6=$?; cd www && npm run test:fast; r7=$?; test $r1 -eq 0 && test $r2 -eq 0 && test $r3 -eq 0 && test $r4 -eq 0 && test $r5 -eq 0 && test $r6 -eq 0 && test $r7 -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'id="slide-auto-send-validation-hint"' www/index.html succeeds.
    - grep -q "Auto-send command unsafe" www/index.html succeeds (em-dash + period verbatim).
    - grep -q '#slide-auto-send-input\[data-invalid="true"\]' www/index.html succeeds (CSS rule).
    - grep -q "validation-hint" www/index.html succeeds.
    - grep -q "isAutoSendSafe" www/main.js succeeds (handler imports + uses).
    - grep -q "slideAutoSendCommandConfirmed" www/main.js succeeds (re-arm logic).
    - grep -q "savePrefs" www/main.js succeeds (already present; verify reference is threaded into wireSlideDispatcher opts).
    - bash scripts/build.sh exits 0 (no Rust drift; Phase 12 hard invariant).
    - cd www && npm run test:fast exits 0 (no Phase 4-11 regressions; Phase 11 Settings-row tests still green).
  </acceptance_criteria>
  <done>
    index.html has validation-hint sub-row hidden by default + invalid-state CSS; main.js Settings input handler toggles data-invalid + aria-invalid based on isAutoSendSafe and re-arms slideAutoSendCommandConfirmed on every change; main.js boot syncs data-invalid for previously-saved unsafe values; savePrefs threaded through wireSlideDispatcher opts; bash scripts/build.sh + npm test:fast green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create slide-autosend-safety.spec.js with 15 Playwright tests + flip SLIDE-38 to Complete</name>
  <read_first>
    - www/tests/transport/slide-prefs.spec.js — Settings panel setup + localStorage polling pattern
    - www/tests/transport/slide-chip.spec.js — chip lifecycle introspection pattern via window.__slideChip.__getStateForTests().lifecycle
    - www/tests/transport/slide-sender.spec.js — mock-bot setup + setInputFiles harness for the use-time gate test
    - www/tests/transport/mock-serial.js — SERIAL_MOCK + __mockWriterLog
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-VALIDATION.md — task slots 12-XX-12..12-XX-17 + the SAFE_CASES / UNSAFE_CASES coverage requirements
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-RESEARCH.md section "SLIDE-38: Safety spec skeleton" — verbatim test scaffolding
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-UI-SPEC.md section "SLIDE-38" — locked SAFE_CASES + UNSAFE_CASES + chip transition table
  </read_first>
  <files>
    www/tests/transport/slide-autosend-safety.spec.js
  </files>
  <behavior>
    15 tests total per 12-VALIDATION.md slots 12-XX-12..12-XX-17 expanded:

    Pure-helper tests (drive isAutoSendSafe directly via window.__slide.__isAutoSendSafeForTests):
    - Test 1: accepts '' (SLIDE-13 disabled sentinel)
    - Test 2: accepts 'B:SLIDE R\r' (default)
    - Test 3: accepts 'A:SLIDE R\r' (drive switch)
    - Test 4: accepts 'B:DIR\r' (alternate command)
    - Test 5: accepts '\r' (bare CR — Pitfall 5)
    - Test 6: rejects 'B:SLIDE R' (missing CR)
    - Test 7: rejects 'B:SLIDE R\n' (LF instead of CR)
    - Test 8: rejects 'B:SLIDE R; rm -rf /\r' (semicolon)
    - Test 9: rejects 'B:SLIDE R\rB:DIR\r' (multiple CR)
    - Test 10: rejects 'B:SLIDE\x07R\r' (control char BEL)

    Settings + use-time integration tests:
    - Test 11: Settings input invalid value sets data-invalid="true" on save
    - Test 12: Settings input invalid value still persists to localStorage (save not blocked per Anti-Patterns)
    - Test 13: Use-time gate — enterSendMode with unsafe value emits zero auto-type bytes on __mockWriterLog
    - Test 14: First-use confirmation chip surfaces for non-default value via enterSendMode (chip lifecycle === 'first-use-confirm')
    - Test 15: First-use confirmation flag re-arms when user changes auto-send (slideAutoSendCommandConfirmed reset to '')
  </behavior>
  <action>
    Create www/tests/transport/slide-autosend-safety.spec.js. The file is NEW. Per spec-isolation convention, copy setup helpers verbatim from slide-prefs.spec.js + slide-chip.spec.js — DO NOT cross-import.

    Structure:

        import { test, expect } from '@playwright/test';
        import { SERIAL_MOCK } from './mock-serial.js';
        import { MOCK_SERIAL_SLIDE_BOT } from './mock-serial-slide-bot.js';

        async function setup(page) {
            await page.addInitScript(SERIAL_MOCK);
            await page.addInitScript(MOCK_SERIAL_SLIDE_BOT);
            await page.goto('/');
            await page.locator('#terminal-wrapper').focus();
            await page.waitForFunction(() => document.getElementById('terminal').width > 0);
            await page.locator('#settings').evaluate((el) => { el.open = true; });
            await page.locator('#settings-slide').evaluate((el) => { el.open = true; });
        }

        async function setupConnected(page) {
            await setup(page);
            await page.locator('#connect-button').click();
            await expect.poll(
                () => page.evaluate(() => Boolean(navigator.serial._grantedPorts[0]?._reader)),
                { timeout: 8000 },
            ).toBe(true);
            await page.evaluate(() => {
                window.__slide?.__resetForTests?.();
                window.__fileSource?.__resetForTests?.();
                if (window.__mockWriterLog) window.__mockWriterLog.length = 0;
                window.__mockSlideBot?.reset?.();
            });
        }

        const SAFE_CASES = [
            { input: '',                  label: 'empty (SLIDE-13 disabled)' },
            { input: 'B:SLIDE R\r',        label: 'default' },
            { input: 'A:SLIDE R\r',        label: 'drive switch' },
            { input: 'B:DIR\r',            label: 'alternate command' },
            { input: '\r',                 label: 'bare CR' },
        ];
        const UNSAFE_CASES = [
            { input: 'B:SLIDE R',          label: 'missing CR' },
            { input: 'B:SLIDE R\n',        label: 'LF instead of CR' },
            { input: 'B:SLIDE R; rm -rf /\r', label: 'semicolon injection' },
            { input: 'B:SLIDE R\rB:DIR\r',   label: 'multiple CR' },
            { input: 'B:SLIDE\x07R\r',     label: 'control char BEL' },
        ];

        for (const tc of SAFE_CASES) {
            test(`SLIDE-38 — isAutoSendSafe accepts: ${tc.label}`, async ({ page }) => {
                await setup(page);
                const ok = await page.evaluate((s) => window.__slide.__isAutoSendSafeForTests(s), tc.input);
                expect(ok).toBe(true);
            });
        }
        for (const tc of UNSAFE_CASES) {
            test(`SLIDE-38 — isAutoSendSafe rejects: ${tc.label}`, async ({ page }) => {
                await setup(page);
                const ok = await page.evaluate((s) => window.__slide.__isAutoSendSafeForTests(s), tc.input);
                expect(ok).toBe(false);
            });
        }

        test('SLIDE-38 — Settings input invalid value sets data-invalid attribute', async ({ page }) => {
            await setup(page);
            await page.locator('#slide-auto-send-input').fill('B:RM *.* ;');
            await page.locator('#slide-auto-send-input').dispatchEvent('change');
            await expect.poll(
                () => page.locator('#slide-auto-send-input').getAttribute('data-invalid'),
                { timeout: 2000 },
            ).toBe('true');
        });

        test('SLIDE-38 — Settings input invalid value still persists to localStorage (save not blocked)', async ({ page }) => {
            await setup(page);
            await page.locator('#slide-auto-send-input').fill('B:RM *.* ;');
            await page.locator('#slide-auto-send-input').dispatchEvent('change');
            await expect.poll(
                () => page.evaluate(() => {
                    const raw = localStorage.getItem('beastty.prefs');
                    if (!raw) return null;
                    try { return JSON.parse(raw).slideAutoSendCommand; } catch { return null; }
                }),
                { timeout: 2000 },
            ).toBe('B:RM *.* ;\r');
        });

        test('SLIDE-38 — use-time gate blocks unsafe auto-type at session start', async ({ page }) => {
            await setupConnected(page);
            // Plant an unsafe value directly into the live prefs ref (bypassing the Settings handler
            // so we exercise the slide.js use-time gate alone).
            await page.evaluate(() => {
                window.__prefs.live.slideAutoSendCommand = 'B:SLIDE R; rm\r';
            });
            // Drive enterSendMode via the file picker.
            await page.setInputFiles('#send-file-input', {
                name: 'hello.txt', mimeType: 'text/plain', buffer: Buffer.from('hi'),
            });
            // Click [Send 1 file] — Phase 9 happy path triggers enterSendMode.
            await expect(page.locator('#send-modal')).toBeVisible();
            await page.locator('#send-modal-send').click();
            // Allow a short window for the dispatcher to fire (or fail to fire) auto-type.
            await page.waitForTimeout(500);
            // Assert: NO auto-type bytes contain semicolons (the unsafe content was blocked).
            const writerHasSemicolon = await page.evaluate(() => {
                const log = window.__mockWriterLog || [];
                for (const chunk of log) {
                    for (let i = 0; i < chunk.length; i++) {
                        if (chunk[i] === 0x3B) return true;   // ASCII ';'
                    }
                }
                return false;
            });
            expect(writerHasSemicolon).toBe(false);
            // Optional: assert chip enterError was fired.
            const chipState = await page.evaluate(() => window.__slideChip?.__getStateForTests?.()?.lifecycle);
            expect(['error', 'hidden']).toContain(chipState);
        });

        test('SLIDE-38 — first-use confirmation chip surfaces for non-default value', async ({ page }) => {
            await setupConnected(page);
            // Plant a SAFE but non-default value + ensure not previously confirmed.
            await page.evaluate(() => {
                window.__prefs.live.slideAutoSendCommand = 'A:SLIDE R\r';
                window.__prefs.live.slideAutoSendCommandConfirmed = '';
            });
            await page.setInputFiles('#send-file-input', {
                name: 'hello.txt', mimeType: 'text/plain', buffer: Buffer.from('hi'),
            });
            await expect(page.locator('#send-modal')).toBeVisible();
            await page.locator('#send-modal-send').click();
            await expect.poll(
                () => page.evaluate(() => window.__slideChip?.__getStateForTests?.()?.lifecycle),
                { timeout: 8000 },
            ).toBe('first-use-confirm');
        });

        test('SLIDE-38 — confirmation flag re-arms when user changes auto-send command', async ({ page }) => {
            await setup(page);
            // Set initial value + simulate a confirmation.
            await page.locator('#slide-auto-send-input').fill('A:SLIDE R');
            await page.locator('#slide-auto-send-input').dispatchEvent('change');
            await expect.poll(
                () => page.evaluate(() => {
                    const raw = localStorage.getItem('beastty.prefs');
                    if (!raw) return null;
                    try { return JSON.parse(raw).slideAutoSendCommandConfirmed; } catch { return null; }
                }),
                { timeout: 2000 },
            ).toBe('');   // re-armed (empty string sentinel)

            // Now manually mark as confirmed.
            await page.evaluate(() => {
                window.__prefs.live.slideAutoSendCommandConfirmed = 'A:SLIDE R\r';
            });

            // Change the command — confirmation should re-arm to '' again.
            await page.locator('#slide-auto-send-input').fill('B:DIR');
            await page.locator('#slide-auto-send-input').dispatchEvent('change');
            await expect.poll(
                () => page.evaluate(() => {
                    const raw = localStorage.getItem('beastty.prefs');
                    if (!raw) return null;
                    try { return JSON.parse(raw).slideAutoSendCommandConfirmed; } catch { return null; }
                }),
                { timeout: 2000 },
            ).toBe('');
        });

    Constraints:
    - 15 tests total (5 SAFE_CASES + 5 UNSAFE_CASES + 5 integration tests).
    - Test names contain the substrings 12-VALIDATION.md uses for -g filters: `accepts`, `rejects`, `Settings input invalid`, `use-time gate`, `first-use confirmation`, `re-arms`.
    - Use `window.__slide.__isAutoSendSafeForTests` for unit-style tests (the introspection export added in Task 1).
    - Use `window.__prefs.live` for direct prefs mutation (Phase 11 Plan 11-05 D-3 deviation precedent).
    - 8s polling timeouts cover Chromium parallel-load throttling.
    - DO NOT cross-import from other spec files.

    Run the tests:

        cd www && npx playwright test transport/slide-autosend-safety.spec.js --workers=1

    All 15 must pass. If any fails, fix the implementation in Plan 12-03 Tasks 1-3 (the test names + assertions are the locked contract per 12-VALIDATION.md).

    After all 15 pass, flip SLIDE-38 to Complete in REQUIREMENTS.md:

        sed -i 's/^- \[ \] \*\*SLIDE-38/- [x] **SLIDE-38/' .planning/REQUIREMENTS.md
        sed -i 's/| SLIDE-38 | Phase 12 | Pending |/| SLIDE-38 | Phase 12 | Complete |/' .planning/REQUIREMENTS.md
  </action>
  <verify>
    <automated>cd www && npx playwright test transport/slide-autosend-safety.spec.js --workers=1; r1=$?; cd .. && grep -c "^test(" www/tests/transport/slide-autosend-safety.spec.js > /tmp/test_count_38.txt; awk '{exit ($1 == 15) ? 0 : 1}' /tmp/test_count_38.txt; r2=$?; grep -q "^- \[x\] \*\*SLIDE-38" .planning/REQUIREMENTS.md; r3=$?; grep -qE "^\| SLIDE-38 \| Phase 12 \| Complete" .planning/REQUIREMENTS.md; r4=$?; test $r1 -eq 0 && test $r2 -eq 0 && test $r3 -eq 0 && test $r4 -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - test -f www/tests/transport/slide-autosend-safety.spec.js succeeds.
    - grep -c "^test(" www/tests/transport/slide-autosend-safety.spec.js returns 15 (counting both inline and templated test() calls; the SAFE_CASES + UNSAFE_CASES for-loops produce 10 of these).
    - grep -q "isAutoSendSafe accepts" www/tests/transport/slide-autosend-safety.spec.js succeeds.
    - grep -q "isAutoSendSafe rejects" www/tests/transport/slide-autosend-safety.spec.js succeeds.
    - grep -q "data-invalid" www/tests/transport/slide-autosend-safety.spec.js succeeds.
    - grep -q "use-time gate" www/tests/transport/slide-autosend-safety.spec.js succeeds.
    - grep -q "first-use" www/tests/transport/slide-autosend-safety.spec.js succeeds.
    - grep -q "re-arm" www/tests/transport/slide-autosend-safety.spec.js succeeds.
    - cd www && npx playwright test transport/slide-autosend-safety.spec.js --workers=1 exits 0 with all 15 tests green.
    - REQUIREMENTS.md SLIDE-38 row Pending → Complete (top-level checkbox + traceability table).
    - cd www && npm run test:fast exits 0 (no Phase 4-11 regressions).
  </acceptance_criteria>
  <done>
    15 SLIDE-38 Playwright tests green deterministically; SLIDE-38 flipped Pending → Complete in REQUIREMENTS.md; cd www && npm run test:fast green; cargo --workspace 283/283 baseline preserved; bash scripts/build.sh exit 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Settings input #slide-auto-send-input | Untrusted command string entering the prefs blob |
| prefs.js → slide.js readAutoSendCommandBytes | Persisted command crosses to the wire-write path |
| slide.js → Z80 wire | Auto-typed bytes reach the CP/M prompt |
| User → first-use-confirm chip [Confirm] / [Reset to default] | User-facing confirmation surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering / Elevation of Privilege | Auto-send command injection (e.g., `B:RM *.* ; SLIDE R\r`) reaches Z80 prompt | mitigate | SAFE_AUTO_SEND_RE = /^[A-Za-z0-9:]*\r$/ at use site in slide.js readAutoSendCommandBytes — rejects `;`, pipes, LF, multiple CR, control chars, backslash. Empty string short-circuits per SLIDE-13 disabled sentinel. Save-time visual cue (data-invalid + .validation-hint) provides defense-in-depth UX feedback without blocking save (per Anti-Patterns). |
| T-12-04 | Spoofing | User tricked into saving a non-default value (hostile-config injection via shared prefs export, social engineering) | mitigate | First-use confirmation chip surfaces ONLY at session START (Pitfall 6) when prefs.slideAutoSendCommandConfirmed !== current value. User must click [Confirm] before the command reaches the wire. Confirmation flag is exact-string-keyed — any change re-arms. Chip auto-hides after 30 s if user dismisses (defensive — no chip should pin indefinitely). |
| T-12-05 | Information Disclosure | XSS via auto-send command rendered in chip text | mitigate | Chip text rendering uses escapeHtml (existing slide-chip.js helper) on the visibleValue. The literal `\r` is rendered as the two-character sequence (NOT a control char) per UI-SPEC. innerHTML write is gated by escape-by-construction; the safety regex already rejects HTML-relevant characters (`<`, `>`, `&`, `"`, `'` are not in [A-Za-z0-9:]) so post-validation values cannot carry XSS payloads anyway. |
| T-12-07 | Repudiation | First-use chip dismissed via Esc / 30s timeout but session still proceeds | mitigate | enterFirstUseConfirm 30s timeout calls `hide()` (no-op for the session); the original surfaceFirstUseConfirm Promise NEVER resolves to `true` from a timeout. enterSendMode awaits the Promise; on dismiss the Promise is GC'd unresolved (a deviation rather than a true escape — the dispatchSendMode loop waits indefinitely until a session restart). NOTE: this is a Phase 12 known limitation — flag in SUMMARY for Phase 12.1 cleanup. |
| T-12-11 | Denial of Service | Use-time gate fires DOM mutations on every send attempt (data-invalid setAttribute + hint unhide) | accept | DOM mutations are O(1) per send attempt; the user-visible cost is irrelevant. |
</threat_model>

<verification>
- cd www && npx playwright test transport/slide-autosend-safety.spec.js --workers=1 → all 15 tests green
- cd www && npm run test:fast → no Phase 4-11 regressions; Phase 11 chip + prefs tests still green
- cargo test --workspace --quiet → 283/283 baseline preserved (Phase 12 hard invariant)
- bash scripts/build.sh → exit 0 (no wasm regeneration needed)
- grep -E "^- \[x\] \*\*SLIDE-38" .planning/REQUIREMENTS.md → 1 line (top-level checkbox flipped)
- grep -E "^\| SLIDE-38 \| Phase 12 \| Complete" .planning/REQUIREMENTS.md → 1 line (traceability table flipped)
</verification>

<success_criteria>
- SAFE_AUTO_SEND_RE = /^[A-Za-z0-9:]*\r$/ locked at use time in slide.js; bypassed for empty string per SLIDE-13.
- Save-time visual cue (data-invalid + .validation-hint + aria-invalid) appears on Settings input when value is unsafe; does NOT block save.
- First-use-confirm chip lifecycle state with [Confirm] / [Reset to default] inline buttons + 30 s defensive auto-hide.
- Confirmation flag (slideAutoSendCommandConfirmed) keyed to exact string; re-arms automatically on any Settings change.
- 15 new Playwright tests pass deterministically (5 SAFE + 5 UNSAFE pure-helper tests + 5 integration tests).
- SLIDE-38 flipped Pending → Complete in REQUIREMENTS.md.
- Phase 4-11 test:fast baseline preserved; cargo --workspace 283/283; bash scripts/build.sh exit 0.
- Zero Rust changes (Phase 12 hard invariant).
</success_criteria>

<output>
After completion, create `.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-03-SUMMARY.md` documenting:
- prefs.js + slide.js + slide-chip.js + index.html + main.js LOC deltas with key signatures.
- New isAutoSendSafe + enterFirstUseConfirm + shouldSurfaceFirstUseConfirm + surfaceFirstUseConfirm helpers.
- 15 new Playwright tests + which 12-VALIDATION.md task IDs they fill (12-XX-12..12-XX-17).
- REQUIREMENTS.md SLIDE-38 flip.
- Any auto-fix deviations (Rule 1 / Rule 2 / Rule 3 per execute-plan workflow).
- T-12-07 (first-use chip Esc/timeout dismissal) called out as a known limitation if not fully resolved.
- Confirmation that cd www && npm run test:fast green + cargo --workspace 283/283 + bash scripts/build.sh exit 0.
</output>

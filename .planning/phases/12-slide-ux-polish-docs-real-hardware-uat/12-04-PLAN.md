---
phase: 12
plan: 04
type: execute
wave: 3
depends_on: [12-01, 12-02, 12-03]
files_modified:
  - docs/SLIDE_Z80_REQUIREMENT.md
  - README.md
autonomous: true
requirements: [SLIDE-40, SLIDE-41]
must_haves:
  truths:
    - "docs/SLIDE_Z80_REQUIREMENT.md exists at the repo root and documents (a) the ESC^SLIDE wakeup signature, (b) the v0.2.1 CTRL_CAN bidirectional amendment per ADR-003, (c) the B:SLIDE R command convention, (d) a link to the upstream github.com/blowback/slide repository"
    - "docs/SLIDE_Z80_REQUIREMENT.md carries a 'Status: pending upstream merge' banner because the slide.asm ESC^SLIDE emission and CTRL_CAN echo are not yet on upstream master (verified by inspecting /home/ant/src/microbeast/SLIDE/slide.asm)"
    - "README.md gains a new 'File transfer (SLIDE)' section covering Sending files, Receiving files, and Cancelling sub-sections"
    - "README.md 'Keyboard shortcuts' table is extended with 3 new rows referencing drag-drop, the file-picker button, and Esc-to-cancel during SLIDE transfers"
    - "Both new docs use the project name 'Beastty' (NOT 'BestialiTTY' — per MEMORY.md project_renamed_to_beastty); existing files keep their current spelling per scope boundary"
  artifacts:
    - path: "docs/SLIDE_Z80_REQUIREMENT.md"
      provides: "Z80 firmware requirements doc covering wakeup + v0.2.1 amendment + B:SLIDE R + upstream PR coordination"
      min_lines: 50
    - path: "README.md"
      provides: "New 'File transfer (SLIDE)' section + extended Keyboard shortcuts table"
      contains: "File transfer"
  key_links:
    - from: "docs/SLIDE_Z80_REQUIREMENT.md"
      to: ".planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md"
      via: "explicit reference + cite for the v0.2.1 amendment"
      pattern: "ADR-003"
    - from: "docs/SLIDE_Z80_REQUIREMENT.md"
      to: "github.com/blowback/slide"
      via: "URL link + 'Status: pending upstream merge' banner"
      pattern: "github.com/blowback/slide"
---

<objective>
Close SLIDE-40 (Z80-side requirements doc) and SLIDE-41 (README extension). Both deliverables are pure Markdown — no code, no DOM, no CSS. Per CONTEXT Claude's Discretion defaults (locked) and 12-UI-SPEC.md §"Documentation deliverables (NO UI surface)":

1. SLIDE-40: Create `docs/SLIDE_Z80_REQUIREMENT.md` (NEW; the `docs/` directory does not yet exist — create it). Brief doc, both audiences (Z80 firmware authors + Beastty users), 4 sections per CONTEXT outline + Pitfall 7 (link to repo root, not a hardcoded PR #) + "Status: pending upstream merge" banner per UAT-10-01 idiom.
2. SLIDE-41: Extend `README.md` (in place) with a new "File transfer (SLIDE)" section + 3 new rows in the existing "Keyboard shortcuts" table.

Purpose:
- Z80 firmware authors maintaining the upstream `github.com/blowback/slide` repo have a single canonical source of Beastty's expectations (wakeup signature, v0.2.1 CTRL_CAN echo, command convention).
- New README readers discover SLIDE without diving into PROJECT.md / ROADMAP.md.
- Both docs reference behaviors landed in Plans 12-01..12-03 (collision modal, first-use confirm chip, drag-drop ↔ pointer-select isolation).

Output:
- New file `docs/SLIDE_Z80_REQUIREMENT.md` (~80-150 lines).
- Modified `README.md` with appended "File transfer (SLIDE)" section + 3 new keyboard-shortcut rows.

Depends on Plans 12-01..12-03: README content references the SLIDE-12 drag-drop coexistence, SLIDE-36 collision modal, and SLIDE-38 first-use confirm chip — those must ship first so the doc text accurately describes shipped behavior.
</objective>

<execution_context>
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/workflows/execute-plan.md
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-CONTEXT.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-RESEARCH.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-PATTERNS.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-VALIDATION.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-01-SUMMARY.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-02-SUMMARY.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-03-SUMMARY.md

@README.md
@.planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md

<interfaces>
docs/ directory location: REPO ROOT (NOT inside .planning/).

    /home/ant/src/microbeast/bestialitty/docs/SLIDE_Z80_REQUIREMENT.md   <-- NEW
    /home/ant/src/microbeast/bestialitty/docs/SLIDE-UAT.md                <-- Plan 12-05 creates

The docs/ directory does NOT exist yet (verified via `ls /home/ant/src/microbeast/bestialitty/docs` returns "no docs/ dir"). The first task in this plan creates it.

ADR-003 reference path (relative to repo root):
    .planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md

Upstream slide.asm path (READ ONLY for verification — confirms PR not yet merged):
    /home/ant/src/microbeast/SLIDE/slide.asm
    /home/ant/src/microbeast/SLIDE/SPEC-v0.2.md

Verified-current README.md state (102 lines per RESEARCH §Standard Stack — read in full before editing):
- Existing "Keyboard shortcuts" section header at ~line 37-43.
- Existing "UI / clipboard" table at ~line 45-53.
- "Can I run it locally?" section appears ~line 88. The new "File transfer (SLIDE)" section is appended BEFORE this section per CONTEXT Claude's Discretion default ("append the new section; extend keyboard shortcuts in place").

Project naming convention (per MEMORY.md):
- USE "Beastty" in NEW files (SLIDE-40 doc + Plan 12-05 UAT).
- Existing files (PROJECT.md, CLAUDE.md, README.md) currently mix "Beastty" and "BestialiTTY"; the README extension follows the spelling already in use in the surrounding text.

Pitfall 7 — DO NOT hardcode a PR number. Link to `https://github.com/blowback/slide` (repo root); use prose ("see open PRs for the v0.2.1 amendment branch") instead of a PR URL.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/ directory and write docs/SLIDE_Z80_REQUIREMENT.md</name>
  <read_first>
    - .planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md IN FULL — Phase 12 SLIDE-40 cites this ADR as the protocol authority + reuses Nygard structure
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-CONTEXT.md §"Specifics" — the SLIDE-40 outline (locked)
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-RESEARCH.md §"Section: SLIDE-40 — Z80 Requirement Doc" + §"Pitfall 7" — the PR-pending banner pattern + repo-root linking convention
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-PATTERNS.md §"docs/SLIDE_Z80_REQUIREMENT.md" — verbatim doc outline
    - /home/ant/src/microbeast/SLIDE/SPEC-v0.2.md (cited but not modified)
  </read_first>
  <files>
    docs/SLIDE_Z80_REQUIREMENT.md
  </files>
  <behavior>
    - The docs/ directory is created if missing (`mkdir -p docs`).
    - docs/SLIDE_Z80_REQUIREMENT.md is created with a Nygard-inspired ADR-style header (Status / Date / Audience) per ADR-003 precedent.
    - Content covers the four locked sections per CONTEXT D-discretion outline:
      1. Wakeup signature: ESC ^ S L I D E (7 bytes 0x1B 0x5E 0x53 0x4C 0x49 0x44 0x45)
      2. v0.2.1 amendment: bidirectional CTRL_CAN echo (cite ADR-003)
      3. Send command convention: B:SLIDE R
      4. Upstream patch coordination — link to github.com/blowback/slide repo root (NO hardcoded PR #) + "Status: pending upstream merge" banner per Pitfall 7
    - Audience framed for both Z80 firmware authors and Beastty users (per CONTEXT Claude's Discretion default).
    - NO inlined slide.asm patch diff (per CONTEXT default: "brief — protocol amendment summary + B:SLIDE R + link to upstream PR; no inlined diff").
    - Cross-link section listing ADR-003 + SPEC-v0.2.md + upstream repo.
  </behavior>
  <action>
    Step 1 — Create the docs/ directory at the repo root:

        mkdir -p /home/ant/src/microbeast/bestialitty/docs

    Step 2 — Write docs/SLIDE_Z80_REQUIREMENT.md.

    Use the verbatim outline from 12-PATTERNS.md §"docs/SLIDE_Z80_REQUIREMENT.md (create — markdown)" as the structural template. Adapt the prose to Beastty's project voice (see existing README.md tone for reference — calm, informational, no emojis, no exclamation marks).

    Required header:

        # SLIDE — Z80-side requirements for Beastty

        **Status:** Pending upstream merge (see §Upstream patch below)
        **Date:** 2026-05-08
        **Audience:** Z80 firmware authors maintaining slide.com on the MicroBeast,
                      and Beastty users running the SLIDE protocol against patched
                      MicroBeast firmware.

        > Beastty is an in-browser VT52 terminal emulator for the MicroBeast Z80
        > retrocomputer. The SLIDE protocol layered on top of VT52 enables file
        > transfer between the host PC and the Z80. This document specifies the
        > Z80-side behavior Beastty depends on.

    Required sections (in order):

    1. **Wakeup signature: ESC ^ S L I D E** — Document the 7-byte signature `0x1B 0x5E 0x53 0x4C 0x49 0x44 0x45`. Beastty enters SLIDE recv mode only on detecting this prefix on the inbound serial stream. Pre-v0.2.1 slide.com does NOT emit it; modern slide.com (post-PR) does. Mention torn-chunk safety: the signature is detected across arbitrary Web Serial chunk boundaries.

    2. **v0.2.1 amendment: bidirectional CTRL_CAN echo** — Cite ADR-003 (`.planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md`) as the authoritative source. Either side may initiate `CTRL_CAN` (raw byte `0x18`); the other side MUST echo `CTRL_CAN` back within ~500 ms. Both sides drain the wire and return to idle. The amendment makes CAN symmetric — previously only the receiver could emit it. Quote ADR-003 §Decision points 1-4 by reference (do not duplicate the full ADR content here).

    3. **Send command convention: B:SLIDE R** — Beastty's default auto-send command is `B:SLIDE R\r` (drive `B:`, command `SLIDE`, mode `R` for receive). The Z80 must accept this at the CP/M prompt and enter SLIDE receive mode within ~250 ms (Beastty's wakeup-detection window — Phase 11 D-15 establishes a 3 s timeout fallback for slow Z80 boards). Note the SLIDE-38 (Phase 12) safety regex `/^[A-Za-z0-9:]*\r$/` — the auto-send command is validated client-side before any bytes hit the wire, so Z80 firmware authors do not need to sanitize input on their end.

    4. **Upstream patch** — The Z80-side patch implementing items 1-2 above is tracked at `https://github.com/blowback/slide`. Status: **pending upstream merge** as of 2026-05-08 (verified by inspection of `slide.asm` HEAD on the upstream main branch — no ESC^SLIDE emission, no CTRL_CAN echo path). Until the patch lands, Beastty's `Compatibility mode` Settings option (default `auto`) provides a 3-second wakeup-timeout fallback path for stock slide.com (per Phase 11 D-15). Per Pitfall 7: do NOT hardcode a PR number — link to the repo root; readers can find the open branch from there.

    5. **Cross-link** — List with relative paths:
        - `.planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md` — the protocol authority for the v0.2.1 amendment
        - `https://github.com/blowback/slide` — upstream Z80 reference implementation
        - `SPEC-v0.2.md` (in the upstream repo) — base v0.2 protocol specification
        - `https://github.com/microbeast/microbeast` — MicroBeast hardware reference (if known) — omit if unknown

    Constraints:
    - DO NOT inline a slide.asm patch diff (CONTEXT default).
    - DO NOT hardcode a PR number (Pitfall 7).
    - DO NOT use emojis (project voice).
    - DO NOT use exclamation marks (project voice).
    - The doc length target is ~80-150 lines (Pitfall 8 — UAT scope creep; SLIDE-40 has the same brevity bias).
    - Use em-dash `—` (U+2014) for sentence-internal pauses, NOT two ASCII hyphens.
    - The "Status: pending upstream merge" banner is mandatory until the slide.asm patch lands; this matches the UAT-10-01 idiom.
    - Use the project name "Beastty" (NOT "BestialiTTY") per MEMORY.md project_renamed_to_beastty.
  </action>
  <verify>
    <automated>test -f docs/SLIDE_Z80_REQUIREMENT.md && grep -qE 'ESC.\^.*SLIDE|wakeup' docs/SLIDE_Z80_REQUIREMENT.md && grep -q "ADR-003" docs/SLIDE_Z80_REQUIREMENT.md && grep -q "B:SLIDE R" docs/SLIDE_Z80_REQUIREMENT.md && grep -q "github.com/blowback/slide" docs/SLIDE_Z80_REQUIREMENT.md && grep -q "pending upstream merge" docs/SLIDE_Z80_REQUIREMENT.md</automated>
  </verify>
  <acceptance_criteria>
    - test -d docs succeeds (directory exists at repo root).
    - test -f docs/SLIDE_Z80_REQUIREMENT.md succeeds.
    - grep -qE 'ESC.\^.*SLIDE|wakeup' docs/SLIDE_Z80_REQUIREMENT.md succeeds (matches the 12-VALIDATION.md task 12-XX-18 smoke test).
    - grep -q "ADR-003" docs/SLIDE_Z80_REQUIREMENT.md succeeds (cross-reference present).
    - grep -q "B:SLIDE R" docs/SLIDE_Z80_REQUIREMENT.md succeeds (command convention documented).
    - grep -q "github.com/blowback/slide" docs/SLIDE_Z80_REQUIREMENT.md succeeds (repo-root link).
    - grep -q "pending upstream merge" docs/SLIDE_Z80_REQUIREMENT.md succeeds (Pitfall 7 banner).
    - grep -q "Beastty" docs/SLIDE_Z80_REQUIREMENT.md succeeds (project name correct per MEMORY.md).
    - Word "BestialiTTY" does NOT appear: `grep -c "BestialiTTY" docs/SLIDE_Z80_REQUIREMENT.md` returns `0`.
    - Word "TODO" does NOT appear: `grep -c "TODO" docs/SLIDE_Z80_REQUIREMENT.md` returns `0`.
    - Hardcoded PR number (e.g., `pull/123`, `/pull/`) does NOT appear: `grep -c "/pull/" docs/SLIDE_Z80_REQUIREMENT.md` returns `0`.
    - File length is between 50 and 200 lines (per CONTEXT brevity guidance): `[ $(wc -l < docs/SLIDE_Z80_REQUIREMENT.md) -ge 50 ] && [ $(wc -l < docs/SLIDE_Z80_REQUIREMENT.md) -le 200 ]`.
    - REQUIREMENTS.md SLIDE-40 row Pending → Complete: `sed -i 's/^- \[ \] \*\*SLIDE-40/- [x] **SLIDE-40/' .planning/REQUIREMENTS.md` AND `sed -i 's/| SLIDE-40 | Phase 12 | Pending |/| SLIDE-40 | Phase 12 | Complete |/' .planning/REQUIREMENTS.md`. Verify both flips: `grep -q "^- \[x\] \*\*SLIDE-40" .planning/REQUIREMENTS.md` AND `grep -qE "^\| SLIDE-40 \| Phase 12 \| Complete" .planning/REQUIREMENTS.md`.
  </acceptance_criteria>
  <done>
    docs/ directory exists at repo root; docs/SLIDE_Z80_REQUIREMENT.md created with all 4 locked sections + cross-link list + "pending upstream merge" banner; project name "Beastty" used consistently; no emojis, no exclamations, no hardcoded PR number; SLIDE-40 flipped Pending → Complete in REQUIREMENTS.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend README.md with File transfer section + Keyboard shortcuts table rows</name>
  <read_first>
    - README.md IN FULL (102 lines per RESEARCH — read all to understand structure + voice + locate the "Keyboard shortcuts" table + the "Can I run it locally?" section)
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-PATTERNS.md §"README.md (modify — docs)" — verbatim section text + table rows
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-CONTEXT.md §"Claude's Discretion" "README.md updates (SLIDE-41) — structure" — append-only default
  </read_first>
  <files>
    README.md
  </files>
  <behavior>
    - README.md gains 3 new rows in the existing "Keyboard shortcuts" table (or in a new sub-table if the existing one is locked to non-SLIDE shortcuts; locate the right section).
    - README.md gains a NEW "File transfer (SLIDE)" section appended BEFORE "Can I run it locally?" (or wherever the existing structure dictates per CONTEXT Claude's Discretion default).
    - The new section has three sub-sections: Sending files (PC → Z80), Receiving files (Z80 → PC), Cancelling.
    - The new section text references the Phase 12 + Phase 11 + Phase 10 behaviors that already shipped (drag-drop overlay, top-bar [↑ Send file] button, send modal with collision auto-rename, first-use confirm chip for non-default auto-send command, Settings → SLIDE file transfer sub-block, Esc-to-cancel).
    - The Keyboard shortcuts table extension covers: (a) drag-files-onto-canvas, (b) click-↑-Send-file, (c) Esc-during-SLIDE-transfer.
  </behavior>
  <action>
    Step 1 — Read README.md in full and locate the modification points:
    - The existing "Keyboard shortcuts" section header (CONTEXT estimates ~line 37-43; verify by reading the file).
    - The existing "UI / clipboard" or similar table within the Keyboard shortcuts section.
    - The "Can I run it locally?" section (CONTEXT estimates ~line 88).

    Step 2 — Extend the Keyboard shortcuts table with 3 new rows.

    Use the existing table format verbatim (likely a Markdown pipe table with two columns "Shortcut" / "Action"). Add the rows AT THE END of the existing table (before any blank line that separates the table from the next paragraph). Per 12-PATTERNS.md §"Phase 12 SLIDE-41 keyboard-shortcut extensions":

        | Drag files onto canvas        | Open SLIDE send modal for the dropped files |
        | Click ↑ Send file (top bar)   | Open file picker for SLIDE send             |
        | Esc (during SLIDE transfer)   | Cancel the in-flight SLIDE send or receive  |

    If the existing table has a different column structure or header text, ADAPT the new rows to match — the goal is consistency with the surrounding format, not literal column-text matching.

    Step 3 — Append the new "File transfer (SLIDE)" section BEFORE the "Can I run it locally?" section (or before whichever section CONTEXT default identifies as the natural insertion point).

    Per 12-PATTERNS.md §"Phase 12 SLIDE-41 new 'File transfer' section" — adapt to README voice but preserve the structural shape:

        ## File transfer (SLIDE)

        Beastty supports the SLIDE protocol for sending and receiving files between
        your computer and the MicroBeast.

        ### Sending files (PC → Z80)

        Drag files onto the terminal area, or click the `↑ Send file` button in the
        top bar. The send modal previews each filename rewritten to CP/M 8.3 form,
        and lets you confirm or cancel the batch before the transfer starts.

        If two or more files would collide on the Z80 side after 8.3 truncation
        (case-insensitive), the modal shows a per-collision-group preview of the
        auto-rename scheme (`REPORT.TXT, REPORT~1.TXT, REPORT~2.TXT, ...`) and offers
        three resolutions: `Send N renamed`, `Send only first`, or `Refuse batch`.

        By default Beastty auto-types `B:SLIDE R\r` at the Z80 prompt before the
        transfer to put the Z80 into receive mode. The auto-send command is
        configurable in Settings → SLIDE file transfer; the first time you change it,
        a chip prompts you to confirm the new value.

        ### Receiving files (Z80 → PC)

        When the Z80 sends a file via `B:SLIDE S FILE.TXT`, Beastty auto-detects the
        SLIDE wakeup signature (`ESC ^ S L I D E`) and downloads each file via your
        browser's Downloads tray. Settings → SLIDE file transfer lets you optionally
        save received files to a chosen folder instead.

        ### Cancelling

        Press `Esc`, or click `[Cancel]` on the floating SLIDE chip, to abort an
        in-flight send or receive. The wire returns to a clean CP/M prompt.

    Constraints:
    - Use the existing README voice and Markdown style (level-2 headers `##`, level-3 `###`, no emojis, no exclamations).
    - Use em-dash `—` (U+2014) where appropriate.
    - Do NOT add screenshots in this task (CONTEXT default: "text-only unless one image clarifies").
    - Use the project name "Beastty" — match the spelling used in the surrounding README text. If the existing README still uses "BestialiTTY" in the title or description, the new section ALSO uses "Beastty" (per MEMORY.md project_renamed_to_beastty — new content uses the new name; existing content stays).
    - Reference the SLIDE-12 / SLIDE-36 / SLIDE-38 shipped behaviors verbatim from the prose above. DO NOT promise behavior that is not yet shipped (e.g., "preset dropdown" — Phase 12 does NOT ship that, per CONTEXT §"Out of scope" P2 differentiators).
    - The "File transfer (SLIDE)" section MUST be appended IN PLACE — DO NOT restructure existing sections.
  </action>
  <verify>
    <automated>grep -q "^## File transfer" README.md && grep -q "^### Sending files" README.md && grep -q "^### Receiving files" README.md && grep -q "^### Cancelling" README.md && grep -q "Drag files onto canvas\|Drag.*canvas\|drag.*canvas" README.md && grep -q "↑ Send file" README.md && grep -q "Active SLIDE transfer\|SLIDE transfer\|during SLIDE" README.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "^## File transfer" README.md succeeds (new section header present).
    - grep -q "^### Sending files" README.md succeeds.
    - grep -q "^### Receiving files" README.md succeeds.
    - grep -q "^### Cancelling" README.md succeeds.
    - grep -q "REPORT.TXT, REPORT~1.TXT, REPORT~2.TXT" README.md succeeds (collision modal example present).
    - grep -q "B:SLIDE R" README.md succeeds (auto-send command documented).
    - grep -q "ESC.*SLIDE\|wakeup" README.md succeeds (wakeup signature documented in receive section).
    - grep -q "Drag files onto canvas\|drag.*canvas\|Drag.*canvas" README.md succeeds (Keyboard shortcuts row 1 — matches 12-VALIDATION.md task 12-XX-19/20 smoke test).
    - grep -q "↑ Send file\|↑ Send" README.md succeeds (Keyboard shortcuts row 2).
    - grep -qE "during SLIDE|Esc.*SLIDE|SLIDE transfer" README.md succeeds (Keyboard shortcuts row 3 — matches 12-VALIDATION.md task 12-XX-20 smoke test for "Active SLIDE transfer" or equivalent).
    - REQUIREMENTS.md SLIDE-41 row Pending → Complete: `sed -i 's/^- \[ \] \*\*SLIDE-41/- [x] **SLIDE-41/' .planning/REQUIREMENTS.md` AND `sed -i 's/| SLIDE-41 | Phase 12 | Pending |/| SLIDE-41 | Phase 12 | Complete |/' .planning/REQUIREMENTS.md`. Verify: `grep -q "^- \[x\] \*\*SLIDE-41" .planning/REQUIREMENTS.md` AND `grep -qE "^\| SLIDE-41 \| Phase 12 \| Complete" .planning/REQUIREMENTS.md`.
    - The README.md change is APPEND-ONLY for the new section (existing line N..M before the insertion point must be byte-identical post-edit). Verify via `git diff README.md` and confirm the diff shows only additions in the relevant ranges (the keyboard-shortcuts table extension also adds rows to the existing table — both inserts are append-style).
    - No new "TODO" / "FIXME" markers: `grep -cE "TODO|FIXME" README.md | awk '{exit ($1 == 0) ? 0 : 1}'`.
    - cd www && npm run test:fast exits 0 (no Phase 4-11 regressions; markdown-only changes do not affect runtime tests but this is a sanity gate).
  </acceptance_criteria>
  <done>
    README.md has new "File transfer (SLIDE)" level-2 section with three level-3 sub-sections (Sending / Receiving / Cancelling); Keyboard shortcuts table extended with 3 new rows referencing drag-drop / file-picker / Esc-cancel; SLIDE-41 flipped Pending → Complete in REQUIREMENTS.md; no emojis or exclamations; voice and Markdown style match the existing README.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc author → Doc readers | Information flow only; no code, no DOM, no runtime |
| Cross-references → External URLs | github.com/blowback/slide is an external dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-doc-01 | Information Disclosure | Hardcoded PR # in SLIDE-40 doc creates dead link if PR is closed/replaced | mitigate | Per Pitfall 7 — link to `https://github.com/blowback/slide` repo root only; use prose ("see open PRs for the v0.2.1 amendment") instead of a PR number. The "Status: pending upstream merge" banner makes the dependency explicit until the patch lands. |
| T-12-doc-02 | Information Disclosure | README claims behavior that has not shipped | mitigate | Plan 12-04 depends on Plans 12-01..12-03 — README content describes shipped behaviors only (drag-drop, collision modal, first-use confirm chip, Settings sub-block, Esc-cancel). Out-of-scope P2 differentiators (preset dropdown, ETA, etc.) are NOT mentioned (per CONTEXT §"Out of scope"). |
| T-12-doc-03 | Repudiation | Doc fails to cite ADR-003 + upstream sources | accept | Cross-link section enumerates ADR-003, SPEC-v0.2.md, upstream repo. Beastty's project archive (.planning/) preserves history regardless. |
</threat_model>

<verification>
- test -d docs → exists
- test -f docs/SLIDE_Z80_REQUIREMENT.md → exists
- grep validation per acceptance criteria above
- grep -qE "^- \[x\] \*\*(SLIDE-40|SLIDE-41)" .planning/REQUIREMENTS.md → 2 lines flipped
- grep -qE "^\| (SLIDE-40|SLIDE-41) \| Phase 12 \| Complete" .planning/REQUIREMENTS.md → 2 traceability rows flipped
- cd www && npm run test:fast → no regressions (sanity)
</verification>

<success_criteria>
- docs/SLIDE_Z80_REQUIREMENT.md exists with 4 locked sections + cross-link + "Status: pending upstream merge" banner.
- README.md has the new "File transfer (SLIDE)" section with 3 sub-sections + 3 new keyboard-shortcut rows.
- SLIDE-40 + SLIDE-41 flipped Pending → Complete in REQUIREMENTS.md.
- Project naming convention honored: new files use "Beastty" (not "BestialiTTY").
- No hardcoded PR numbers.
- No emojis, no exclamation marks.
- Phase 4-11 test:fast baseline preserved (sanity — markdown-only).
</success_criteria>

<output>
After completion, create `.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-04-SUMMARY.md` documenting:
- docs/SLIDE_Z80_REQUIREMENT.md final line count + section headers.
- README.md insertion line ranges (Keyboard shortcuts table + new File transfer section).
- REQUIREMENTS.md SLIDE-40 + SLIDE-41 flips.
- Any deviations from the planned content (e.g., re-ordering of sections to match README voice) with Rule classification.
</output>

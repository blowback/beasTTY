---
phase: 12
plan: 05
type: execute
wave: 3
depends_on: [12-01, 12-02, 12-03]
files_modified:
  - docs/SLIDE-UAT.md
autonomous: true
requirements: [SLIDE-42]
must_haves:
  truths:
    - "docs/SLIDE-UAT.md exists at the repo root and mirrors the 10-HUMAN-UAT.md format verbatim (front-matter + Setup + per-test expected/steps/result + Summary + Sign-off)"
    - "The UAT covers exactly 4 test classes per CONTEXT specifics: UAT-12-01 multi-file send including binary .COM, UAT-12-02 multi-file recv including zero-byte file, UAT-12-03 cancel mid-send, UAT-12-04 cancel mid-recv with Z80 echo verified"
    - "UAT-12-04 inherits UAT-10-01's blocked-result idiom — it is marked 'result: blocked' until the upstream slide.asm patch lands (per CONTEXT default + Pitfall 7)"
    - "UAT-12-01..03 are also gated on the wakeup-signature patch landing on upstream slide.asm; the Setup section explicitly notes this dependency"
    - "Each test specifies expected behavior, steps, and result placeholder following the 10-HUMAN-UAT.md format"
  artifacts:
    - path: "docs/SLIDE-UAT.md"
      provides: "Real-hardware UAT scaffold with 4 tests against patched MicroBeast"
      min_lines: 100
  key_links:
    - from: "docs/SLIDE-UAT.md UAT-12-04"
      to: ".planning/phases/10-slide-receiver-cancellation/10-HUMAN-UAT.md UAT-10-01"
      via: "explicit reference + same blocked-result idiom"
      pattern: "blocked"
    - from: "docs/SLIDE-UAT.md Setup"
      to: "github.com/blowback/slide"
      via: "explicit dependency note in the Setup section"
      pattern: "github.com/blowback/slide"
---

<objective>
Close SLIDE-42 — the real-hardware UAT for the v1.1 SLIDE milestone. Per CONTEXT Claude's Discretion (locked) and 12-UI-SPEC.md §"Documentation deliverables (NO UI surface)" + 12-PATTERNS.md §"docs/SLIDE-UAT.md (create — markdown UAT template)":

Create `docs/SLIDE-UAT.md` mirroring the `.planning/phases/10-slide-receiver-cancellation/10-HUMAN-UAT.md` format verbatim (front-matter + Setup + per-test expected/steps/result format + Summary + Sign-off). Cover exactly 4 tests:

- UAT-12-01: Multi-file send including binary `.COM`
- UAT-12-02: Multi-file recv including zero-byte file
- UAT-12-03: Cancel mid-send (PC-initiated)
- UAT-12-04: Cancel mid-recv with Z80 echo verified

Purpose: Closes the v1.1 milestone with a real-hardware acceptance gate. UAT-12-04 inherits the UAT-10-01 blocked-result idiom because the slide.asm CTRL_CAN echo patch is not yet upstream (verified — `grep -qE "ESC.*SLIDE|wakeup|0x18.*echo" /home/ant/src/microbeast/SLIDE/slide.asm` returns no matches). UAT-12-01..03 are also dependent on the upstream patch (the wakeup signature is required to enter SLIDE recv mode); the Setup section explicitly notes this.

Output:
- New file `docs/SLIDE-UAT.md` (~100-150 lines per Pitfall 8 brevity bias).

Depends on Plans 12-01..12-03: UAT references the SLIDE-12 / SLIDE-36 / SLIDE-38 shipped behaviors. The UAT also depends on Plan 12-04 if Task 1 of 12-04 (`docs/` directory creation) hasn't run — but since 12-04 + 12-05 both depend on 12-01..12-03, executor will likely sequence 12-04 before 12-05; if 12-05 runs first, it MUST `mkdir -p docs` defensively.
</objective>

<execution_context>
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/workflows/execute-plan.md
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-CONTEXT.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-RESEARCH.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-PATTERNS.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-VALIDATION.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-01-SUMMARY.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-02-SUMMARY.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-03-SUMMARY.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-04-SUMMARY.md

@.planning/phases/10-slide-receiver-cancellation/10-HUMAN-UAT.md
@.planning/phases/06-daily-driver-polish-session-deployment/06-HUMAN-UAT.md
@.planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md

<interfaces>
Target file location (NEW):
    /home/ant/src/microbeast/bestialitty/docs/SLIDE-UAT.md

Template source (read in full):
    /home/ant/src/microbeast/bestialitty/.planning/phases/10-slide-receiver-cancellation/10-HUMAN-UAT.md (170 lines)

Mirror verbatim:
- YAML front-matter shape (`status: pending`, `phase`, `source: [...]`, `started:`, `updated:`)
- Per-test header format: `### UAT-{phase}-{NN}: {description} ({REQ-ID})`
- Per-test body: `**expected:** ...` / `**steps:** ...` / `**result:** ...`
- Setup section (adapt — Phase 12 requires explicit MicroBeast hardware connection + patched slide.com)
- Summary section (`total: 4` / `passed:` / `issues:` / etc.)
- Sign-off section (Tester / Date / Pass count / Notes)

Locked test count: exactly 4 (Pitfall 8 — UAT scope creep).

Locked test name pattern (matches 12-VALIDATION.md task 12-XX-21 smoke test):
    grep -c '^### UAT-12-' docs/SLIDE-UAT.md → 4

Project naming convention (per MEMORY.md):
- USE "Beastty" (NOT "BestialiTTY") in all NEW content.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/SLIDE-UAT.md mirroring 10-HUMAN-UAT.md format</name>
  <read_first>
    - .planning/phases/10-slide-receiver-cancellation/10-HUMAN-UAT.md IN FULL — Phase 12 SLIDE-42 mirrors this format verbatim (front-matter + Setup + 6 tests + Summary + Sign-off + Gaps)
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-CONTEXT.md §"Specifics" §"`docs/SLIDE-UAT.md` test outline" — locked 4 tests
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-RESEARCH.md §"Section: SLIDE-42 — Real-Hardware UAT" + §Pitfall 8 — scope creep prevention; lock to 4 tests
    - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-PATTERNS.md §"docs/SLIDE-UAT.md (create — markdown UAT template)" — verbatim front-matter + Setup adaptations + per-test pattern
    - .planning/decisions/ADR-003-slide-v0-2-1-can-amendment.md — UAT-12-04 cites this ADR + the blocked-result rationale
  </read_first>
  <files>
    docs/SLIDE-UAT.md
  </files>
  <behavior>
    - The docs/ directory exists (created by Plan 12-04 OR `mkdir -p docs` defensively in this task — both OK).
    - docs/SLIDE-UAT.md mirrors the 10-HUMAN-UAT.md format verbatim:
      - YAML front-matter (status: pending, phase, source [12-CONTEXT, 12-RESEARCH, 12-UI-SPEC], started/updated dates)
      - Title heading: `# SLIDE — Real-hardware UAT (Phase 12 SLIDE-42)`
      - Setup section: bench setup, hardware connection requirement, patched slide.com requirement, browser state, dev-server invocation
      - 4 per-test sections: UAT-12-01..04
      - Summary section with `total: 4`, `passed:`, `issues:`, `pending:`, `skipped:`, `blocked:` lines
      - Sign-off section: Tester, Date, Pass count, Notes placeholders
      - (Optional) Gaps section if any partial-completion observations
    - All 4 tests have explicit expected/steps/result fields.
    - UAT-12-04 starts with `result: blocked` per CONTEXT default + UAT-10-01 inheritance.
    - UAT-12-01..03 start with `result: TBD` or `result: pending` placeholders (not blocked, but flagged in Setup as upstream-patch-dependent).
    - Total length is ~100-150 lines (Pitfall 8 brevity).
  </behavior>
  <action>
    Step 1 — Defensive directory creation (in case Plan 12-04 has not run yet):

        mkdir -p /home/ant/src/microbeast/bestialitty/docs

    Step 2 — Write docs/SLIDE-UAT.md.

    Use the verbatim front-matter + Setup + per-test pattern from 10-HUMAN-UAT.md. The full skeleton:

        ---
        status: pending
        phase: 12-slide-ux-polish-docs-real-hardware-uat
        source: [12-CONTEXT.md, 12-RESEARCH.md, 12-UI-SPEC.md]
        started: 2026-05-08
        updated: 2026-05-08
        ---

        # SLIDE — Real-hardware UAT (Phase 12 SLIDE-42)

        > End-to-end verification of the v1.1 SLIDE FileTransfer milestone against
        > a real MicroBeast Z80 with patched slide.com. Mirrors the 10-HUMAN-UAT.md
        > format. All four tests are gated on the upstream `github.com/blowback/slide`
        > PR landing (see Setup below); UAT-12-04 inherits the UAT-10-01 blocked-result
        > idiom for the CTRL_CAN echo path until the patched slide.asm ships.

        ## Setup

        - Real MicroBeast hardware connected over USB serial (19200 8N1, no flow control —
          MicroBeast preset).
        - **Patched `slide.com` from `github.com/blowback/slide`** (post-PR build that emits
          the `ESC ^ S L I D E` wakeup signature and echoes `CTRL_CAN`). Pre-PR slide.com
          will fail UAT-12-01..04 because the wakeup detection never fires; for legacy
          slide.com testing use Beastty's Settings → SLIDE file transfer → Compatibility
          mode → "force-start" path (covered by 06-HUMAN-UAT.md daily-driver tests).
        - Fresh Chromium tab; localhost dev server running (`scripts/dev.sh` or equivalent).
        - DevTools open; clear console.
        - Beastty Settings:
          - Auto-send command: default `B:SLIDE R\r` (or whichever value is set per
            user preference — see SLIDE-38 first-use-confirm chip if non-default)
          - Save received files to folder: at user discretion (`showDirectoryPicker`
            opt-in; tested separately in 10-HUMAN-UAT.md UAT-10-02).

        ## UAT-12-01: Multi-file send including binary .COM (SLIDE-07, SLIDE-13, SLIDE-15, SLIDE-16, SLIDE-36)

        **expected:** Beastty sends a 3-file batch (one binary `.COM`, one text `.TXT`,
        one with a name that triggers the SLIDE-36 collision modal) to the MicroBeast
        Z80. The auto-typed `B:SLIDE R\r` reaches the CP/M prompt, the wakeup signature
        is detected, the SLIDE chip transitions through awaiting-wakeup → active → sent-summary,
        and all three files land on the Z80's drive `B:` with byte-identical content
        (verified via `B:DIR` listing + a CP/M `TYPE` of the text file). The collision
        modal correctly auto-renames the duplicate to `~1.TXT`.

        **steps:**
        1. Connect to MicroBeast at 19200 8N1.
        2. Drag three files onto the Beastty terminal area:
           - `HELLO.COM` (a small CP/M `.COM` binary, e.g. a known-good HELLO test program)
           - `README.TXT` (any plain ASCII file)
           - `readme.txt` (a different file with a colliding 8.3 name)
        3. Confirm the send modal appears showing the rewrite preview AND the SLIDE-36
           collision row (`• README.TXT \n  ↳ README.TXT, README~1.TXT`).
        4. Click `[Send 3 renamed]` (default focus).
        5. Watch the SLIDE chip lifecycle: `awaiting-wakeup` → `active` → `sent-summary`.
        6. After completion, on the Z80: type `B:DIR` and verify the three files appear:
           `HELLO.COM`, `README.TXT`, `README~1.TXT`.
        7. Run `B:TYPE README.TXT` and verify content matches the source file.
        8. Run `B:HELLO.COM` (if the binary is executable) — confirm it runs without
           CP/M complaints.

        **result:** TBD (pending Z80 PR for ESC^SLIDE wakeup; see Setup blocker rationale)

        ## UAT-12-02: Multi-file recv including zero-byte file (SLIDE-18, SLIDE-19, SLIDE-21, SLIDE-22, SLIDE-23, SLIDE-24)

        **expected:** Z80 sends a 3-file batch including (a) a zero-byte file, (b) a
        sub-frame file (< 1024 bytes), (c) a 1 MB+ binary file. Beastty's receiver
        chip transitions through awaiting-wakeup → active (showing per-file progress
        with throughput on the 2 s sliding window) → received-summary. All three files
        land in the browser's Downloads tray (or chosen folder if the FSAP toggle is on)
        with byte-identical content verified via SHA-256 or `cmp`.

        **steps:**
        1. On the MicroBeast, prepare three test files on drive `B:`:
           - `EMPTY.TXT` (zero bytes)
           - `SHORT.TXT` (~100 bytes)
           - `BIG.BIN` (~1 MB random binary; can be generated via `B:DUMP` to a fixed seed)
        2. From the MicroBeast prompt, run `B:SLIDE S EMPTY.TXT SHORT.TXT BIG.BIN`.
        3. Watch the SLIDE chip lifecycle on Beastty: `awaiting-wakeup` → `active`
           (file count `1/3`, `2/3`, `3/3`; throughput shows `—` for first 2 s then KB/s).
        4. After completion, verify the chip transitions to `received-summary` for 5 s.
        5. Open the browser Downloads tray (or chosen folder); verify three files
           landed with the correct names (uppercase 8.3 form preserved per SLIDE-20).
        6. Compare each downloaded file's SHA-256 against the source on the MicroBeast
           (use `B:CRC` or another known-good hash tool, or recompute on the source PC).
           All three SHA-256 values must match.

        **result:** TBD (pending Z80 PR for ESC^SLIDE wakeup; see Setup blocker rationale)

        ## UAT-12-03: Cancel mid-send (PC-initiated) (SLIDE-27, SLIDE-30)

        **expected:** Beastty initiates a send of a multi-MB file; partway through
        (after at least one full file has shipped successfully), the user presses Esc.
        Beastty emits `CTRL_CAN`, settles in-flight writes (≤ 200 ms), waits for Z80
        echo (≤ 500 ms), drains for 100 ms, re-arms framer. The chip shows
        `cancelled-summary` for 5 s. The wire returns to a clean CP/M prompt without
        requiring a Z80 reset; a follow-up `B:DIR` runs cleanly.

        **steps:**
        1. Drag a 5 MB binary file onto Beastty.
        2. Click `[Send 1 file]`.
        3. After the chip enters `active` state and progress reaches ~30%, press Esc.
        4. Verify the chip transitions to `cancelled-summary` for 5 s.
        5. Verify the SLIDE chip eventually hides; the canvas returns to terminal mode.
        6. On the Z80, type `B:DIR` and verify the prompt responds normally
           (no hang, no echo of stale data).
        7. Verify the partial file was NOT fully written: `B:DIR` shows either no
           file or a 0-byte placeholder.

        **result:** TBD (pending Z80 PR for ESC^SLIDE wakeup; see Setup blocker rationale)

        ## UAT-12-04: Cancel mid-recv with Z80 echo verified (SLIDE-27, SLIDE-29, SLIDE-30, ADR-003)

        **expected:** Z80 starts sending a multi-MB file (`B:SLIDE S BIG.BIN`); partway
        through, the user presses Esc on the host. Beastty emits `CTRL_CAN` (raw byte
        `0x18`); the patched slide.asm echoes `CTRL_CAN` back within 500 ms (per
        ADR-003 v0.2.1 amendment); both sides drain the wire and return to idle. The
        Beastty chip shows `cancelled-summary` for 5 s; the Z80 returns to the CP/M
        prompt without a hang. A follow-up `B:SLIDE S BIG.BIN` run succeeds without
        Z80 reset.

        **steps:**
        1. On the MicroBeast, run `B:SLIDE S BIG.BIN`.
        2. After the chip enters `active` state and progress reaches ~30%, press Esc on the host.
        3. Watch the chip lifecycle: `active` → `cancelled-summary` (5 s).
        4. Verify the Z80 returns to the CP/M prompt automatically (no hang).
        5. On the Z80, run `B:SLIDE S BIG.BIN` AGAIN.
        6. Verify the second run completes successfully (no Z80 reset needed,
           no wire desync).

        **result:** blocked (Z80 SLIDE.COM does not yet implement the v0.2.1 ADR-003
        ESC^SLIDE wakeup + CTRL_CAN echo amendment; PR to github.com/blowback/slide
        is the gate. Inherits the UAT-10-01 blocked-result idiom; re-run after the
        patched slide.asm lands.)

        ## Summary

        total: 4
        passed: 0
        issues: 0
        pending: 3
        skipped: 0
        blocked: 1

        ## Sign-off

        - Tester:
        - Date:
        - Pass count: 0/4
        - Notes:

        ## Gaps

        (None at scaffold time. Gaps section is filled by the human tester after
        running the UAT.)

    Constraints:
    - The 4 test header strings MUST start with `### UAT-12-` (literal — matches 12-VALIDATION.md task 12-XX-21 smoke test `grep -c '^### UAT-12-' docs/SLIDE-UAT.md` returns 4).
    - UAT-12-04 result line MUST contain `blocked` AND reference the upstream PR (matches the Pitfall 7 banner pattern).
    - DO NOT add a 5th test (Pitfall 8 — scope lock).
    - DO NOT use emojis.
    - DO NOT use exclamation marks.
    - Use em-dash `—` (U+2014) for sentence-internal pauses.
    - Project name is "Beastty" throughout (per MEMORY.md).
    - The phrase `pending Z80 PR` (or equivalent) MUST appear in UAT-12-01..03 result lines so future re-runs know what's gating those tests.

    Step 3 — Flip SLIDE-42 to Complete in REQUIREMENTS.md:

        sed -i 's/^- \[ \] \*\*SLIDE-42/- [x] **SLIDE-42/' .planning/REQUIREMENTS.md
        sed -i 's/| SLIDE-42 | Phase 12 | Pending |/| SLIDE-42 | Phase 12 | Complete |/' .planning/REQUIREMENTS.md

    Verify both flips landed:

        grep -q "^- \[x\] \*\*SLIDE-42" .planning/REQUIREMENTS.md
        grep -qE "^\| SLIDE-42 \| Phase 12 \| Complete" .planning/REQUIREMENTS.md
  </action>
  <verify>
    <automated>test -f docs/SLIDE-UAT.md && [ "$(grep -c '^### UAT-12-' docs/SLIDE-UAT.md)" = "4" ] && grep -q "blocked" docs/SLIDE-UAT.md && grep -q "github.com/blowback/slide" docs/SLIDE-UAT.md && grep -q "^status:" docs/SLIDE-UAT.md && grep -q "## Setup" docs/SLIDE-UAT.md && grep -q "## Summary" docs/SLIDE-UAT.md && grep -q "## Sign-off" docs/SLIDE-UAT.md && grep -q "^- \[x\] \*\*SLIDE-42" .planning/REQUIREMENTS.md && grep -qE "^\| SLIDE-42 \| Phase 12 \| Complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - test -d docs succeeds (directory exists; created in Plan 12-04 or defensively here).
    - test -f docs/SLIDE-UAT.md succeeds.
    - `grep -c '^### UAT-12-' docs/SLIDE-UAT.md` returns exactly `4` (matches 12-VALIDATION.md task 12-XX-21 smoke test).
    - grep -q "^status: pending" docs/SLIDE-UAT.md succeeds (front-matter present).
    - grep -q "## Setup" docs/SLIDE-UAT.md succeeds.
    - grep -q "## Summary" docs/SLIDE-UAT.md succeeds.
    - grep -q "## Sign-off" docs/SLIDE-UAT.md succeeds.
    - grep -q "blocked" docs/SLIDE-UAT.md succeeds (UAT-12-04 result line).
    - grep -q "github.com/blowback/slide" docs/SLIDE-UAT.md succeeds (upstream PR reference).
    - grep -q "ADR-003" docs/SLIDE-UAT.md succeeds (UAT-12-04 cites the protocol authority).
    - grep -q "Beastty" docs/SLIDE-UAT.md succeeds.
    - Word "BestialiTTY" does NOT appear: `grep -c "BestialiTTY" docs/SLIDE-UAT.md` returns `0`.
    - Word "TODO" does NOT appear in test bodies (note: it is acceptable in the Sign-off "Notes:" line if literal placeholder; verify): `grep -c "TODO" docs/SLIDE-UAT.md` returns `0`.
    - File length is between 100 and 200 lines: `[ $(wc -l < docs/SLIDE-UAT.md) -ge 100 ] && [ $(wc -l < docs/SLIDE-UAT.md) -le 200 ]`.
    - REQUIREMENTS.md SLIDE-42 row Pending → Complete: `grep -q "^- \[x\] \*\*SLIDE-42" .planning/REQUIREMENTS.md` AND `grep -qE "^\| SLIDE-42 \| Phase 12 \| Complete" .planning/REQUIREMENTS.md`.
    - cd www && npm run test:fast exits 0 (markdown-only change does not break anything; sanity gate).
  </acceptance_criteria>
  <done>
    docs/SLIDE-UAT.md created with 4 tests + Setup + Summary + Sign-off sections per 10-HUMAN-UAT.md format; UAT-12-04 starts with `result: blocked` referencing ADR-003 + upstream PR; UAT-12-01..03 mark `result: TBD` with explicit "pending Z80 PR" notes; SLIDE-42 flipped Pending → Complete in REQUIREMENTS.md; project name "Beastty" used throughout; no emojis, no exclamations; doc length 100-200 lines.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT author → human tester | Doc-only contract; tester relies on doc accuracy when running real-hardware tests |
| UAT-12-04 → upstream slide.asm | The blocked test depends on an external repo's PR landing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-uat-01 | Information Disclosure | UAT promises behavior that has not shipped | mitigate | Plan 12-05 depends on Plans 12-01..12-03 — UAT references SLIDE-12 / SLIDE-36 / SLIDE-38 shipped behaviors only; UAT-12-01 specifically references the SLIDE-36 collision modal which lands in Plan 12-02. UAT-12-04 explicitly carries the `blocked` status until the upstream slide.asm patch lands. |
| T-12-uat-02 | Repudiation | UAT-12-04 is run against an unpatched Z80 and falsely passes | mitigate | The `result: blocked` line is mandatory in scaffold; testers MUST flip it to `pass` only AFTER verifying the slide.asm CTRL_CAN echo is implemented. The Setup section explicitly identifies the patched slide.com requirement. The blocked-result idiom mirrors UAT-10-01 — testers familiar with that precedent know the convention. |
| T-12-uat-03 | Spoofing | A 5th test is added that drifts from the locked scope | mitigate | Pitfall 8 — scope lock at exactly 4 tests. The grep -c smoke test in acceptance_criteria pins `grep -c '^### UAT-12-' docs/SLIDE-UAT.md` to exactly `4`. |
</threat_model>

<verification>
- test -f docs/SLIDE-UAT.md → exists
- grep -c '^### UAT-12-' docs/SLIDE-UAT.md → 4
- grep validation per acceptance criteria above
- grep -q "^- \[x\] \*\*SLIDE-42" .planning/REQUIREMENTS.md → top-level checkbox flipped
- grep -qE "^\| SLIDE-42 \| Phase 12 \| Complete" .planning/REQUIREMENTS.md → traceability row flipped
- cd www && npm run test:fast → no regressions (sanity)
</verification>

<success_criteria>
- docs/SLIDE-UAT.md exists at repo root with exactly 4 tests in 10-HUMAN-UAT.md format.
- UAT-12-04 starts with `result: blocked` per the upstream-PR-pending idiom.
- UAT-12-01..03 mark `result: TBD` and explicitly note the upstream PR dependency in Setup.
- Project naming convention honored ("Beastty" throughout).
- No emojis, no exclamations, no hardcoded PR numbers.
- SLIDE-42 flipped Pending → Complete in REQUIREMENTS.md.
- Phase 4-11 test:fast baseline preserved (sanity — markdown-only).
</success_criteria>

<output>
After completion, create `.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-05-SUMMARY.md` documenting:
- docs/SLIDE-UAT.md final line count + test count + section headers.
- REQUIREMENTS.md SLIDE-42 flip.
- Confirmation that all 6 Phase 12 SLIDE-* requirement IDs (SLIDE-12, SLIDE-36, SLIDE-38, SLIDE-40, SLIDE-41, SLIDE-42) are now marked Complete in both top-level checkboxes and the traceability table — this plan closes the v1.1 milestone.
- Any deviations with Rule classification.
- Phase 12 closeout note: "Phase 12 ready for /gsd-verify-phase".
</output>

---
phase: 12-slide-ux-polish-docs-real-hardware-uat
plan: 09
type: execute
wave: 2
depends_on: [12-07, 12-08]
files_modified:
  - www/transport/slide.js
  - www/transport/serial.js
  - www/input/tx-sink.js
  - .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-DEBUG-INSTRUMENTATION.md
autonomous: true
gap_closure: true
requirements: []
must_haves:
  truths:
    - "After this plan, no occurrence of `slideDbg`, `slideDbgHex`, `SLIDE_DEBUG`, `serialDbg`, `SERIAL_DEBUG`, `txDbg`, `txDbgHex`, or `TX_DEBUG` exists anywhere in `www/` source."
    - "After this plan, no log line tagged `[slide-debug]`, `[serial-debug]`, or `[tx-debug]` is emitted from any code path, regardless of `localStorage.beastty.debug.slide` value."
    - "Plan 12-07's force-start fix and Plan 12-08's RTS-on-connect fix are still functional and pass all the Playwright tests they shipped — instrumentation removal is mechanical and must not regress production behaviour."
    - "`12-DEBUG-INSTRUMENTATION.md` is deleted from the phase directory; the file's existence was scoped to the diagnostic phase and its removal is documented in the SUMMARY of this plan."
    - "cargo test --workspace stays at 283/283 (zero Rust touched — Phase 12 invariant per CLAUDE.md)."
    - "`npm run test:fast --workers=1` baseline preserved post-cleanup."
  artifacts:
    - path: "www/transport/slide.js"
      provides: "All 49 occurrences of slideDbg/slideDbgHex/SLIDE_DEBUG removed: the gate const, the helper functions (slideDbg + slideDbgHex), and every call site. The surrounding comments referencing the diagnostic instrumentation are also removed."
    - path: "www/transport/serial.js"
      provides: "All 26 occurrences of serialDbg/SERIAL_DEBUG removed: the gate const, the helper function, and every call site (including the inbound-chunk inline block in runReadLoop and the 5-step teardown probes)."
    - path: "www/input/tx-sink.js"
      provides: "All 11 occurrences of txDbg/txDbgHex/TX_DEBUG removed: the gate const, the helpers, and every call site in pushTxBytes / writeSlideFrame / writeSlideFrameAwaitable / setWireOwner."
    - path: ".planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-DEBUG-INSTRUMENTATION.md"
      provides: "Deleted. The diagnostic-instrumentation reference doc is no longer relevant after cleanup."
  key_links:
    - from: "www/transport/slide.js post-cleanup"
      to: "the production code path that previously surrounded each slideDbg call"
      via: "every slideDbg(...) line removed cleanly without disturbing the production logic immediately above and below"
      pattern: "no occurrence of slideDbg in www/"
---

<objective>
Remove the diagnostic instrumentation added in commits a8fb03b (slide.js + serial.js initial Dbg helpers) and 4b8669e (tx-sink.js Dbg helpers + extended call-site coverage in slide.js / serial.js). These were temporary, opt-in (gated on `localStorage.beastty.debug.slide === '1'`), and added solely to disambiguate the three send-path symptoms documented in `12-HUMAN-UAT.md` Test 4. The diagnostic capture led to the slide-team finding that became Plan 12-08 (RTS-on-connect). After Plan 12-07 (force-start fix) and Plan 12-08 (RTS fix) verify on real hardware, this instrumentation is dead weight — gated to silent-by-default but still increases the JS source footprint and adds noise to future code reads.

Purpose: Honour the explicit "Removal" section in `12-DEBUG-INSTRUMENTATION.md`. Restore the JS modules to a clean post-Phase-11 shape plus the two minimal gap-closure code fixes from 12-07 and 12-08.

Output: Three modified JS files (slide.js, serial.js, tx-sink.js) + one deleted markdown reference file. Zero Rust changes. Zero new modules. Zero behavioural changes — the instrumentation is silent in production already, so removal cannot regress runtime behaviour.

Wave 2: This plan depends on 12-07 and 12-08 landing first. If a hardware re-test against patched slide.com after 12-08 surfaces a new issue, the diagnostic helpers are still useful — keep them through wave 1 verification, then strip in wave 2.
</objective>

<execution_context>
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/workflows/execute-plan.md
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-DEBUG-INSTRUMENTATION.md
@CLAUDE.md

<!-- Hard invariants for this plan -->
<!-- 1. NO Rust changes. cargo --workspace stays 283/283. -->
<!-- 2. NO behavioural change to production code paths. The instrumentation is gated on localStorage flag and silent by default; removing it cannot change observable behaviour. -->
<!-- 3. NO touching the Plan 12-07 or 12-08 production fixes (the slideChipRef.enterActive() call in case 'force-start', the four getPrefs()-based requestToSend reads in serial.js connect-time setSignals call sites, the new pref in prefs.js, the Settings checkbox in index.html / main.js). -->
<!-- 4. Project name is "Beastty" (not BestialiTTY). NO AI attribution in commit messages. -->

<grep-anchors>
<!-- Exact identifier inventory the executor must purge — extracted from the codebase. -->

In www/transport/slide.js (49 occurrences total):
  - The gate const `SLIDE_DEBUG = (() => { ... })();` block at lines ~80-84
  - The `function slideDbg(tag, payload)` helper at lines ~85-88
  - The `function slideDbgHex(bytes, max = 16)` helper at lines ~89-95
  - All `slideDbg(...)` call sites — currently 47 instances per `grep -c "slideDbg" www/transport/slide.js` (49 total = 1 gate + 1 fn + 1 fn + ~46 call sites; actual count may differ by a few)
  - All `slideDbgHex(...)` call sites
  - All comment lines referencing `// Diagnostic instrumentation gate` / `// Remove this block` / `// Phase 12 hardware UAT root-cause work` that surround the helpers

In www/transport/serial.js (26 occurrences total):
  - The gate const `SERIAL_DEBUG` block at lines ~32-35
  - The `function serialDbg` helper at lines ~36-39
  - All `serialDbg(...)` call sites
  - Surrounding comments

In www/input/tx-sink.js (11 occurrences total):
  - The gate const `TX_DEBUG` block at lines ~47-50
  - The `function txDbg` helper at lines ~51-54
  - The `function txDbgHex` helper at lines ~55-58 (or wherever — read the file)
  - All `txDbg(...)` and `txDbgHex(...)` call sites
  - Surrounding comments
</grep-anchors>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip slideDbg / slideDbgHex / SLIDE_DEBUG from www/transport/slide.js</name>
  <files>www/transport/slide.js</files>
  <action>
    Open `www/transport/slide.js`.

    Step 1 — Remove the helper block (lines ~77-95 region). Specifically remove:
    - The 4-line comment header above the gate const, anchored on text "Diagnostic instrumentation gate — Phase 12 hardware UAT root-cause work" and ending at "Remove this block + all slideDbg() call sites once the diagnosis lands."
    - The `const SLIDE_DEBUG = (() => { ... })();` block.
    - The `function slideDbg(tag, payload) { ... }` block.
    - The `function slideDbgHex(bytes, max = 16) { ... }` block.

    The line BEFORE the comment header (currently line ~76, which discusses the W3 multi-data-frames-per-chunk assumption) and the line AFTER the slideDbgHex closing brace (currently line ~96, which is `\nexport const EVT_NONE = 0;` or similar) MUST remain — they are pre-existing production code. Verify by reading the surrounding 5 lines on each side BEFORE editing.

    Step 2 — Remove every `slideDbg(...)` and `slideDbgHex(...)` call site in the file. Use `grep -n "slideDbg" www/transport/slide.js` to enumerate; expected count post-grep is ~47 individual call lines. Each call site is currently a single statement on its own line, often immediately above or below the production statement it instruments. Remove the entire line.

    Edge cases the executor must handle:
    - Some call sites span multiple lines (e.g. `slideDbg('foo', { ... multi-line object ... })`). Remove ALL lines of the multi-line call, not just the first.
    - Some call sites are inside the `case 'retry'` / `case 'force-start'` / `case 'cancel'` branches. The Plan 12-07 fix added a chip enterActive call inside `case 'force-start'`; that code is NOT instrumentation and MUST be preserved. Read the surrounding 6 lines on each side to confirm you are removing only the slideDbg statement.
    - The line `slideDbg('handleChipInlineAction:enter', { action, pendingSendSessionIsNull: pendingSendSession === null, mode });` at the top of `handleChipInlineAction` is pure instrumentation — remove it.
    - The line `slideDbg('dispatchTerminalMode:wakeup-match', { hasPendingSend: pendingSendSession !== null });` near the wakeup-completion clause is pure instrumentation — remove it.

    Step 3 — Remove any orphaned blank lines created by the strips. If two adjacent removals leave a triple-blank-line gap, collapse to a single blank line. Two consecutive blank lines is acceptable (often present pre-existing); three+ should be collapsed.

    Step 4 — Sanity grep to confirm zero residue:

        cd www && grep -n "slideDbg\|SLIDE_DEBUG\|slideDbgHex" transport/slide.js
        # Expected: zero output.

    Step 5 — Run the full test:fast at --workers=1 to confirm no regression:

        cd www && npm run test:fast -- --workers=1

    All Phase 7-12 spec files must remain green (matching the pre-strip baseline). The Plan 12-07 chip-lifecycle test in slide-compatibility.spec.js must still pass. cargo --workspace remains 283/283.

    Commit ONLY this file:

        git add www/transport/slide.js
        git commit -m "chore(12-09): remove slideDbg diagnostic instrumentation"
  </action>
  <verify>
    <automated>cd www && grep -c "slideDbg\|SLIDE_DEBUG\|slideDbgHex" transport/slide.js</automated>
    Expected: `0`.

    <automated>cd www && grep -c "\\[slide-debug\\]" transport/slide.js</automated>
    Expected: `0` (no log-tag string in source either).

    <automated>cd www && npx playwright test slide-compatibility.spec.js -g "Force start" --workers=1</automated>
    The Plan 12-07 force-start chip-lifecycle test still passes (instrumentation removal must not regress the production fix).
  </verify>
  <done>
    - Zero occurrences of `slideDbg`, `slideDbgHex`, `SLIDE_DEBUG`, or the log tag `[slide-debug]` in `www/transport/slide.js`.
    - The Plan 12-07 production fix (the `slideChipRef.enterActive()` call in `case 'force-start'`) is INTACT.
    - Single atomic commit on `www/transport/slide.js` only, message `chore(12-09): remove slideDbg diagnostic instrumentation`, no AI attribution.
  </done>
</task>

<task type="auto">
  <name>Task 2: Strip serialDbg / SERIAL_DEBUG from www/transport/serial.js</name>
  <files>www/transport/serial.js</files>
  <action>
    Open `www/transport/serial.js`.

    Step 1 — Remove the helper block (lines ~29-39 region). Specifically remove:
    - The 4-line comment header anchored on "Diagnostic instrumentation gate — Phase 12 hardware UAT root-cause work" through "Remove this block + serialDbg() call sites once the diagnosis lands."
    - The `const SERIAL_DEBUG = (() => { ... })();` block.
    - The `function serialDbg(tag, payload) { ... }` block.

    Verify the line before (the closing of the import block — `import { getPrefs } from '../state/prefs.js';` or similar) and the line after (the start of the production constants block — `// Constants -----` or `const VID_MICROBEAST = ...`) are preserved untouched.

    Step 2 — Remove every `serialDbg(...)` call site. Use `grep -n "serialDbg" www/transport/serial.js` to enumerate. The call sites cluster around:
    - The runReadLoop inbound-chunk inline block (chunk-arrival probes).
    - The `disconnect:enter` / `:awaiting-teardown` / `:teardown-resolved` / `:exit` probes (line ~429 etc).
    - The teardown 5-step probes (line ~545 region, capturing setSignals / reader.cancel / writer.releaseLock / port.close / pump-on-port-lost).

    Each call site is a single statement on its own line. Remove the entire line. For multi-line `serialDbg(tag, { ... multi-line ... })` calls, remove all lines of the call.

    Step 3 — Be careful around the teardown function (line ~538+). The Plan 12-08 production fix did NOT touch teardown; the `await port.setSignals({ dataTerminalReady: false, requestToSend: false });` line at ~546 is pre-existing close-time logic and MUST be preserved. The serialDbg lines wrapping it are instrumentation — remove only those.

    Step 4 — Remove any orphaned blank lines.

    Step 5 — Sanity grep:

        cd www && grep -n "serialDbg\|SERIAL_DEBUG" transport/serial.js
        # Expected: zero output.

    Step 6 — Run the full Phase 5 connect.spec.js + Phase 12 connect-related specs:

        cd www && npx playwright test connect.spec.js --workers=1

    Both the Plan 12-08 RTS-default test and the RTS-pref-override test must still pass. The Phase 5 D-04 retry / D-08 form persist tests must still pass.

    Commit ONLY this file:

        git add www/transport/serial.js
        git commit -m "chore(12-09): remove serialDbg diagnostic instrumentation"
  </action>
  <verify>
    <automated>cd www && grep -c "serialDbg\|SERIAL_DEBUG" transport/serial.js</automated>
    Expected: `0`.

    <automated>cd www && grep -c "\\[serial-debug\\]" transport/serial.js</automated>
    Expected: `0`.

    <automated>cd www && npx playwright test connect.spec.js --workers=1</automated>
    All connect.spec.js tests pass — including the two Plan 12-08 RTS tests.

    <automated>cd www && grep -n "requestToSend" transport/serial.js | wc -l</automated>
    Expected: ~6 (4 connect-time getPrefs-gated + 2 close-time literal-false). Confirms Plan 12-08 production logic is intact.
  </verify>
  <done>
    - Zero occurrences of `serialDbg`, `SERIAL_DEBUG`, or `[serial-debug]` in `www/transport/serial.js`.
    - The Plan 12-08 production fix (four getPrefs-gated `requestToSend` reads in connect-time setSignals call sites) is INTACT.
    - Plan 12-08's two close-time `requestToSend: false` literals are INTACT.
    - Single atomic commit on `www/transport/serial.js` only, message `chore(12-09): remove serialDbg diagnostic instrumentation`, no AI attribution.
  </done>
</task>

<task type="auto">
  <name>Task 3: Strip txDbg / txDbgHex / TX_DEBUG from www/input/tx-sink.js</name>
  <files>www/input/tx-sink.js</files>
  <action>
    Open `www/input/tx-sink.js`.

    Step 1 — Remove the helper block (lines ~44-58 region; verify exact lines via `grep -n "TX_DEBUG\|txDbg" www/input/tx-sink.js`). Specifically remove:
    - The 3-line comment header anchored on "Diagnostic instrumentation gate" / "Remove this block + txDbg() call sites once the diagnosis lands."
    - The `const TX_DEBUG = (() => { ... })();` block.
    - The `function txDbg(tag, payload) { ... }` block.
    - The `function txDbgHex(bytes, max = 16) { ... }` block.

    Step 2 — Remove every `txDbg(...)` and `txDbgHex(...)` call site. Use `grep -n "txDbg" www/input/tx-sink.js`. Per the count check, ~11 occurrences total (3 helper-block lines + ~8 call sites distributed across pushTxBytes, writeSlideFrame, writeSlideFrameAwaitable, setWireOwner).

    Each call site is a single statement on its own line. Remove the entire line.

    Step 3 — Remove orphaned blank lines.

    Step 4 — Sanity grep:

        cd www && grep -n "txDbg\|TX_DEBUG\|txDbgHex" input/tx-sink.js
        # Expected: zero output.

    Step 5 — Run the Phase 8 / 9 / 10 / 11 / 12 specs that exercise tx-sink:

        cd www && npx playwright test tx-sink.spec.js --workers=1
        cd www && npx playwright test slide-sender.spec.js --workers=1
        cd www && npx playwright test slide-recv.spec.js --workers=1

    All must pass.

    Commit ONLY this file:

        git add www/input/tx-sink.js
        git commit -m "chore(12-09): remove txDbg diagnostic instrumentation"
  </action>
  <verify>
    <automated>cd www && grep -c "txDbg\|TX_DEBUG\|txDbgHex" input/tx-sink.js</automated>
    Expected: `0`.

    <automated>cd www && grep -c "\\[tx-debug\\]" input/tx-sink.js</automated>
    Expected: `0`.

    <automated>cd www && npx playwright test tx-sink.spec.js --workers=1</automated>
    All tx-sink.spec.js tests pass.
  </verify>
  <done>
    - Zero occurrences of `txDbg`, `txDbgHex`, `TX_DEBUG`, or `[tx-debug]` in `www/input/tx-sink.js`.
    - tx-sink production behaviour (pushTxBytes, writeSlideFrame, writeSlideFrameAwaitable, setWireOwner) is unchanged.
    - Single atomic commit on `www/input/tx-sink.js` only, message `chore(12-09): remove txDbg diagnostic instrumentation`, no AI attribution.
  </done>
</task>

<task type="auto">
  <name>Task 4: Delete .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-DEBUG-INSTRUMENTATION.md</name>
  <files>.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-DEBUG-INSTRUMENTATION.md</files>
  <action>
    Delete the file via `git rm`:

        git rm .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-DEBUG-INSTRUMENTATION.md

    The file's existence was scoped to the diagnostic phase and explicitly noted in its own "Removal" section: "Once the gap-closure plans land and verify, delete this file." Plans 12-07 and 12-08 (this plan's wave-1 dependencies) shipped the gap closures.

    DO NOT delete or modify any other file in the phase directory. The `12-HUMAN-UAT.md` file references the diagnostic capture log via `capture_log:` in its Gap 2 frontmatter — that reference becomes a historical pointer once the file is gone, which is acceptable (the gap is closed by 12-08; the capture-log breadcrumb's value is exhausted).

    Commit ONLY this deletion:

        git commit -m "chore(12-09): delete diagnostic-instrumentation reference doc"
  </action>
  <verify>
    <automated>ls .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-DEBUG-INSTRUMENTATION.md 2>&1 | grep -c "No such file"</automated>
    Expected: `1` (file no longer exists).

    <automated>git log --oneline -1 -- .planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-DEBUG-INSTRUMENTATION.md | head -1</automated>
    Expected: most recent commit on the (now-deleted) path is the deletion commit message.
  </verify>
  <done>
    - File deleted via `git rm`.
    - Single atomic commit, message `chore(12-09): delete diagnostic-instrumentation reference doc`, no AI attribution.
    - No other phase-12 files modified or deleted.
  </done>
</task>

</tasks>

<verification>
Phase-wide post-commit verification (after all 4 tasks):

1. `cargo test --workspace` → 283/283 (Phase 12 zero-Rust invariant).
2. Whole-tree grep guards:
   - `cd www && grep -rn "slideDbg\\|SLIDE_DEBUG\\|slideDbgHex\\|serialDbg\\|SERIAL_DEBUG\\|txDbg\\|TX_DEBUG\\|txDbgHex"` → no matches.
   - `cd www && grep -rn "\\[slide-debug\\]\\|\\[serial-debug\\]\\|\\[tx-debug\\]"` → no matches.
3. `cd www && npm run test:fast -- --workers=1` baseline preserved.
4. `cd www && bash scripts/build.sh` exits 0.
5. The Plan 12-07 force-start chip-lifecycle test in slide-compatibility.spec.js passes.
6. The Plan 12-08 connect.spec.js RTS-default + RTS-pref-override tests pass.
7. `git log --oneline -4` shows four commits in order:
   - `chore(12-09): remove slideDbg diagnostic instrumentation`
   - `chore(12-09): remove serialDbg diagnostic instrumentation`
   - `chore(12-09): remove txDbg diagnostic instrumentation`
   - `chore(12-09): delete diagnostic-instrumentation reference doc`
   None of the commit messages contain AI attribution.
8. `git diff HEAD~4 HEAD --stat` shows exactly four entries: three modified .js files (line-count delta should be net-negative — expected ~80-150 lines removed across the three files) and one deleted .md file.
</verification>

<success_criteria>
- All four tasks committed atomically.
- Whole-tree grep for any of {slideDbg, SLIDE_DEBUG, slideDbgHex, serialDbg, SERIAL_DEBUG, txDbg, TX_DEBUG, txDbgHex, [slide-debug], [serial-debug], [tx-debug]} returns zero matches in `www/`.
- 12-DEBUG-INSTRUMENTATION.md is deleted.
- Plan 12-07 force-start fix and Plan 12-08 RTS-on-connect fix are still functional and pass their respective Playwright tests.
- Whole-crate cargo --workspace remains 283/283.
- `npm run test:fast --workers=1` baseline preserved.
- Net diff is line-removal only (no production logic touched, only instrumentation stripped).
</success_criteria>

<output>
After completion, create `.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-09-SUMMARY.md` per the SUMMARY template.

Diary entry should record:
- Total lines removed per file (helpful for the "net negative diff" reality check).
- Confirmation that no production logic was touched (only instrumentation lines and their surrounding scaffolding comments).
- Confirmation that Plan 12-07 and 12-08 production fixes survived the cleanup.
- Any pre-existing test flakes observed during full-suite runs (cite spec name + reference deferred-items.md per Phase 11/12 precedent).
- Confirmation that no Rust files were touched (cargo workspace unchanged at 283/283).
- A note in the SUMMARY that the Phase 12 hardware UAT gap-closure milestone is complete pending re-test on real MicroBeast (the user runs UAT-12-01 / UAT-12-02 against the patched slide.com on real hardware to verify end-to-end).
</output>

---
phase: 12-slide-ux-polish-docs-real-hardware-uat
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - www/transport/slide.js
  - www/tests/transport/slide-compatibility.spec.js
autonomous: true
gap_closure: true
requirements: [SLIDE-39]
must_haves:
  truths:
    - "After the user clicks [Force start] on the wakeup-required chip, the chip lifecycle transitions OUT of 'awaiting-timeout' (per slideChipRef.enterActive() — the session is now active, mirroring dispatchTerminalMode line 568's wakeup-completion-clause idiom)."
    - "After the click, slide.js's mode is 'send' AND slideChipRef.__getStateForTests().lifecycle is NOT 'awaiting-timeout' (it should be 'active' so the user gets visible UI feedback)."
    - "Playwright spec slide-compatibility.spec.js (currently asserting only mode === 'send') is extended to assert chip lifecycle transition, so the regression cannot recur."
    - "cargo test --workspace stays at 283/283 (no Rust touched — Phase 12 zero-Rust invariant preserved per CLAUDE.md)."
  artifacts:
    - path: "www/transport/slide.js"
      provides: "case 'force-start' in handleChipInlineAction now calls slideChipRef.enterActive() after enterSendModeInternal succeeds, matching the wakeup-completion-clause idiom in dispatchTerminalMode"
    - path: "www/tests/transport/slide-compatibility.spec.js"
      provides: "Extended [Force start] test asserting chip lifecycle transition (not just dispatcher mode); regression-guard against the silent UI regression diagnosed in 12-force-start-button-does-nothing.md"
  key_links:
    - from: "www/transport/slide.js handleChipInlineAction case 'force-start'"
      to: "slideChipRef.enterActive()"
      via: "direct method call after enterSendModeInternal(session) succeeds inside the existing try/catch block"
      pattern: "slideChipRef.enterActive\\(\\)"
---

<objective>
Close UAT Gap 1 (`12-HUMAN-UAT.md` Test 3 / Force-start "does nothing"): wire the missing chip-lifecycle update into `case 'force-start'` in `www/transport/slide.js:handleChipInlineAction`, and extend the matching Playwright test to pin the new contract.

Purpose: The force-start button was the documented escape hatch for hardware UAT against unpatched `slide.com` (Phase 11 D-15 / SLIDE-35). Diagnosis in `.planning/debug/12-force-start-button-does-nothing.md` confirmed the dispatcher correctly transitions to send mode and writes `CTRL_RDY` to the wire, but never updates the chip — leaving the user with zero visible feedback ("does nothing"). Without this fix, the user cannot run UAT-12-01 against legacy firmware AND cannot use force-start as a workaround for the Gap 2 RTS issue while it ships in Plan 12-08.

Output: Two modified files (one one-line code change in slide.js + one extended spec file). Zero Rust changes. Zero new modules. Zero new prefs. Atomic and self-contained.
</objective>

<execution_context>
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/workflows/execute-plan.md
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-HUMAN-UAT.md
@.planning/debug/12-force-start-button-does-nothing.md
@CLAUDE.md

<!-- Hard invariants for this plan -->
<!-- 1. NO Rust changes. Phase 12 is JS-only. cargo --workspace stays 283/283. -->
<!-- 2. NO new modules. NO new prefs. NO new commits beyond the two atomic ones below. -->
<!-- 3. Project name is "Beastty" (not BestialiTTY). NO AI attribution in commit messages. -->
<!-- 4. Diagnostic instrumentation (slideDbg/serialDbg/txDbg) is removed in Plan 12-09 — DO NOT remove it here. -->

<interfaces>
<!-- Existing slideChipRef API surface (extracted from slide.js head + slide-chip.js exports). -->
<!-- The executor uses these directly — no codebase exploration needed. -->

slideChipRef (injected via wireSlideDispatcher opts.slideChip; nullable):
  enterActive()           // Transition chip to 'active' lifecycle (the missing call).
  enterAwaitingWakeup({ armTimer })  // Transition chip to 'awaiting-wakeup' (used by case 'retry').
  enterFirstUseConfirm({ value, onConfirm, onReset })
  enterSummary({ ... })
  hide()                  // Used by case 'cancel' to dismiss the chip.
  __getStateForTests()    // Returns { lifecycle, ... } for Playwright introspection.
  onStateChange(observer) // Observer receives { kind: 'inline-action', action: 'retry'|'cancel'|'force-start' }.

The reference idiom that case 'force-start' is missing — see slide.js:611:

    if (slideChipRef && typeof slideChipRef.enterActive === 'function') {
        slideChipRef.enterActive();
    }

That block currently fires from dispatchTerminalMode's wakeup-completion clause (after a 7-byte ESC^SLIDE match). The fix replicates the same idiom in case 'force-start' so the chip transitions identically when the user manually skips the wakeup wait.
</interfaces>

<file-locations>
<!-- Exact line ranges to anchor edits. Read these first. -->

slide.js case 'force-start' (the bug):
  www/transport/slide.js:399-412 (the existing case 'force-start' branch, missing the enterActive call)

slide.js wakeup-completion enterActive idiom (the reference):
  www/transport/slide.js:609-613 (dispatchTerminalMode's existing call site that case 'force-start' must mirror)

slide-compatibility.spec.js test to extend:
  www/tests/transport/slide-compatibility.spec.js:269-287 (the existing [Force start] test that asserts mode='send' but not chip lifecycle)
</file-locations>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add chip enterActive() call to slide.js case 'force-start'</name>
  <files>www/transport/slide.js</files>
  <action>
    Open `www/transport/slide.js` and locate `handleChipInlineAction` `case 'force-start'` (currently lines ~399-412 — anchor on the comment "Skip wakeup wait; jump directly into send mode").

    The current shape is:

        case 'force-start':
            // Skip wakeup wait; jump directly into send mode (equivalent to
            // having Compatibility mode set to 'force-start' for this one
            // session — CONTEXT D-15 verbatim semantic). Consume the pending
            // session here so the wakeup-completion clause in
            // dispatchTerminalMode does not also fire.
            if (pendingSendSession) {
                const session = pendingSendSession;
                pendingSendSession = null;
                try { enterSendModeInternal(session); } catch (err) {
                    console.error('[slide.js] force-start (chip) enterSendModeInternal failed:', err);
                }
            }
            return;

    Modify it so that after `enterSendModeInternal(session)` succeeds (inside the existing try block), the chip lifecycle transitions to 'active'. Mirror the idiom at slide.js:609-613 (dispatchTerminalMode wakeup-completion). The change is INSIDE the existing `try` so a chip-method exception does not break the dispatcher; the chip call is itself wrapped in optional-chaining-style guard so a null `slideChipRef` (older boot paths / test harnesses) still works.

    Resulting shape:

        case 'force-start':
            // Skip wakeup wait; jump directly into send mode (equivalent to
            // having Compatibility mode set to 'force-start' for this one
            // session — CONTEXT D-15 verbatim semantic). Consume the pending
            // session here so the wakeup-completion clause in
            // dispatchTerminalMode does not also fire.
            //
            // Phase 12.1 Plan 12-07 — chip lifecycle update missing in original
            // Plan 11-04 implementation. Without this call the chip stayed
            // pinned at 'awaiting-timeout' AFTER the click, with zero visible
            // user feedback (gap diagnosed in
            // .planning/debug/12-force-start-button-does-nothing.md). Mirrors
            // the wakeup-completion enterActive() idiom in dispatchTerminalMode
            // (slide.js search "Phase 11 Plan 11-03 — chip lifecycle hook:
            // session active." for the reference call site). Wrapped in its own
            // try/catch so a chip-method exception does not break the
            // dispatcher; the existing outer try only guards
            // enterSendModeInternal.
            if (pendingSendSession) {
                const session = pendingSendSession;
                pendingSendSession = null;
                try {
                    enterSendModeInternal(session);
                    if (slideChipRef && typeof slideChipRef.enterActive === 'function') {
                        slideChipRef.enterActive();
                    }
                } catch (err) {
                    console.error('[slide.js] force-start (chip) enterSendModeInternal failed:', err);
                }
            }
            return;

    Notes:
    - DO NOT call `enterActive()` BEFORE `enterSendModeInternal()` — the chip's 'active' lifecycle implies the dispatcher is in send mode; flipping the chip first would briefly show 'active' on a still-terminal-mode dispatcher if `enterSendModeInternal` throws.
    - DO NOT remove `slideDbg('handleChipInlineAction:enter', ...)` at the top of `handleChipInlineAction`. That instrumentation is removed in Plan 12-09 cleanly across the file; do not partially strip it here.
    - DO NOT touch `case 'retry'` or `case 'cancel'`. They are not affected by this gap.

    After the edit, run `cd www && npm run lint 2>/dev/null || true` (project may not have a lint script — non-fatal). Then run the slide-compatibility spec subset (Task 2 below extends the test that proves the new behaviour).

    Commit ONLY this file:

        git add www/transport/slide.js
        git commit -m "fix(12-07): force-start now updates chip lifecycle to active"

    The commit message MUST NOT contain any AI attribution per project memory `feedback_commit_messages.md`.
  </action>
  <verify>
    <automated>cd www && npx playwright test slide-compatibility.spec.js -g "Force start" --workers=1</automated>
    Existing test at slide-compatibility.spec.js:269 still passes (the chip-lifecycle assertion lands in Task 2, but Task 1's edit must not regress the existing mode=='send' assertion).
  </verify>
  <done>
    - `www/transport/slide.js` `case 'force-start'` calls `slideChipRef.enterActive()` after a successful `enterSendModeInternal(session)`, guarded by `slideChipRef && typeof slideChipRef.enterActive === 'function'`.
    - Existing slide-compatibility "Force start" test still passes.
    - Single atomic commit on `www/transport/slide.js` only, message `fix(12-07): force-start now updates chip lifecycle to active`, no AI attribution.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend slide-compatibility.spec.js to assert chip lifecycle transition after [Force start]</name>
  <files>www/tests/transport/slide-compatibility.spec.js</files>
  <action>
    Open `www/tests/transport/slide-compatibility.spec.js`. Locate the existing test:

        test('[Force start] click jumps directly into send mode without waiting for wakeup', async ({ page }) => {

    at line ~269. The test currently asserts `window.__slide.__getStateForTests().mode` becomes `'send'` but is silent on chip lifecycle. Extend the test (in-place — same `test()` block, same `beforeEach`) to also assert that `window.__slideChip.__getStateForTests().lifecycle` transitions OUT of `'awaiting-timeout'` (specifically: becomes `'active'`) after the click.

    Add the new assertion BELOW the existing `expect.poll(...).toBe('send')`. Use the same `expect.poll` idiom with a 2000 ms timeout (Plan 11-05 precedent for chip lifecycle polls under parallel test load):

        await expect.poll(
            () => page.evaluate(() => window.__slideChip.__getStateForTests().lifecycle),
            { timeout: 2000 },
        ).toBe('active');

    DO NOT split the test into two — the existing test name `[Force start] click jumps directly into send mode without waiting for wakeup` is a 12-VALIDATION.md `-g` filter anchor (Plan 11-05 convention). Inline the new assertion. Add a comment above it:

        // Phase 12.1 Plan 12-07 — chip lifecycle MUST also transition out of
        // 'awaiting-timeout' so the user gets visible feedback on the click.
        // Regression guard for the gap diagnosed in
        // .planning/debug/12-force-start-button-does-nothing.md (force-start
        // case in slide.js previously omitted the enterActive() call).

    DO NOT touch any other test in the file. DO NOT add new test.describe blocks. DO NOT extend setup/commonReset.

    Run the spec twice in a row (parallelism flake check per Phase 11 deferred-items.md precedent):

        cd www && npx playwright test slide-compatibility.spec.js --workers=1
        cd www && npx playwright test slide-compatibility.spec.js --workers=1

    Both runs must pass. If the chip-lifecycle assertion times out, verify Task 1 was committed correctly (the enterActive call is the production fix that makes this assertion green). If the existing setup with `setWakeupDelay(20000)` somehow leaves the chip in a different lifecycle state, read the chip ref's `__getStateForTests` shape via console.log inside `page.evaluate` once for diagnosis — do NOT relax the assertion.

    Commit ONLY this file:

        git add www/tests/transport/slide-compatibility.spec.js
        git commit -m "test(12-07): assert chip lifecycle transitions to active after force-start"

    Commit message MUST NOT contain AI attribution.
  </action>
  <verify>
    <automated>cd www && npx playwright test slide-compatibility.spec.js -g "Force start" --workers=1</automated>
    The "[Force start]" test passes with both the original mode==='send' assertion AND the new lifecycle==='active' assertion.

    Phase-wide regression sanity:
    <automated>cargo test --workspace 2>&1 | tail -5</automated>
    Must show 283 tests passing (Phase 12 zero-Rust-changes invariant).

    Full Phase 11/12 Playwright fast suite still green at --workers=1:
    <automated>cd www && npm run test:fast -- --workers=1</automated>
    Pre-existing parallelism flakes documented in deferred-items.md may still flake at higher worker counts; --workers=1 must be deterministic.
  </verify>
  <done>
    - The "[Force start] click jumps directly into send mode without waiting for wakeup" test now contains a second assertion proving `window.__slideChip.__getStateForTests().lifecycle === 'active'` after the click.
    - Both assertions pass on two consecutive `--workers=1` runs.
    - cargo test --workspace remains at 283/283.
    - Single atomic commit on `slide-compatibility.spec.js` only, message `test(12-07): assert chip lifecycle transitions to active after force-start`, no AI attribution.
  </done>
</task>

</tasks>

<verification>
Phase-wide post-commit verification:

1. `cargo test --workspace` → 283/283 (Phase 12 zero-Rust invariant).
2. `cd www && npx playwright test slide-compatibility.spec.js --workers=1` (twice consecutive — parallelism flake guard).
3. `git log --oneline -2` shows two commits in order: `test(12-07): assert chip lifecycle transitions to active after force-start` then `fix(12-07): force-start now updates chip lifecycle to active`. Neither commit message contains "Co-Authored-By: Claude" or any AI attribution.
4. `git diff HEAD~2 HEAD --stat` shows exactly two files changed: `www/transport/slide.js` and `www/tests/transport/slide-compatibility.spec.js`. No other files touched.

Manual UAT-readiness sanity (not part of this plan's verification but should hold):
- The user can re-run UAT-12-01 against unpatched `slide.com`: drag-drop a file, wait for the awaiting-timeout chip, click [Force start], and observe the chip transition to 'active' lifecycle. The visual feedback gap is closed.
</verification>

<success_criteria>
- Both tasks committed atomically (separate commits) on the two listed files only.
- Existing slide-compatibility "Force start" test now contains both `mode==='send'` and `lifecycle==='active'` assertions; both green on `--workers=1`.
- Whole-crate cargo --workspace remains at 283/283 (zero Rust changes).
- Phase 11 / 12 Playwright fast suite passes at `--workers=1`; pre-existing 10-worker parallelism flakes documented in deferred-items.md are unchanged (out of scope).
- Diagnostic instrumentation (slideDbg, slideDbgHex, SLIDE_DEBUG, slideDbg call sites) is UNCHANGED in this plan — Plan 12-09 removes it.
</success_criteria>

<output>
After completion, create `.planning/phases/12-slide-ux-polish-docs-real-hardware-uat/12-07-SUMMARY.md` per the SUMMARY template.

Diary entry should record:
- The exact line of code added in slide.js (the `enterActive()` guarded call).
- The exact assertion added in slide-compatibility.spec.js.
- Whether `enterSendModeInternal` was placed inside the same try/catch as `enterActive` or split (the plan specifies same try; record what was done and why).
- Any deviations (Rule 1 / 3) — should be zero for a 2-task code+test plan, but document if encountered.
- Confirmation that no diagnostic-instrumentation cleanup happened in this plan (Plan 12-09 owns that).
</output>

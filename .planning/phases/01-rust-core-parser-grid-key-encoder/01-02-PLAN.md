---
phase: 01-rust-core-parser-grid-key-encoder
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - .planning/research/captures/capture-01-cpm-boot/bytes.bin
  - .planning/research/captures/capture-01-cpm-boot/README.md
  - .planning/research/captures/capture-01-cpm-boot/hexdump.txt
  - .planning/research/captures/capture-02-basic/bytes.bin
  - .planning/research/captures/capture-02-basic/README.md
  - .planning/research/captures/capture-02-basic/hexdump.txt
  - .planning/research/captures/README.md
autonomous: false
requirements:
  - PARSER-07

must_haves:
  truths:
    - "A directory `.planning/research/captures/capture-01-cpm-boot/` exists with bytes.bin + README.md + hexdump.txt (OR a documented deferral README records why D-08 fallback applies)"
    - "A directory `.planning/research/captures/capture-02-basic/` exists with bytes.bin + README.md + hexdump.txt (OR documented deferral)"
    - "The CR/LF convention the MicroBeast's CP/M actually emits is documented in the boot capture's README.md (resolves PARSER-07 Open Question #1 from RESEARCH.md)"
    - "The serial parameters (19200 8N1 no flow control per XPORT-04 assumption) are confirmed or corrected in the capture README.md"
    - "The capture-directory top-level README.md records which sequences were observed (VT52 opcode inventory) so Plan 04 can flag any MicroBeast-emitted sequence not already in the research inventory"
  artifacts:
    - path: ".planning/research/captures/capture-01-cpm-boot/bytes.bin"
      provides: "Raw serial byte capture of MicroBeast boot + CP/M prompt + `dir`/`stat`"
    - path: ".planning/research/captures/capture-01-cpm-boot/README.md"
      provides: "Serial params + workload description + CR/LF observation"
      contains: "CR/LF"
    - path: ".planning/research/captures/capture-01-cpm-boot/hexdump.txt"
      provides: "Annotated hex view of bytes.bin for reviewable diffs"
    - path: ".planning/research/captures/capture-02-basic/bytes.bin"
      provides: "Raw capture of BASIC LIST/RUN session"
    - path: ".planning/research/captures/capture-02-basic/README.md"
      provides: "Serial params + workload description + scroll/error behavior notes"
    - path: ".planning/research/captures/capture-02-basic/hexdump.txt"
      provides: "Annotated hex view"
    - path: ".planning/research/captures/README.md"
      provides: "Top-level inventory: what was captured, which VT52 opcodes observed, CR/LF finding summary, known deviations from DEC manual"
  key_links:
    - from: ".planning/research/captures/capture-01-cpm-boot/README.md"
      to: "parser PARSER-07 (CR/LF behavior)"
      via: "Documented CR/LF finding consumed by Plan 04 when implementing C0 handling"
      pattern: "CR.*(col 0|column 0|carriage return)"
    - from: ".planning/research/captures/README.md"
      to: "parser Plan 04 scope"
      via: "Sequence inventory list is an input to Plan 04's scope check"
      pattern: "ESC [A-Z=>]"
---

<objective>
Record a live byte-capture of the MicroBeast over serial across two workloads
(boot + CP/M commands; BASIC LIST/RUN) and document the CR/LF convention,
serial parameters, and observed VT52 sequence inventory. The captures ground
Phase 1's parser scope in real-device behavior rather than pure DEC-manual
inference — specifically resolving PARSER-07 and RESEARCH Open Question #1
(MicroBeast CR/LF convention).

Purpose: Per D-01, capture runs BEFORE (or concurrently with) the Plan 03
parser-strategy spike; the observed inventory informs the spike's sequence
set. Per D-08, the phase is NOT gated on having the MicroBeast physically
available — if capture cannot run, this plan records a documented deferral
(fallback to DEC-manual-derived fixtures in Plan 04) and still produces the
capture directory skeleton + README documenting the deferral reason.

Output: Either (A) full captures committed under
`.planning/research/captures/` with bytes.bin + README.md + hexdump.txt per
D-07, OR (B) a deferral README at `.planning/research/captures/README.md`
documenting the physical constraint and confirming Plan 04 proceeds from
DEC-manual + Wikipedia VT52 inventory (already enumerated in 01-RESEARCH.md).

This is the ONE plan in Phase 1 that cannot be fully automated by Claude —
it requires human-in-loop hardware interaction. The checkpoint task blocks
on the author running `tio` against the MicroBeast.
</objective>

<execution_context>
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/workflows/execute-plan.md
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rust-core-parser-grid-key-encoder/01-CONTEXT.md
@.planning/phases/01-rust-core-parser-grid-key-encoder/01-RESEARCH.md
@.planning/phases/01-rust-core-parser-grid-key-encoder/01-VALIDATION.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Capture MicroBeast boot + CP/M session (or record deferral)</name>
  <files>
    - .planning/research/captures/capture-01-cpm-boot/bytes.bin
    - .planning/research/captures/capture-01-cpm-boot/README.md
    - .planning/research/captures/capture-01-cpm-boot/hexdump.txt
  </files>
  <read_first>
    - .planning/phases/01-rust-core-parser-grid-key-encoder/01-CONTEXT.md (D-05 capture tool choice; D-06 workload definition; D-07 layout; D-08 deferral allowance)
    - .planning/phases/01-rust-core-parser-grid-key-encoder/01-RESEARCH.md (Environment Availability table — `tio` verified at /usr/bin/tio v2.7; capture invocation examples)
    - .planning/research/PITFALLS.md (pitfall #3 — CR/LF bias convention matters for cursor positioning)
    - .planning/research/FEATURES.md (VT52 sequence inventory — sequences expected in a CP/M boot session)
  </read_first>
  <action>
    This is a checkpoint:human-action task — Claude's role is to prepare the
    environment and tell the author what to run. Before the checkpoint blocks:

    1. Run `mkdir -p .planning/research/captures/capture-01-cpm-boot` so the
       output directory exists regardless of whether the author takes Path A
       (capture) or Path B (deferral).
    2. Verify `tio` is on PATH: `which tio`. If absent, fall back to
       documenting the alternative in Path A (minicom with similar flags).
    3. Print the Path A / Path B instructions from `<how-to-verify>` to the
       terminal so the author can follow them.
    4. Block on the `<resume-signal>` until the author responds.

    After the author responds, run the `<verify>` command to confirm the
    expected state is on disk before moving to Task 2.
  </action>
  <verify>
    <automated>test -d .planning/research/captures/capture-01-cpm-boot && test -f .planning/research/captures/capture-01-cpm-boot/README.md && ( (test -s .planning/research/captures/capture-01-cpm-boot/bytes.bin && test -s .planning/research/captures/capture-01-cpm-boot/hexdump.txt) || grep -qE "defer|D-08" .planning/research/captures/capture-01-cpm-boot/README.md )</automated>
  </verify>
  <what-built>
    Claude has created the capture directory skeleton (from Plan 01's
    `.gitkeep` seed) and provided the exact `tio` invocation and the README
    template. The author runs the serial capture against their physical
    MicroBeast and commits the output files.
  </what-built>
  <how-to-verify>
    This task has two valid completion paths. Pick the one that matches your
    situation and complete its checklist.

    ---

    ### Path A: MicroBeast is accessible — perform live capture

    1. **Identify the serial device path.**
       - Linux: `ls -l /dev/serial/by-id/` and pick the MicroBeast's USB-serial adapter. Likely `/dev/ttyUSB0` or `/dev/ttyACM0`.
       - Confirm no other program holds the port: `lsof /dev/ttyUSB0` should be empty.

    2. **Create the capture directory.**
       ```bash
       mkdir -p .planning/research/captures/capture-01-cpm-boot
       ```

    3. **Run `tio` against the MicroBeast at the MicroBeast preset (19200 8N1 no flow control — XPORT-04 assumption):**
       ```bash
       tio -b 19200 -d 8 -p none -s 1 -f none \
         -L -l .planning/research/captures/capture-01-cpm-boot/bytes.bin \
         /dev/ttyUSB0
       ```
       (Substitute the device path from step 1. `-L` enables logging; `-l` picks the log file.)

    4. **Drive the workload** (per D-06):
       - Power-cycle the MicroBeast so the boot banner is captured.
       - Wait for the CP/M prompt.
       - Type `DIR` then Enter. Observe output.
       - Type `STAT` then Enter. Observe output.
       - (Optionally repeat once so the parser has more than a single `dir` to chew on.)

    5. **Stop `tio`** with `Ctrl-T q` (tio's quit combo) so it closes the log file cleanly.

    6. **Produce an annotated hex dump**:
       ```bash
       xxd -c 16 .planning/research/captures/capture-01-cpm-boot/bytes.bin \
         > .planning/research/captures/capture-01-cpm-boot/hexdump.txt
       ```

    7. **Write the README.md** at `.planning/research/captures/capture-01-cpm-boot/README.md` with EXACTLY these sections filled in from observation (use the template below):

       ```markdown
       # Capture 01: CP/M Boot + `DIR` / `STAT`

       **Captured:** YYYY-MM-DD (use the day you actually ran it)
       **Tool:** tio 2.7
       **Device:** /dev/ttyUSB0 (or your actual device path)
       **Adapter:** (record VID:PID from `lsusb` — e.g., `10c4:ea60` for CP2102)

       ## Serial Parameters

       - Baud: 19200
       - Data bits: 8
       - Parity: none
       - Stop bits: 1
       - Flow control: none

       (Confirm or CORRECT this table based on what actually worked; if the
       MicroBeast refuses 19200 and you had to try 9600 or 38400, write the
       value that actually produced readable output. This verifies the
       XPORT-04 assumption for Phase 5.)

       ## Workload

       1. Power-cycled MicroBeast.
       2. Captured boot banner -> CP/M prompt.
       3. Ran: `DIR`
       4. Ran: `STAT`
       (List every command you typed. Be exact.)

       ## CR/LF Convention Observation

       (CRITICAL for PARSER-07. Inspect the first few lines of bytes.bin:
       look at what the MicroBeast emits at line endings. One of:
       - "CR only (0x0D, no LF)" — parser treats CR as return-to-col-0, LF as advance-row.
       - "LF only (0x0A, no CR)" — common CP/M tradition; parser default still works IF LF also resets col (non-standard VT52; document as divergence).
       - "CRLF (0x0D 0x0A)" — standard, both effects.
       Quote an actual byte sequence from the hexdump to justify your answer.)

       Example:
       > Observed: every line terminator in the boot banner is `0x0D 0x0A` (CRLF).
       > Justification: `hexdump.txt` line 3 shows `... 4c 47 52 41 4d 0d 0a 43 50 2f 4d 20` — "GRAM\r\nCP/M ".
       > Conclusion: standard VT52 CR/LF — CR returns to col 0, LF advances row, no override needed.

       ## Sequence Inventory

       List every VT52 / control sequence observed in this capture. At minimum:
       - Any 0x1B (ESC) byte — scan `hexdump.txt` with `grep -c '1b'`.
       - For each ESC: which letter follows? Record as `ESC <letter>` or
         `ESC Y <row> <col>` (with bytes).
       - Any 0x07 (BEL)?
       - Any 0x08 (BS)? 0x09 (HT)?

       Flag any sequence observed that is NOT in 01-RESEARCH.md "VT52 Opcode
       Table". Plan 04 will decide whether to add it or document as silent
       no-op.

       ## Notable Observations

       - (Anything surprising: partial glyphs, unexpected high-bit bytes,
         adapter-level quirks, BREAK-on-connect, etc.)
       - (Whether the boot banner repeats — indicates DTR-induced reset,
         which Phase 5 will need to handle.)
       ```

    8. **Confirm the files exist** and report back:
       ```bash
       ls -la .planning/research/captures/capture-01-cpm-boot/
       wc -c .planning/research/captures/capture-01-cpm-boot/bytes.bin
       # Should be non-empty; a boot banner + a few commands is typically 500 B - 5 KB.
       ```

    9. When the three files exist and README.md has all sections filled in, type `approved` to continue.

    ---

    ### Path B: MicroBeast is NOT accessible — record deferral per D-08

    D-08 explicitly allows this. The parser in Plan 04 falls back to
    DEC-manual-derived fixtures, which are already enumerated in
    01-RESEARCH.md's "VT52 Opcode Table".

    1. **Create the capture directory** (so later passes of Phase 1 / Phase 2 can drop actual captures in without restructuring):
       ```bash
       mkdir -p .planning/research/captures/capture-01-cpm-boot
       ```

    2. **Write a deferral README** at `.planning/research/captures/capture-01-cpm-boot/README.md`:
       ```markdown
       # Capture 01: Deferred per D-08

       **Status:** DEFERRED. Live MicroBeast capture not performed in Phase 1.
       **Reason:** (Explain: "MicroBeast hardware not on hand during Phase 1", "USB-serial adapter misplaced", etc.)
       **Decision authority:** CONTEXT.md D-08 explicitly allows deferral —
       parser work proceeds from DEC-manual + Wikipedia VT52 inventory
       (already enumerated in 01-RESEARCH.md "VT52 Opcode Table").

       ## Impact

       - PARSER-07 (CR/LF convention): parser defaults to standard VT52
         (CR → col 0 same row; LF → row+1 same col; CRLF → newline) per
         01-RESEARCH.md. Override toggle is Phase 4's (INPUT-05). First
         real MicroBeast run may surface a mismatch — fix in a follow-up.
       - XPORT-04 serial-params assumption (19200 8N1 no flow control):
         UNVERIFIED; Phase 5 first-connect is the validation point.
       - Sequence inventory: assumed to match 01-RESEARCH.md "VT52 Opcode
         Table". Any MicroBeast sequence outside this list is a Phase 4+
         follow-up.

       ## Re-entry

       When the MicroBeast becomes available, replace this file with the
       Path A template and produce `bytes.bin` + `hexdump.txt`.
       ```

    3. **Do NOT create `bytes.bin` or `hexdump.txt`** in the deferral case. The absence of these files + the presence of this README is the signal to Plan 04 that it proceeds from the DEC-manual fallback.

    4. Type `deferred` to continue — the executor treats this as a successful completion of this task because D-08 allows it.

    ---

    Either way, report back which path you took.
  </how-to-verify>
  <resume-signal>Type `approved` (Path A) or `deferred` (Path B)</resume-signal>
  <acceptance_criteria>
    Task is complete when EITHER:

    **Path A (capture):**
    - `test -d .planning/research/captures/capture-01-cpm-boot`
    - `test -s .planning/research/captures/capture-01-cpm-boot/bytes.bin` (non-empty)
    - `test -s .planning/research/captures/capture-01-cpm-boot/README.md` (non-empty)
    - `test -s .planning/research/captures/capture-01-cpm-boot/hexdump.txt` (non-empty)
    - `grep -iE "CR/LF|carriage|line ending|0x0D|0x0a" .planning/research/captures/capture-01-cpm-boot/README.md` returns at least one match (CR/LF finding documented)
    - `grep -iE "baud|19200|9600|38400" .planning/research/captures/capture-01-cpm-boot/README.md` returns at least one match (serial params recorded)

    **OR Path B (deferral):**
    - `test -f .planning/research/captures/capture-01-cpm-boot/README.md`
    - `grep -iE "defer|D-08" .planning/research/captures/capture-01-cpm-boot/README.md` returns at least one match
    - `! test -f .planning/research/captures/capture-01-cpm-boot/bytes.bin` (no byte file — that's the deferral signal)
  </acceptance_criteria>
  <done>
    CP/M boot capture either exists and documents CR/LF + serial params, OR a D-08 deferral README exists in its place.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Capture MicroBeast BASIC LIST/RUN session (or record deferral)</name>
  <files>
    - .planning/research/captures/capture-02-basic/bytes.bin
    - .planning/research/captures/capture-02-basic/README.md
    - .planning/research/captures/capture-02-basic/hexdump.txt
  </files>
  <read_first>
    - .planning/phases/01-rust-core-parser-grid-key-encoder/01-CONTEXT.md (D-06 workload #2 — BASIC LIST/RUN)
    - .planning/research/captures/capture-01-cpm-boot/README.md (whatever the prior task produced — the BASIC capture uses the SAME serial params as confirmed in capture-01)
  </read_first>
  <action>
    This is a checkpoint:human-action task — Claude prepares, the author
    runs the serial capture, Claude verifies. Before blocking:

    1. Run `mkdir -p .planning/research/captures/capture-02-basic` so the
       output directory exists.
    2. Read `.planning/research/captures/capture-01-cpm-boot/README.md`
       (produced by Task 1) and extract the confirmed serial parameters.
       Use the same params in the instructions printed to the author.
    3. Print the Path A / Path B instructions from `<how-to-verify>`.
    4. Block on `<resume-signal>`.

    After the author responds, run `<verify>` to confirm state on disk.
  </action>
  <verify>
    <automated>test -d .planning/research/captures/capture-02-basic && test -f .planning/research/captures/capture-02-basic/README.md && ( (test -s .planning/research/captures/capture-02-basic/bytes.bin && test -s .planning/research/captures/capture-02-basic/hexdump.txt) || grep -qE "defer|D-08" .planning/research/captures/capture-02-basic/README.md )</automated>
  </verify>
  <what-built>
    Same pattern as Task 1, but the workload is BASIC rather than CP/M.
    BASIC typically handles CR/LF, scroll, and syntax errors differently
    from the CP/M shell — this capture surfaces those differences.
  </what-built>
  <how-to-verify>
    Follow the same Path A / Path B structure as Task 1, but for the BASIC
    workload. Key differences:

    ### Path A — live capture

    1. `mkdir -p .planning/research/captures/capture-02-basic`
    2. Run `tio` with the SAME serial params confirmed by capture-01 and log to `capture-02-basic/bytes.bin`.
    3. Drive the workload (per D-06):
       - From CP/M, invoke BASIC (however your MicroBeast's BASIC starts — record the exact command).
       - Enter a short program. Suggested (tests scroll + line numbers + CR/LF):
         ```
         10 FOR I = 1 TO 25
         20 PRINT I; " MicroBeast"
         30 NEXT I
         ```
       - Type `LIST` and observe.
       - Type `RUN` and observe. The 25-line output forces at least one scroll.
       - Deliberately enter a syntax error (e.g. `PRINF "oops"`) and capture the error message. This surfaces BASIC's error-print behavior.
       - Exit BASIC cleanly.
    4. `xxd -c 16 capture-02-basic/bytes.bin > capture-02-basic/hexdump.txt`
    5. Write `capture-02-basic/README.md` with the same template as Task 1, but note:
       - How does BASIC's line termination differ from CP/M's (if at all)?
       - What sequences does scroll produce? Any `ESC I` (reverse LF) or just a stream of LFs?
       - What's the BASIC error-message format?
    6. Type `approved`.

    ### Path B — deferral

    Same as Task 1 Path B but with D-08 rationale adjusted for the BASIC
    workload. The deferral is transitive — if Task 1 deferred for "hardware
    unavailable", Task 2 defers for the same reason.
  </how-to-verify>
  <resume-signal>Type `approved` (Path A) or `deferred` (Path B)</resume-signal>
  <acceptance_criteria>
    Task is complete when EITHER:

    **Path A (capture):**
    - `test -s .planning/research/captures/capture-02-basic/bytes.bin`
    - `test -s .planning/research/captures/capture-02-basic/README.md`
    - `test -s .planning/research/captures/capture-02-basic/hexdump.txt`
    - `grep -iE "LIST|RUN|BASIC|scroll|error" .planning/research/captures/capture-02-basic/README.md` returns at least one match (workload documented)

    **OR Path B (deferral):**
    - `test -f .planning/research/captures/capture-02-basic/README.md`
    - `grep -iE "defer|D-08" .planning/research/captures/capture-02-basic/README.md` returns at least one match
    - `! test -f .planning/research/captures/capture-02-basic/bytes.bin`
  </acceptance_criteria>
  <done>
    BASIC LIST/RUN capture either exists with scroll + error documentation, or a D-08 deferral README exists.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write top-level captures inventory README</name>
  <files>
    - .planning/research/captures/README.md
  </files>
  <read_first>
    - .planning/research/captures/capture-01-cpm-boot/README.md (whatever Task 1 produced — Path A or Path B)
    - .planning/research/captures/capture-02-basic/README.md (whatever Task 2 produced)
    - .planning/phases/01-rust-core-parser-grid-key-encoder/01-RESEARCH.md (VT52 Opcode Table — the reference inventory to compare against)
    - .planning/phases/01-rust-core-parser-grid-key-encoder/01-CONTEXT.md (D-07 — top-level layout)
  </read_first>
  <action>
Read both per-capture READMEs and synthesize a top-level inventory README at
`.planning/research/captures/README.md`. The format is:

```markdown
# MicroBeast Capture Inventory

**Created:** (date)
**Phase:** 01-rust-core-parser-grid-key-encoder
**Purpose:** Ground Phase 1 parser scope in real-device behavior per CONTEXT D-06.

## Captures

| Dir | Workload | Status | Bytes | Date |
|-----|----------|--------|-------|------|
| `capture-01-cpm-boot/` | Boot + CP/M + `DIR` + `STAT` | (captured / deferred) | (byte count or "—") | (date or "—") |
| `capture-02-basic/` | BASIC `LIST`/`RUN` + syntax error | (captured / deferred) | (byte count or "—") | (date or "—") |

## Observed VT52 Sequence Inventory

(Aggregate across both captures. One row per unique ESC sequence seen. If
both captures are deferred, write "DEFERRED — fall back to 01-RESEARCH.md
'VT52 Opcode Table'" and skip this section.)

| Sequence | Bytes | Capture | Count (approx) | Notes |
|----------|-------|---------|----------------|-------|
| ESC J | 1B 4A | capture-01 | 2 | erases screen between prompts |
| ESC Y 0x20 0x20 | 1B 58 20 20 | capture-01 | 1 | home via direct addressing |
| ... | | | | |

## CR/LF Convention

(One line summarizing what the per-capture READMEs said, or the deferral state.)

Example (Path A result):
> MicroBeast CP/M emits CRLF (0x0D 0x0A) at line endings. Standard VT52 parser
> behavior (CR -> col 0, LF -> row+1) is correct without override.

Example (Path B result):
> DEFERRED per D-08. Parser defaults to standard VT52 CR/LF convention
> documented in 01-RESEARCH.md. First Phase 5 connect validates.

## Serial Parameters (XPORT-04 verification)

(From the per-capture READMEs.)

Example:
> Confirmed: 19200 8N1 no flow control works against MicroBeast. XPORT-04
> assumption stands.

## Divergences from DEC VT52 Manual

(Any sequence observed that is NOT in 01-RESEARCH.md's VT52 Opcode Table,
OR any sequence in the table that the MicroBeast never emits despite being
"expected". Per D-08, full-screen-editor sequences may be absent; that's
fine.)

Example:
> - MicroBeast never emits ESC F / ESC G (graphics mode) in either workload.
>   Plan 04 still implements these as silent no-ops per PARSER-04 — the
>   DEC manual documents them and a future workload may use them.
> - BASIC error messages terminate with BEL (0x07) — unexpected; flag for
>   Phase 3 visible-bell handling.

## Consumed by

- Plan 03 (parser-strategy spike) — sequence inventory shapes the spike's
  minimum test set (but D-02 locks a 7-sequence floor regardless).
- Plan 04 (parser core) — sequence inventory + CR/LF finding drive
  PARSER-07 implementation. Each entry in "Observed VT52 Sequence Inventory"
  becomes a fixture test case.
- Phase 5 (Web Serial transport) — XPORT-04 serial params taken from here.
```

Fill in every table and section from the concrete contents of the two
per-capture READMEs. If both Tasks 1 and 2 deferred, the inventory/CR/LF
/serial-params sections are short but still present with explicit
"DEFERRED" markers so downstream plans can grep for them.
  </action>
  <verify>
    <automated>test -s .planning/research/captures/README.md && grep -qE "Captures|Inventory|Consumed by" .planning/research/captures/README.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/research/captures/README.md` exists and is non-empty
    - Contains section header `## Captures` (verify: `grep -q "^## Captures" .planning/research/captures/README.md`)
    - Contains section header `## Observed VT52 Sequence Inventory` (verify: `grep -q "Sequence Inventory" .planning/research/captures/README.md`)
    - Contains section header `## CR/LF Convention` (verify: `grep -q "CR/LF Convention" .planning/research/captures/README.md`)
    - Contains section header `## Serial Parameters` (verify: `grep -q "Serial Parameters" .planning/research/captures/README.md`)
    - Contains section header `## Consumed by` (verify: `grep -q "Consumed by" .planning/research/captures/README.md`)
    - References both `capture-01-cpm-boot/` and `capture-02-basic/` (verify: `grep -qc "capture-01-cpm-boot" .planning/research/captures/README.md && grep -qc "capture-02-basic" .planning/research/captures/README.md`)
    - Each capture row has a status of either "captured" or "deferred" (not a blank placeholder)
  </acceptance_criteria>
  <done>
    Inventory README synthesizes both per-capture READMEs, documents the CR/LF finding or its deferral, records the XPORT-04 serial-params verification status, and tells Plan 04 which fixture inputs it has available.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MicroBeast serial adapter -> tio -> local disk | Bytes flow in one direction; no data leaves the machine; `tio` writes only to the local log file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | Capture byte-stream accuracy | MEDIUM | mitigate | `tio -L -l <file>` logs raw bytes directly; author confirms no terminal-side transformation (no `stty` mangling). Acceptance criterion requires non-empty `bytes.bin` with a `hexdump.txt` alongside for reviewability. |
| T-02-02 | Information Disclosure | Capture may contain host secrets or credentials typed during the session | LOW | mitigate | D-06 workloads (CP/M `DIR`/`STAT`, BASIC `LIST`/`RUN`) are idle / demo workloads; no auth, no secrets. Author instructed to NOT type passwords, SSH keys, or similar during the capture session. README template prompts author to document exactly what they typed so any sensitive input is visible in review. |
| T-02-03 | Denial of Service | Running `tio` while another process holds the serial port | LOW | accept | Path A instructions tell author to check `lsof /dev/ttyUSB0` first. If the port is busy, `tio` fails loudly; no silent data loss. |
| T-02-04 | Repudiation | Capture date / device not recorded, so provenance can't be audited later | LOW | mitigate | README template requires date, device path, adapter VID:PID, and workload description. Acceptance criterion greps for serial-params content. |
| T-02-05 | DTR-induced MicroBeast reset on connect (RESEARCH Pitfall 12 preview) | Tampering (accidental state reset) | LOW | accept | Plan 02 scope is OBSERVATIONAL — if the MicroBeast resets on `tio` connect, the capture shows a boot banner, which is the intended workload anyway. Phase 5 handles DTR/RTS signaling; Phase 1 only observes. |
| T-02-06 | Plan output gating downstream parser work | DoS (project-level) | LOW | mitigate | D-08 explicit deferral path (Path B in each task) ensures Plan 02 never blocks Plans 03 / 04. Acceptance criteria accept either capture OR deferral; top-level README communicates the state to downstream plans. |

No untrusted code is executed; no parser runs over the captured bytes until
Plan 04 (which has its own threat model covering untrusted byte ingestion).
</threat_model>

<verification>
```bash
# Capture directories exist
test -d .planning/research/captures/capture-01-cpm-boot
test -d .planning/research/captures/capture-02-basic

# Top-level inventory exists and synthesizes the per-capture READMEs
test -s .planning/research/captures/README.md
grep -q "Captures" .planning/research/captures/README.md
grep -q "CR/LF Convention" .planning/research/captures/README.md
grep -q "Serial Parameters" .planning/research/captures/README.md

# Either Path A (captured) or Path B (deferred) per task
for cap in capture-01-cpm-boot capture-02-basic; do
  if [ -f .planning/research/captures/$cap/bytes.bin ]; then
    # Path A: hexdump must also exist
    test -s .planning/research/captures/$cap/hexdump.txt
  else
    # Path B: README must reference D-08
    grep -qE "defer|D-08" .planning/research/captures/$cap/README.md
  fi
done
```

All six checks must pass (treating the conditional per-capture check as a
unit that must resolve one way or the other per capture).
</verification>

<success_criteria>
Plan 02 is complete when:

- [ ] Capture directory `capture-01-cpm-boot/` exists with EITHER (bytes.bin + README.md + hexdump.txt, all non-empty) OR (README.md with explicit D-08 deferral reference, no bytes.bin)
- [ ] Capture directory `capture-02-basic/` follows the same pattern
- [ ] Top-level `.planning/research/captures/README.md` exists and documents: captures table, VT52 sequence inventory (or deferral), CR/LF convention finding, serial-params (XPORT-04) verification status, divergences-from-DEC-manual, and "Consumed by" downstream plans
- [ ] PARSER-07 is either resolved (CR/LF convention documented) or explicitly deferred with D-08 citation
- [ ] Plan 03 (spike) and Plan 04 (parser core) can start without re-litigating the capture question
</success_criteria>

<output>
After completion, create `.planning/phases/01-rust-core-parser-grid-key-encoder/01-02-SUMMARY.md`.
</output>

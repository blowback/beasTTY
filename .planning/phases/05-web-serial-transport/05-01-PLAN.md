---
phase: 05-web-serial-transport
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - www/playwright.config.js
  - www/tests/transport/mock-serial.js
  - www/tests/transport/polite-fail.spec.js
  - www/tests/transport/connect.spec.js
  - www/tests/transport/readloop.spec.js
  - www/tests/transport/reconnect.spec.js
  - www/tests/transport/config.spec.js
  - www/tests/transport/paste.spec.js
  - www/tests/transport/errors.spec.js
  - .planning/phases/05-web-serial-transport/05-HUMAN-UAT.md
autonomous: true
requirements:
  - XPORT-01
  - XPORT-02
  - XPORT-03
  - XPORT-04
  - XPORT-05
  - XPORT-06
  - XPORT-07
  - XPORT-08
  - XPORT-09
  - XPORT-10
  - XPORT-11
  - PLAT-01
  - PLAT-02
tags: [phase-5, web-serial, playwright, mock, wave-0, nyquist]

must_haves:
  truths:
    - "Every XPORT-*/PLAT-* requirement + SC-1..SC-5 has at least one Playwright spec file stub under www/tests/transport/ that will turn green as later waves land implementation."
    - "navigator.serial mock fixture exists as an exported init-script string that overrides the API before main.js boots."
    - "window.__simulateUnplug / __simulateReplug / __mockReaderPush / __mockWriterLog hooks are installed and exercised by at least one stubbed spec call."
    - "Playwright testMatch discovers tests/transport/*.spec.js alongside existing render/input globs."
    - "05-HUMAN-UAT.md exists with a draft front-matter and a section skeleton mirroring 04-HUMAN-UAT.md so Wave 5 can fill test rows."
  artifacts:
    - path: "www/tests/transport/mock-serial.js"
      provides: "Exported SERIAL_MOCK init-script string + MockReader/MockWriter/MockSerialPort/MockSerial class hierarchy + test-hook wiring"
      contains: "export const SERIAL_MOCK"
    - path: "www/tests/transport/polite-fail.spec.js"
      provides: "Stub specs for PLAT-01, PLAT-02, D-32, D-33"
    - path: "www/tests/transport/connect.spec.js"
      provides: "Stub specs for XPORT-01..04, D-01, D-02, D-06, D-07, D-08, D-09, D-11"
    - path: "www/tests/transport/readloop.spec.js"
      provides: "Stub specs for XPORT-11, D-35, D-38, D-39"
    - path: "www/tests/transport/reconnect.spec.js"
      provides: "Stub specs for XPORT-06..08, XPORT-10, D-03..D-05, D-24..D-26, D-30, D-31, D-36, D-37, D-42"
    - path: "www/tests/transport/config.spec.js"
      provides: "Stub specs for XPORT-05, D-08"
    - path: "www/tests/transport/paste.spec.js"
      provides: "Stub specs for XPORT-09, D-12..D-23, D-41"
    - path: "www/tests/transport/errors.spec.js"
      provides: "Stub specs for D-27, D-28, D-29, D-37, D-40"
    - path: "www/playwright.config.js"
      provides: "testMatch extended with '**/transport/*.spec.js'"
      contains: "tests/transport"
    - path: ".planning/phases/05-web-serial-transport/05-HUMAN-UAT.md"
      provides: "Real-hardware UAT skeleton (front-matter + Current Test + Tests + Summary + Gaps sections)"
  key_links:
    - from: "www/tests/transport/*.spec.js"
      to: "www/tests/transport/mock-serial.js"
      via: "import { SERIAL_MOCK } from './mock-serial.js'"
      pattern: "import \\{ SERIAL_MOCK \\} from '\\./mock-serial\\.js'"
    - from: "www/tests/transport/*.spec.js"
      to: "navigator.serial mock"
      via: "page.addInitScript(SERIAL_MOCK) before page.goto('/')"
      pattern: "page\\.addInitScript\\(SERIAL_MOCK\\)"
    - from: "www/playwright.config.js"
      to: "www/tests/transport/"
      via: "testMatch glob"
      pattern: "\\*\\*/transport/\\*\\.spec\\.js"
---

<objective>
Wave 0 scaffolding — install the full Playwright test harness for Phase 5 BEFORE any production code lands, so every subsequent wave (02..07) can verify its work against pre-written spec stubs.

Purpose: Nyquist sampling discipline — the 13 phase requirements (XPORT-01..11, PLAT-01..02) plus the 5 ROADMAP SCs must all have an automated verification target that exists at commit time. Later waves "un-fixme" the stubs as implementation lands; zero new spec files need to appear in Waves 1-5.

Output: 1 shared mock fixture (`mock-serial.js`) + 7 spec stub files + 1 playwright.config.js extension + 1 05-HUMAN-UAT.md skeleton. Every stub compiles, every stub uses `test.fixme` or a trivially-true assertion so the suite stays green (zero red-flakes) even though no production code exists yet.
</objective>

<execution_context>
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/workflows/execute-plan.md
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-web-serial-transport/05-CONTEXT.md
@.planning/phases/05-web-serial-transport/05-RESEARCH.md
@.planning/phases/05-web-serial-transport/05-PATTERNS.md
@.planning/phases/05-web-serial-transport/05-VALIDATION.md
@.planning/phases/04-keyboard-input/04-HUMAN-UAT.md
@www/playwright.config.js
@www/tests/input/crlf-override.spec.js
@www/tests/render/bell.spec.js

<interfaces>
<!-- Critical contracts the executor must replicate verbatim. -->
<!-- Source: 05-RESEARCH.md Pattern 7 (lines 702-803) — the mock itself. -->
<!-- Source: 05-PATTERNS.md §"Playwright Spec Files" — spec structure. -->

Mock export shape (verbatim from 05-RESEARCH.md lines 702-803):
```js
// www/tests/transport/mock-serial.js
export const SERIAL_MOCK = `
(() => {
  const DEFAULT_INFO = { usbVendorId: 0x10c4, usbProductId: 0xea60 };
  window.__mockWriterLog = [];
  window.__mockState = { opened: false, port: null, listeners: {} };

  class MockReader { /* constructor/read/cancel/releaseLock — see RESEARCH lines 713-724 */ }
  class MockWriter { /* constructor/write/releaseLock — see RESEARCH lines 725-733 */ }
  class MockSerialPort extends EventTarget { /* see RESEARCH lines 734-752 */ }
  class MockSerial extends EventTarget { /* see RESEARCH lines 753-762 */ }

  const serial = new MockSerial();
  Object.defineProperty(navigator, 'serial', { value: serial, configurable: true });

  window.__simulateUnplug = () => { /* RESEARCH lines 767-781 */ };
  window.__simulateReplug = () => { /* RESEARCH lines 782-790 */ };
  window.__mockReaderPush = (bytes) => { /* RESEARCH lines 791-802 */ };
})();`;
```

Spec setup pattern (from 05-PATTERNS.md §"Test setup helper + DOM warmup"):
```js
import { test, expect } from '@playwright/test';
import { SERIAL_MOCK } from './mock-serial.js';

async function setup(page) {
  await page.addInitScript(SERIAL_MOCK);   // MUST run before page.goto
  await page.goto('/');
  await page.locator('#terminal-wrapper').focus();
  await page.waitForFunction(() => document.getElementById('terminal').width > 0);
  await page.locator('#connection').evaluate((el) => { el.open = true; }); // spec-local
}
```

Required hook contract (the mock exposes):
- `window.__simulateUnplug()` — dispatches `disconnect` event on navigator.serial, sets port.readable=null, resolves any pending read() with `{done:true}`.
- `window.__simulateReplug()` — dispatches `connect` event on navigator.serial.
- `window.__mockReaderPush(bytes)` — simulates MicroBeast output bytes arriving via reader.read().
- `window.__mockWriterLog` — array of `{ bytes: number[], ts: number }` recording every writer.write call.
- `navigator.serial._grantedPorts` — array (for `_grantedPorts[0]._config` / `_lastSignals` introspection).

Phase 4 playwright.config.js (existing — www/playwright.config.js:7):
```js
testMatch: ['**/render/*.spec.js', '**/input/*.spec.js'],
// Phase 5 extends:
testMatch: ['**/render/*.spec.js', '**/input/*.spec.js', '**/transport/*.spec.js'],
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create www/tests/transport/mock-serial.js + extend playwright.config.js testMatch</name>
  <read_first>
    - 05-RESEARCH.md lines 696-803 — Pattern 7 full mock implementation (copy verbatim)
    - 05-PATTERNS.md §"www/tests/transport/mock-serial.js" (lines 257-286) — export shape + `__`-prefix hook convention
    - www/playwright.config.js — full file (existing testMatch at line 7)
    - www/tests/render/bell.spec.js — existing pre-boot API stub precedent (Object.defineProperty pattern)
    - 05-CONTEXT.md §"D-42" — __simulateUnplug/__simulateReplug requirement
  </read_first>
  <files>
    www/tests/transport/mock-serial.js
    www/playwright.config.js
  </files>
  <action>
    Create `www/tests/transport/mock-serial.js` by copying the full `SERIAL_MOCK` init-script from 05-RESEARCH.md lines 696-803 VERBATIM, with these exact specifics:

    1. File exports `export const SERIAL_MOCK = \`...backticked IIFE...\`;` — nothing else (no default export, no other exports). The backtick string is a self-executing IIFE `(() => { ... })();`.

    2. Inside the IIFE, declare:
       - `const DEFAULT_INFO = { usbVendorId: 0x10c4, usbProductId: 0xea60 };` (CP2102N — verbatim hex per D-02).
       - `window.__mockWriterLog = [];` — array of `{ bytes: number[], ts: number }` per writer.write.
       - `window.__mockState = { opened: false, port: null, listeners: {} };`

    3. Define four classes (copy structure from 05-RESEARCH.md lines 713-762):
       - `MockReader` — `constructor(port)`, `async read()` (returns `{value, done:true}` when cancelled, resolves pending read when `this.waiter` set), `async cancel()` (sets `this.cancelled = true`, resolves waiter with `{done:true}`), `releaseLock()` no-op.
       - `MockWriter` — `constructor(port)`, `async write(bytes)` pushes `{ bytes: Array.from(bytes), ts: performance.now() }` to `window.__mockWriterLog`, `releaseLock()` no-op.
       - `MockSerialPort extends EventTarget` — `constructor(info = DEFAULT_INFO)` sets `this._info`, `this._opened=false`, `this.readable=null`, `this.writable=null`, `this.connected=true`. `getInfo()` returns `{ ...this._info }`. `async open(config)` sets `this._opened=true; this._config=config;` creates a `MockReader` + `MockWriter`, sets `this.readable={getReader:()=>reader}` and `this.writable={getWriter:()=>writer}`. `async close()` sets `_opened=false; readable=null; writable=null`. `async setSignals(s)` stores `this._lastSignals = s`. `async getSignals()` returns `{dataCarrierDetect:false, clearToSend:true, ringIndicator:false, dataSetReady:true}`. `async forget() {}`.
       - `MockSerial extends EventTarget` — `constructor()` sets `this._grantedPorts = []`. `async requestPort(opts)` creates `new MockSerialPort(DEFAULT_INFO)`, pushes into `_grantedPorts`, returns it. `async getPorts()` returns `[...this._grantedPorts]`.

    4. After class definitions, install the mock: `const serial = new MockSerial(); Object.defineProperty(navigator, 'serial', { value: serial, configurable: true });`

    5. Install three test hooks on `window`:
       - `window.__simulateUnplug = () => { ... }` — grabs the last-granted port, sets `port.connected=false` and `port.readable=null`, creates `new Event('disconnect', {bubbles:true})` with Object.defineProperty-overridden `target = port`, dispatches on `serial`. If `port._reader && port._reader.waiter` exists, resolve waiter with `{value:undefined, done:true}` and null the waiter.
       - `window.__simulateReplug = () => { ... }` — grabs the last-granted port, sets `port.connected=true`, creates `new Event('connect', {bubbles:true})` with `target=port`, dispatches on `serial`.
       - `window.__mockReaderPush = (bytes) => { ... }` — creates `new Uint8Array(bytes)`. If port has a pending waiter, resolve it `{value:chunk, done:false}`; otherwise `port._reader.pending.push(chunk)`.

    6. MockReader needs: `this.pending = []` (queue for chunks pushed before a read is pending), `this.waiter = null` (promise resolver when read awaits). The `async read()` body: if `this.cancelled` return `{value:undefined, done:true}`; if `this.pending.length > 0` return `{value: this.pending.shift(), done:false}`; otherwise `return new Promise((resolve) => { this.waiter = resolve; });`.

    7. Extend `www/playwright.config.js` line 7 testMatch array: change from
       `testMatch: ['**/render/*.spec.js', '**/input/*.spec.js'],`
       to
       `testMatch: ['**/render/*.spec.js', '**/input/*.spec.js', '**/transport/*.spec.js'],`
       Do not touch any other config entry.

    Constraints:
    - The mock is a STRING (backtick template literal), NOT a module with live ES imports, because `page.addInitScript` runs it in the page context before any module loads.
    - Do NOT use TextDecoder/TextEncoder inside the mock — Pitfall #10 / D-35 applies to test fixtures too.
    - `window.__`-prefix convention locked by main.js:55-64 (existing `window.__testGridView` precedent in Phase 4).
    - File header comment block mirrors `www/input/tx-sink.js` lines 1-14 (phase / source / analog preamble). Source lines: 05-CONTEXT D-40..D-42, 05-RESEARCH Pattern 7.
  </action>
  <behavior>
    - SERIAL_MOCK export is a string beginning with `(() => {` and ending with `})();`
    - After addInitScript(SERIAL_MOCK) + page load, `navigator.serial` is the mock instance (not undefined, not the real API in headless Chrome which has it absent anyway).
    - `window.__mockWriterLog` starts as an empty array.
    - `navigator.serial.requestPort({filters:[...]})` returns a MockSerialPort with VID/PID 10c4:ea60.
    - Calling `window.__simulateUnplug()` after a grant dispatches a 'disconnect' event with the port as target.
    - Playwright test runner discovers files matching `**/transport/*.spec.js` without affecting existing render/ and input/ suites.
  </behavior>
  <verify>
    <automated>cd www && npx playwright test --list 2>&1 | grep -E "transport/" | head -5</automated>
  </verify>
  <done>
    - `www/tests/transport/mock-serial.js` exists; `grep -c 'export const SERIAL_MOCK' www/tests/transport/mock-serial.js` returns 1.
    - `grep -c '10c4' www/tests/transport/mock-serial.js` returns >= 1 (VID present).
    - `grep -c 'ea60' www/tests/transport/mock-serial.js` returns >= 1 (PID present).
    - `grep -c '__simulateUnplug' www/tests/transport/mock-serial.js` returns >= 1.
    - `grep -c '__simulateReplug' www/tests/transport/mock-serial.js` returns >= 1.
    - `grep -c '__mockReaderPush' www/tests/transport/mock-serial.js` returns >= 1.
    - `grep -c '__mockWriterLog' www/tests/transport/mock-serial.js` returns >= 1.
    - `grep -c "'\*\*/transport/\*\.spec\.js'" www/playwright.config.js` returns 1.
    - `cd www && node -e "import('./tests/transport/mock-serial.js').then(m => console.log(typeof m.SERIAL_MOCK))"` prints `string`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 7 Playwright spec stubs under www/tests/transport/</name>
  <read_first>
    - 05-PATTERNS.md §"Playwright Spec Files" (lines 289-418) — full shared structure + per-spec REQ-ID coverage table
    - 05-RESEARCH.md §Wave 0 Gaps (lines 1343-1352) — exact list of 7 spec files + requirement coverage
    - 05-RESEARCH.md §"Phase Requirements → Test Map" (lines 1260-1275) — grep strings per requirement
    - www/tests/input/crlf-override.spec.js — verbatim structural analog (setup helper + describe + fast/slow tags)
    - www/tests/input/keydown-arrows.spec.js — @fast tag simple click-and-assert template
    - 05-UI-SPEC.md §"Copywriting Contract" lines 147-219 — exact label strings for stub assertions
  </read_first>
  <files>
    www/tests/transport/polite-fail.spec.js
    www/tests/transport/connect.spec.js
    www/tests/transport/readloop.spec.js
    www/tests/transport/reconnect.spec.js
    www/tests/transport/config.spec.js
    www/tests/transport/paste.spec.js
    www/tests/transport/errors.spec.js
  </files>
  <action>
    Create 7 spec files. Each file MUST:

    1. Open with `// Phase 5 Plan NN — {coverage} — {brief}.` header line.
    2. Import `import { test, expect } from '@playwright/test';`
    3. Import `import { SERIAL_MOCK } from './mock-serial.js';` EXCEPT for polite-fail.spec.js which needs to DELETE navigator.serial (not mock it).
    4. Define an `async function setup(page)` helper mirroring `www/tests/input/crlf-override.spec.js` lines 4-11 (addInitScript BEFORE goto, terminal-wrapper focus, width > 0 wait).
    5. Use `test.describe('{REQ-ID} — {label}', () => { ... });` wrapper.
    6. Include `test.fixme` on every sub-test (so suite stays green — later waves un-fixme as implementation lands).

    Per-file specifics (copy describe-block labels verbatim from 05-PATTERNS.md lines 408-417):

    **www/tests/transport/polite-fail.spec.js** — `describe('PLAT-01, PLAT-02 — Polite-fail for non-Chromium', ...)`. Setup here uses `page.addInitScript(() => { delete navigator.serial; })` INSTEAD of SERIAL_MOCK. Three fixme stubs:
      - `test.fixme('body replaced with polite-fail content when navigator.serial undefined @fast')` — body shape: `await setup(page); await expect(page.locator('h1')).toHaveText('BestialiTTY requires a Chromium-based browser');` Wrapped with fixme so it doesn't fail yet.
      - `test.fixme('title reads "BestialiTTY — Chromium required"')`
      - `test.fixme('no canvas element on polite-fail page')`

    **www/tests/transport/connect.spec.js** — `describe('XPORT-01..04 + D-01/D-02/D-06..D-11 — Connect to MicroBeast', ...)`. Six fixme stubs:
      - `test.fixme('Connect button visible in top-bar with data-state="disconnected" @fast')`
      - `test.fixme('click Connect calls requestPort with CP2102N filter 10c4:ea60')`
      - `test.fixme('port.open called with 19200 8N1 none none preset @fast')`
      - `test.fixme('setSignals called with DTR=false RTS=false after open')`
      - `test.fixme('button label cycles Connect → Connecting… → Disconnect')`
      - `test.fixme('button border color transitions gray → amber → green')`

    **www/tests/transport/readloop.spec.js** — `describe('XPORT-11 + SC-5 + D-35/D-38/D-39 — Read loop', ...)`. Four fixme stubs:
      - `test.fixme('pushed bytes feed into term.feed and render on grid @fast')`
      - `test.fixme('reader.read called with no size hint')`
      - `test.fixme('visibilitychange !hidden triggers requestFrame catch-up')`
      - `test.fixme('read error transitions state to port-lost')`

    **www/tests/transport/reconnect.spec.js** — `describe('XPORT-06..08, XPORT-10 + SC-3 + D-03..D-05/D-24..D-26/D-30..D-31/D-36/D-37/D-42 — Reconnect', ...)`. Seven fixme stubs:
      - `test.fixme('reader.cancel called before port.close on Disconnect click @fast')`
      - `test.fixme('simulateUnplug transitions state to port-lost')`
      - `test.fixme('simulateReplug with matching VID/PID auto-reconnects silently')`
      - `test.fixme('auto-reconnect retries once after 500ms on transient open fail')`
      - `test.fixme('reload with granted port stashes reference but does NOT auto-open')`
      - `test.fixme('connect/disconnect listeners registered on navigator.serial not port')`
      - `test.fixme('localStorage bestialitty.port.preset written after first open')`

    **www/tests/transport/config.spec.js** — `describe('XPORT-05 + D-08 — Serial config form', ...)`. Five fixme stubs:
      - `test.fixme('baud select defaults to 19200 @fast')`
      - `test.fixme('databits/stopbits/parity/flowctl defaults match MicroBeast preset @fast')`
      - `test.fixme('Reset to MicroBeast preset button snaps all five selects to defaults')`
      - `test.fixme('changing baud while connected shows Config changed hint')`
      - `test.fixme('connect honors non-default config values')`

    **www/tests/transport/paste.spec.js** — `describe('XPORT-09 + D-12..D-23/D-41 — Paste pump', ...)`. Seven fixme stubs:
      - `test.fixme('Paste test button routes textarea through paste-pump @fast')`
      - `test.fixme('paste at 19200 baud paces >= 95% of expected duration @slow')`
      - `test.fixme('progress line Pasting N B — P% updates per chunk')`
      - `test.fixme('Cancel button halts pump and shows "Paste cancelled"')`
      - `test.fixme('Esc while paste active cancels and does NOT emit 0x1B')`
      - `test.fixme('keypresses interleaved during paste queue-jump between chunks')`
      - `test.fixme('port-lost mid-paste shows "Paste cancelled — port lost (N bytes unsent)"')`
      - `test.fixme('CR/LF mode crlf rewrites 0x0D to 0x0D 0x0A before enqueue')`

    **www/tests/transport/errors.spec.js** — `describe('D-27..D-29 + D-37 — Error log & lifecycle', ...)`. Five fixme stubs:
      - `test.fixme('error log shows last 5 entries newest-first @fast')`
      - `test.fixme('permission revoked mid-read shows permission-revoked code')`
      - `test.fixme('port-in-use error on open shows port-in-use code')`
      - `test.fixme('multiple CP2102N adapters on reconnect shows multiple-adapters code')`
      - `test.fixme('error log timestamp uses HH:MM:SS 24-hour format')`

    Every `test.fixme(...)` body contains at least one `await setup(page);` call plus a stub assertion (even if trivially `expect(true).toBe(true)`). This keeps the file from exporting an empty test and exercises the setup helper (and thus mock installation) during linting.

    CRITICAL: Use `test.fixme('title @tag', async ({ page }) => { ... })` — the fixme modifier at test-declaration level (not `test.skip`) so the runner reports them as "expected-to-fail" not "skipped". Later waves convert `test.fixme(` → `test(` as each assertion becomes executable.

    **Counting minimum test stubs across all 7 files: 3+6+4+7+5+8+5 = 38 total stubs.**

    Header comment template for every spec file:
    ```
    // Phase 5 Plan 01 (Wave 0) — {REQ-IDs} stub spec.
    // Source: 05-RESEARCH.md §Validation Architecture; 05-CONTEXT.md D-{XX}.
    // Stubs are test.fixme until later waves land production code.
    ```
  </action>
  <behavior>
    - `npx playwright test tests/transport --list` reports 38 discovered tests.
    - `npx playwright test tests/transport` runs and exits with code 0 (all tests fixme'd = no failures).
    - Running full suite (`npx playwright test`) still passes the Phase 3 + Phase 4 tests without regression.
    - No spec file imports anything from `www/transport/serial.js` or `www/input/paste-pump.js` (those don't exist yet — stubs use the mock only).
  </behavior>
  <verify>
    <automated>cd www && npx playwright test tests/transport --list 2>&1 | tail -5 && cd www && npx playwright test tests/transport 2>&1 | tail -10</automated>
  </verify>
  <done>
    - All 7 spec files exist under `www/tests/transport/`.
    - `find www/tests/transport -name '*.spec.js' | wc -l` returns 7.
    - `grep -rn 'test.fixme' www/tests/transport/ | wc -l` returns >= 38.
    - `cd www && npx playwright test tests/transport` exits 0.
    - `cd www && npx playwright test` (full suite) exits 0 (no regression in render/ + input/ suites).
    - `grep -c "import { SERIAL_MOCK }" www/tests/transport/connect.spec.js` returns 1 (likewise for 5 other specs — polite-fail.spec.js is the one exception that deletes navigator.serial directly).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create 05-HUMAN-UAT.md skeleton with real-hardware test rows</name>
  <read_first>
    - .planning/phases/04-keyboard-input/04-HUMAN-UAT.md — full file (verbatim front-matter + Tests/Summary/Gaps section shape)
    - 05-CONTEXT.md lines 55-57 — list of real-hardware UAT items (connect, unplug, reload, paste, polite-fail)
    - 05-VALIDATION.md §"Manual-Only Verifications" (lines 61-70) — 6 manual items and their SC mapping
    - 05-UI-SPEC.md §"Polite-fail page" (lines 208-219) — exact copy to verify against
  </read_first>
  <files>.planning/phases/05-web-serial-transport/05-HUMAN-UAT.md</files>
  <action>
    Create `.planning/phases/05-web-serial-transport/05-HUMAN-UAT.md` mirroring `.planning/phases/04-keyboard-input/04-HUMAN-UAT.md` shape verbatim.

    1. YAML front-matter (verbatim from 04 with phase changed):
       ```yaml
       ---
       status: draft
       phase: 05-web-serial-transport
       source: [05-VERIFICATION.md, 05-VALIDATION.md]
       started: 2026-04-23
       updated: 2026-04-23
       ---
       ```

    2. H1 heading: `# Phase 5 — Human UAT (real hardware)`

    3. Section `## Current Test` with body `[testing not yet started]`.

    4. Section `## Tests` containing 6 test rows — one per 05-VALIDATION.md manual item. Each row uses this exact shape:
       ```
       ### N. {short title}

       **expected:** {what the human should observe}

       **steps:**
       1. {step}
       2. {step}

       **result:** pending

       **reason:** (fill after test)
       ```

       Six test rows (fill `expected`/`steps` skeletally — Wave 6 Plan 07 fills full details):

       **1. Real MicroBeast connect + type commands (SC-1 / XPORT-04)**
       expected: Power the MicroBeast, plug USB-C, click Connect, pick CP2102N 10c4:ea60 in native picker. Connection pane shows `MicroBeast (CP2102N 10c4:ea60) — 19200 8N1`, Connect button border turns green, button label reads `Disconnect`. Type `HELP` + Enter — MicroBeast responds on canvas. No boot banner appears on Connect (DTR/RTS stayed low).
       steps: (placeholder — Wave 6 fills)

       **2. Physical unplug / replug (XPORT-06 / XPORT-08 / SC-3)**
       expected: With connection live, yank USB cable — within ~1 s button border turns red, label reads `Reconnect`. Error log shows `read-error` or similar. Reinsert USB — border cycles red → amber → green silently; typing resumes without permission prompt.
       steps: (placeholder)

       **3. Reload with granted port (XPORT-07 / SC-3c)**
       expected: With connection live, hit reload. App loads; Connect button reads `Connect`, border gray. Connection pane shows `MicroBeast (CP2102N 10c4:ea60) — click Connect`. Click Connect — no permission prompt (port already granted), connects in < 1 s.
       steps: (placeholder)

       **4. Paste at 19200 baud no-overrun (XPORT-09 / SC-4b)**
       expected: Connect, type `copy con dummy.txt` on MicroBeast CP/M, paste ~2 KB of text via Debug pane Paste test button. Progress line ticks 0% → 100% over ~1.2 s. No dropped bytes in MicroBeast file (compare SHA256 before/after).
       steps: (placeholder)

       **5. Polite fail in Firefox AND Safari (PLAT-01 / PLAT-02 / SC-5a)**
       expected: Open BestialiTTY URL in Firefox stable AND Safari (macOS). Each shows polite-fail page with heading `BestialiTTY requires a Chromium-based browser`, bulleted browser list, Download Chromium link. Zero console errors. Title reads `BestialiTTY — Chromium required`. No canvas flash before takeover.
       steps: (placeholder)

       **6. 5-minute daily-driver feel (PROJECT.md Core Value)**
       expected: Drive a real work session — CP/M shell, BASIC program, intentional paste, intentional Ctrl+C, intentional Disconnect + Connect. Focus retention on every chrome click (terminal stays focused; typing never misses). No jarring pane pops during paste. Reconnect after accidental unplug is seamless.
       steps: (placeholder)

    5. Section `## Summary` with a table shell:
       ```
       | metric | value |
       |--------|-------|
       | passed | - |
       | issues | - |
       ```

    6. Section `## Gaps` with body `(to be filled as tests run)`.

    7. Section `## Sign-Off` with body `**Approval:** pending`.

    The `(placeholder — Wave 6 fills)` marker is intentional — Wave 6 Plan 07 owns filling the step-by-step detail AFTER all implementation lands. Wave 0 just locks the test rows exist.
  </action>
  <behavior>
    - File exists and contains 6 `### N. {title}` headings (one per VALIDATION.md manual item).
    - Front-matter YAML is valid.
    - Each test row has `expected:`, `steps:`, `result: pending`, `reason:` fields.
  </behavior>
  <verify>
    <automated>grep -c '^### [1-6]\. ' .planning/phases/05-web-serial-transport/05-HUMAN-UAT.md && grep -c 'result: pending' .planning/phases/05-web-serial-transport/05-HUMAN-UAT.md</automated>
  </verify>
  <done>
    - `.planning/phases/05-web-serial-transport/05-HUMAN-UAT.md` exists.
    - File contains exactly 6 `### N. ` test-row headings (1..6).
    - File contains 6 `result: pending` lines.
    - File contains `status: draft` and `phase: 05-web-serial-transport` in front-matter.
    - Tests 1..6 each cover one SC or XPORT-* ID from 05-VALIDATION.md manual-only table.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-harness → browser | Playwright init-script runs privileged DOM replacement BEFORE app boot; any accidental global leak from the mock would pollute real-browser runs |
| mock fixture → real navigator.serial | If the mock leaks into production bundles (shouldn't — www/tests/ is outside the deploy path), it would masquerade as the real API |
| spec file → mock internal state | Tests inspect `navigator.serial._grantedPorts[0]._config` — private API; change to mock is a spec-wide concern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | www/tests/transport/mock-serial.js leaking global state into real-browser builds | mitigate | Mock is confined to `www/tests/` (outside static-deploy path); addInitScript is Playwright-only API not in production bundles; file header documents "TEST-ONLY — never imported from www/main.js or any production module" |
| T-05-01-02 | Information Disclosure | `window.__mockWriterLog` exposing simulated TX bytes cross-tab | accept | Accept: no production code calls addInitScript; `window.__`-prefix convention is per-origin and test-only (consistent with Phase 4 `window.__testGridView` precedent at main.js:55-64) |
| T-05-01-03 | Denial of Service | Mock reader.read() returns a Promise that never resolves (no pending chunk, no cancel) | mitigate | Test specs using the mock MUST either push a chunk via `__mockReaderPush` OR call `__simulateUnplug` to resolve pending read — documented via stub `test.fixme` comments; real usage is test-scoped (Playwright default timeout 30s catches hang) |
| T-05-01-04 | Spoofing | Test specs might accidentally bypass addInitScript and use real navigator.serial | mitigate | Phase 5 Playwright specs run in headless Chrome — no USB hardware access; `navigator.serial` is `undefined` in headless mode unless addInitScript installs the mock; polite-fail.spec.js explicitly `delete navigator.serial` before boot |
| T-05-01-05 | Elevation | Mock classes not bound to WICG Serial spec shape; divergence could hide real-API bugs | accept | Accept for Wave 0; Wave 2-6 real-hardware UAT (05-HUMAN-UAT.md items 1-4) validates against the actual CP2102N + real navigator.serial; mock is explicitly a spec-level stub, not a compliance reference |
</threat_model>

<verification>
Run these after all 3 tasks complete:

1. Full Playwright discovery + transport subset:
```
cd www && npx playwright test --list 2>&1 | grep -c 'transport/'
```
Expected: >= 38 (sum of fixme stubs across 7 files).

2. Transport subset passes (all fixme = green):
```
cd www && npx playwright test tests/transport
```
Expected: exit 0, "N skipped" or "N expected failures" in summary (no red).

3. Full suite no-regression:
```
cd www && npx playwright test
```
Expected: exit 0; Phase 3 render/ + Phase 4 input/ tests still green (32+ passed).

4. HUMAN-UAT skeleton exists:
```
test -f .planning/phases/05-web-serial-transport/05-HUMAN-UAT.md && grep -c '^### [1-6]\. ' .planning/phases/05-web-serial-transport/05-HUMAN-UAT.md
```
Expected: 6.
</verification>

<success_criteria>
- mock-serial.js exports SERIAL_MOCK as a string beginning with `(() => {`
- 7 spec files exist under www/tests/transport/ with 38+ test.fixme stubs
- playwright.config.js testMatch extended with `'**/transport/*.spec.js'`
- 05-HUMAN-UAT.md has 6 real-hardware test rows mapping to VALIDATION.md manual items
- Full Playwright suite runs with exit 0 (no red-flakes from stubs)
- No production file under `www/main.js`, `www/transport/`, `www/input/` touched (Wave 0 is scaffolding-only)
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-serial-transport/05-01-SUMMARY.md`
</output>

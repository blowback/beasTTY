---
phase: 02-wasm-boundary-minimal-js-harness
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - scripts/smoke-wasm-build.sh
  - www/README.md
autonomous: false
requirements:
  - CORE-03
  - CORE-04
  - CORE-05
tags:
  - wasm
  - verification
  - smoke-test
  - docs

must_haves:
  truths:
    - "scripts/smoke-wasm-build.sh is an executable bash script that runs wasm-pack build into a throwaway temp dir and asserts the four expected output files exist"
    - "scripts/smoke-wasm-build.sh exits 0 on success and non-zero with a readable diagnostic on any missing file"
    - "www/README.md documents the two dev-server options (python3 -m http.server, basic-http-server) per D-14 and the manual SC-1/SC-2/SC-3/SC-4 verification flow"
    - "SC-1 has been demonstrated live: ./scripts/build.sh produces www/pkg/, the dev server serves it, Chromium loads http://localhost:8000/ and shows the boot log with no console errors"
    - "SC-2 has been demonstrated live: pasting Hello + ESC Y + World into the textarea and clicking Feed renders expected ASCII + cursor position in the harness"
    - "SC-3 has been demonstrated: DevTools Memory / Performance track shows no per-frame allocation growth after initial view construction"
    - "SC-4 has been demonstrated live: clicking 64 KB Stress logs ONE [SC-4] line per click AND DevTools Performance tab shows a single Terminal.feed entry per click (not 65,536)"
    - "ls -lh www/pkg/bestialitty_core_bg.wasm output recorded in the plan SUMMARY as an informational bundle-size baseline"
  artifacts:
    - path: "scripts/smoke-wasm-build.sh"
      provides: "CI-style wasm-pack build gate"
      contains: "wasm-pack build crates/bestialitty-core"
    - path: "www/README.md"
      provides: "Dev-server options + SC-1..SC-4 manual verification instructions"
      contains: "python3 -m http.server -d www"
  key_links:
    - from: "scripts/smoke-wasm-build.sh"
      to: "crates/bestialitty-core (wasm-pack --target web)"
      via: "wasm-pack build crates/bestialitty-core --target web --out-dir <tmp> --no-opt"
      pattern: "wasm-pack build crates/bestialitty-core --target web"
    - from: "www/README.md"
      to: "manual SC-1..SC-4 demonstrations"
      via: "step-by-step Chromium + DevTools instructions"
      pattern: "SC-[1-4]"
---

<objective>
Lock in Phase 2's verification story: ship a scripts/smoke-wasm-build.sh
gate that every commit touching the crate or build wrapper can run in < 90
seconds, document the manual SC-1..SC-4 demonstrations in www/README.md,
and run those manual demonstrations live at the checkpoint task so the
phase's Success Criteria are demonstrably satisfied before /gsd-verify-phase.

Purpose: VALIDATION.md Manual-Only Verifications and the phase's four
Success Criteria (ROADMAP) require a browser-based demonstration that
cargo test cannot cover. This plan makes those demonstrations reproducible
and documents them so future milestones can re-run them without rediscovering
the procedure. The automated smoke script also guards against wasm-pack
drift in later phases.

Output: scripts/smoke-wasm-build.sh (executable, CI-friendly); www/README.md
(dev-server options + SC-1..SC-4 procedure); a checkpoint:human-verify
task that the author runs live and confirms.
</objective>

<execution_context>
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/workflows/execute-plan.md
@/home/ant/src/microbeast/bestialitty/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-wasm-boundary-minimal-js-harness/02-CONTEXT.md
@.planning/phases/02-wasm-boundary-minimal-js-harness/02-VALIDATION.md
@.planning/phases/02-wasm-boundary-minimal-js-harness/02-04-SUMMARY.md
@scripts/build.sh
@www/index.html
@www/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/smoke-wasm-build.sh (CI-friendly wasm-pack gate)</name>
  <files>scripts/smoke-wasm-build.sh</files>
  <read_first>
    - scripts/build.sh (from Plan 04 — structure template: shebang, set -euo pipefail, cd to repo root)
    - .planning/phases/02-wasm-boundary-minimal-js-harness/02-VALIDATION.md (Wave 0 Gaps line 1079: smoke script requirement)
    - .planning/phases/02-wasm-boundary-minimal-js-harness/02-RESEARCH.md (lines 938-944: Pitfall #6 — --no-opt guidance when wasm-opt isn't on PATH)
  </read_first>
  <action>
Create `scripts/smoke-wasm-build.sh` with the following EXACT content. Use the Write tool (not heredoc):

```
#!/usr/bin/env bash
# scripts/smoke-wasm-build.sh -- CI-friendly gate that wasm-pack still builds.
#
# Distinct from scripts/build.sh:
# - Writes to a throwaway temp dir (not www/pkg/), so it never mutates the
#   dev-loop's working output.
# - Passes --no-opt so wasm-opt is optional (Pitfall #6 -- some CI environments
#   lack Binaryen; Phase 2 does not require optimization for correctness).
# - Verifies every expected output file exists; exits non-zero with a readable
#   diagnostic if any is missing.
#
# Runtime: ~15s warm, ~60s cold (per VALIDATION.md Estimated runtime).
#
# Usage:
#   ./scripts/smoke-wasm-build.sh
#   bash scripts/smoke-wasm-build.sh

set -euo pipefail
cd "$(dirname "$0")/.."

TMPDIR_OUT="$(mktemp -d -t bestialitty-smoke-XXXXXX)"
trap 'rm -rf "$TMPDIR_OUT"' EXIT

echo "[smoke] Building to $TMPDIR_OUT ..."
wasm-pack build crates/bestialitty-core \
    --target web \
    --out-dir "$TMPDIR_OUT" \
    --no-opt

MISSING=()
for f in bestialitty_core.js bestialitty_core_bg.wasm bestialitty_core.d.ts bestialitty_core_bg.wasm.d.ts; do
    if [ ! -f "$TMPDIR_OUT/$f" ]; then
        MISSING+=("$f")
    fi
done

if [ "${#MISSING[@]}" -ne 0 ]; then
    echo "[smoke] FAIL: wasm-pack did not produce expected files:" >&2
    for f in "${MISSING[@]}"; do
        echo "  - $TMPDIR_OUT/$f" >&2
    done
    echo "[smoke] Directory contents:" >&2
    ls -la "$TMPDIR_OUT" >&2 || true
    exit 1
fi

WASM_SIZE="$(wc -c < "$TMPDIR_OUT/bestialitty_core_bg.wasm")"
echo "[smoke] OK: all expected files present."
echo "[smoke]   bestialitty_core_bg.wasm: ${WASM_SIZE} bytes"
echo "[smoke] (Cleaned up $TMPDIR_OUT on exit.)"
```

After writing, make it executable:

```
chmod +x scripts/smoke-wasm-build.sh
```

**Do NOT:**
- Write to `www/pkg/` — that directory belongs to `scripts/build.sh`'s dev loop.
- Skip `--no-opt` — contributors who haven't installed Binaryen must still be able to run this gate.
  </action>
  <verify>
    <automated>bash -c 'test -x /home/ant/src/microbeast/bestialitty/scripts/smoke-wasm-build.sh &amp;&amp; grep -q "wasm-pack build crates/bestialitty-core" /home/ant/src/microbeast/bestialitty/scripts/smoke-wasm-build.sh &amp;&amp; grep -q -- "--no-opt" /home/ant/src/microbeast/bestialitty/scripts/smoke-wasm-build.sh &amp;&amp; /home/ant/src/microbeast/bestialitty/scripts/smoke-wasm-build.sh'</automated>
  </verify>
  <acceptance_criteria>
    - scripts/smoke-wasm-build.sh exists and is executable
    - First line is `#!/usr/bin/env bash`
    - File contains `set -euo pipefail`
    - File contains `wasm-pack build crates/bestialitty-core`
    - File contains `--target web`
    - File contains `--no-opt`
    - File contains `mktemp -d` (uses throwaway dir, does NOT clobber www/pkg/)
    - File verifies presence of `bestialitty_core.js`, `bestialitty_core_bg.wasm`, `bestialitty_core.d.ts`, `bestialitty_core_bg.wasm.d.ts`
    - Running the script exits 0 and prints `[smoke] OK`
    - After the script runs, `git status --porcelain www/pkg/` is unchanged (script uses its own mktemp dir)
  </acceptance_criteria>
  <done>
CI-friendly wasm-pack smoke gate exists and passes. Phase 3+ can invoke it as a pre-merge check without interfering with the dev-loop www/pkg/.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author www/README.md (dev-server options + SC-1..SC-4 verification procedure)</name>
  <files>www/README.md</files>
  <read_first>
    - www/index.html (Plan 04 — boot log expectations, element ids)
    - www/main.js (Plan 04 — the exact `[boot]` and `[SC-4]` log strings to quote verbatim in the README)
    - .planning/phases/02-wasm-boundary-minimal-js-harness/02-VALIDATION.md (lines 60-68: Manual-Only Verifications table — source of truth for SC-1..SC-4 instructions)
    - .planning/phases/02-wasm-boundary-minimal-js-harness/02-CONTEXT.md (D-14 — two dev-server options)
    - .planning/phases/02-wasm-boundary-minimal-js-harness/02-RESEARCH.md (lines 808-819: dev-server options table with install commands)
  </read_first>
  <action>
Create `www/README.md` with EXACTLY the markdown content below. Use the Write tool. The file must reproduce the `[boot]` and `[SC-4]` log lines verbatim so the author can visually diff them against what DevTools actually shows.

The README body to write:

    # BestialiTTY Phase 2 Harness (www/)

    This directory is the static-site deliverable for Phase 2: a minimal
    `index.html` + `main.js` harness that loads the wasm-pack-built Rust core
    and proves the boundary shape end-to-end. Phase 3 replaces the harness with
    the real canvas renderer; until then, this is the dev loop.

    ## Build

    From the repo root:

        ./scripts/build.sh

    This runs `wasm-pack build --target web` and writes the output to
    `www/pkg/` (gitignored). Re-run whenever you touch `crates/bestialitty-core/`.

    If your `wasm-pack` does not have `wasm-opt` on PATH, the build will warn
    but still succeed. To suppress the warning in dev:

        wasm-pack build crates/bestialitty-core --target web --out-dir ../../www/pkg --no-opt

    ## Serve (pick one — both work; per D-14)

    ### Option A — Python 3.12 (no install needed on Linux/macOS)

        python3 -m http.server -d www 8000

    Open http://localhost:8000/ in a Chromium-based browser. Python 3.7.2+
    serves `.wasm` as `application/wasm`, which Chromium's streaming compile
    requires.

    ### Option B — basic-http-server (single Rust binary)

        cargo install basic-http-server    # once
        basic-http-server www              # default port 4000

    Open http://localhost:4000/.

    Do NOT use a Node-based server (`npx serve`, `vite`, etc.) — per D-14
    the dev loop is kept Node-free.

    ## Phase 2 Success Criteria — manual verification

    The four ROADMAP Success Criteria for Phase 2 are browser-demonstrations,
    not cargo tests. After `./scripts/build.sh` and a running server:

    ### SC-1: wasm-pack → loadable pkg/

    1. Open Chromium DevTools Console before loading the page.
    2. Navigate to http://localhost:8000/.
    3. Expect exactly these log lines and NO errors:

           [boot] encode_key_raw(ArrowUp, none) = [27, 65]
           [boot] Harness ready. Terminal= ... wasm.memory= ...

    The `[27, 65]` array is `[ESC, 'A']` — the Rust `encode` function returned
    the VT52 ArrowUp sequence across the wasm boundary.

    ### SC-2: paste → feed() → ASCII pre render

    1. In the textarea, paste: `Hello\x1BY\x21\x20World`
       where `\x1B` = ESC, `Y` = direct-addressing, `\x21\x20` = row 1 col 0
       (the +32 ESC Y offset: row = 0x21 - 0x20 = 1, col = 0x20 - 0x20 = 0).
    2. Click **Feed**.
    3. `<pre id="grid">` shows `Hello` on row 0 and `World` starting at row 1 col 0.
    4. `<span id="status">` shows `cursor=(1,5) bell=false` (after `World`, 5 chars).
    5. `<pre id="dirty">` shows `11000...` — rows 0 and 1 are dirty.

    ### SC-3: zero-copy Uint8Array views

    1. DevTools → Performance tab → Record.
    2. Click Feed 5 times, then Stop.
    3. In the Memory track (or the Summary's Allocation breakdown), the
       per-frame allocation pattern should be flat after the initial view
       construction — no growing heap from `Uint8Array` churn. The harness's
       defensive `reDeriveViews()` allocates two small `Uint8Array` objects per
       render (far less than 1 KB total) which is steady-state, not growing.

    ### SC-4: 64 KB in ONE feed() call

    1. DevTools Console → Clear.
    2. Click **64 KB Stress**.
    3. Expect exactly these log lines per click (ONE log, not 65,536):

           Terminal.feed 64KB: X ms
           [SC-4] Fed 65536 bytes in ONE feed() call
           [SC-4] Elapsed: N ms
           [SC-4] If this log appears ONCE (not 65536 times), SC-4 is satisfied.

    4. For the flame-graph signal: DevTools Performance → Record → click
       **64 KB Stress** once → Stop. Inspect the timeline: a single `Terminal.feed`
       (or `__wbg_feed_*`) frame should appear, lasting a few milliseconds.
       If 65,536 stacked `feed` frames appear, the harness is broken.

    ## Bundle size (informational — Phase 2 deferred concern)

    After `./scripts/build.sh`:

        ls -lh www/pkg/bestialitty_core_bg.wasm

    The wasm output for Phase 2 is typically in the 20–50 KB range (vte parser
    + boundary glue). Size-reduction work is Phase 6's concern.

    ## Troubleshooting

    - `CompileError: Wasm decoding failed`: dev-server is serving `.wasm`
      with the wrong MIME type. Both recommended servers handle this correctly.
    - `TypeError: Cannot perform %TypedArray%.prototype.length on detached
      ArrayBuffer`: a wasm `memory.grow` detached the view. `main.js` already
      calls `reDeriveViews()` on every render as a defensive guard. If this
      still appears, rerun `./scripts/build.sh` and reload.
    - `ReferenceError: Terminal is not defined`: wasm build is out of date
      or `pkg/` is missing. Run `./scripts/build.sh` and hard-refresh (Ctrl-Shift-R).
    - Firefox/Safari: BestialiTTY is Chromium-only (CLAUDE.md hard
      constraint). Phase 6 ships the polite-fail UX; for Phase 2, use a
      Chromium-based browser.

    ## Files in this directory

    | File | Tracked | Role |
    |------|---------|------|
    | `index.html` | yes | Minimal harness page with D-11/D-12 affordances |
    | `main.js` | yes | ES-module driver: init, views, renderers, buttons |
    | `README.md` | yes | This file |
    | `.gitignore` | yes | Ignores `pkg/` |
    | `pkg/` | NO | wasm-pack output, regenerated by `scripts/build.sh` |

Write this as the contents of `www/README.md`. The leading four-space indent in the "README body" above is a display artifact — the actual file contents should start flush-left with `# BestialiTTY Phase 2 Harness (www/)` as line 1. Shell commands can be rendered either as four-space-indented blocks OR as triple-backtick fenced blocks — use four-space indent throughout to avoid any code-fence confusion during writing.

**Do NOT:**
- Document Web Serial, keyboard input, canvas rendering, or any Phase 3+ concern.
- Add npm / Vite / package.json instructions.
- Commit any `pkg/` content — the file table documents that it's not tracked.
  </action>
  <verify>
    <automated>bash -c 'test -f /home/ant/src/microbeast/bestialitty/www/README.md &amp;&amp; grep -q "python3 -m http.server -d www" /home/ant/src/microbeast/bestialitty/www/README.md &amp;&amp; grep -q "basic-http-server www" /home/ant/src/microbeast/bestialitty/www/README.md &amp;&amp; grep -q "SC-1" /home/ant/src/microbeast/bestialitty/www/README.md &amp;&amp; grep -q "SC-2" /home/ant/src/microbeast/bestialitty/www/README.md &amp;&amp; grep -q "SC-3" /home/ant/src/microbeast/bestialitty/www/README.md &amp;&amp; grep -q "SC-4" /home/ant/src/microbeast/bestialitty/www/README.md &amp;&amp; grep -q "Fed 65536 bytes in ONE feed" /home/ant/src/microbeast/bestialitty/www/README.md &amp;&amp; grep -q "Chromium-only" /home/ant/src/microbeast/bestialitty/www/README.md'</automated>
  </verify>
  <acceptance_criteria>
    - www/README.md exists
    - Contains `python3 -m http.server -d www` (Option A)
    - Contains `basic-http-server www` (Option B)
    - Contains `SC-1`, `SC-2`, `SC-3`, `SC-4` section headings (4 matches)
    - Contains the literal string `[boot] encode_key_raw(ArrowUp, none) = [27, 65]` (quote SC-1 boot log verbatim)
    - Contains the literal string `Fed 65536 bytes in ONE feed` (quote SC-4 log line)
    - Contains `Chromium-only` (CLAUDE.md browser constraint)
    - Contains the Files-in-this-directory table with `index.html`, `main.js`, `README.md`, `.gitignore`, `pkg/`
  </acceptance_criteria>
  <done>
www/README.md is the operator's guide to Phase 2 harness verification — dev-server options, SC-1..SC-4 step-by-step, troubleshooting. The next task's checkpoint references these procedures.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Author runs SC-1..SC-4 in Chromium and confirms demonstrations</name>
  <files>(none — human-verify checkpoint, no file modifications)</files>
  <what-built>
    The Phase 2 deliverable is now complete on disk:
    - Plan 01: wasm32 target + wasm-bindgen cfg-gated dep + ADR-002 + updated CORE-02 test
    - Plan 02: pack_buf + snapshot_grid + pack_ptr + pack_byte_len + dirty_ptr on Terminal; unpack_keycode + unpack_mods on key; 11 new unit tests
    - Plan 03: lib.rs wasm-bindgen façade + extended boundary_api_shape.rs
    - Plan 04: scripts/build.sh + www/index.html + www/main.js + .gitignore entries
    - Plan 05 Task 1: scripts/smoke-wasm-build.sh
    - Plan 05 Task 2: www/README.md with step-by-step SC-1..SC-4 instructions

    What remains is the browser-based demonstration that cargo test cannot cover: the four ROADMAP Success Criteria must be visually and log-confirmed in Chromium.
  </what-built>
  <action>
This is a human-verify checkpoint. No automated action is performed. The author runs the SC-1..SC-4 procedure in `how-to-verify` and replies with the resume-signal. The `<action>` element exists here only so the plan-structure validator accepts the task — the real instructions are in `<how-to-verify>` below.
  </action>
  <how-to-verify>
**Pre-flight:**

    cargo test -p bestialitty-core          # expect all tests green (Phase 1 + 2)
    ./scripts/smoke-wasm-build.sh           # expect [smoke] OK
    ./scripts/build.sh                      # expect www/pkg/ populated
    ls -lh www/pkg/bestialitty_core_bg.wasm # record this size for the SUMMARY

**Start the dev server (pick one):**

    python3 -m http.server -d www 8000
    # OR
    basic-http-server www                   # default port 4000

**SC-1 — wasm-pack ES-module loads in Chromium:**
1. Open Chromium, then DevTools (F12 / Ctrl-Shift-I) → Console tab. Clear the console.
2. Navigate to http://localhost:8000/ (or :4000).
3. Expected console output, no red errors:
   - `[boot] encode_key_raw(ArrowUp, none) = [27, 65]`
   - `[boot] Harness ready. Terminal= ... wasm.memory= ...`
4. Confirm the page renders: textarea with placeholder, Feed + 64 KB Stress buttons, two `<pre>` elements, status span.

**SC-2 — paste → feed() → ASCII render:**
1. Paste exactly this into the textarea: `Hello\x1BY\x21\x20World`
2. Click **Feed**.
3. `<pre id="grid">` first line shows `Hello` (followed by spaces), second line shows `World` (starting at col 0).
4. `<span id="status">` shows `cursor=(1,5) bell=false`.
5. `<pre id="dirty">` shows two leading `1`s followed by zeros.

**SC-3 — zero-copy Uint8Array views, no per-frame alloc growth:**
1. DevTools → Performance tab → Record.
2. Click **Feed** 5-10 times.
3. Stop. In the Memory track, confirm flat (no growing heap sawtooth attributable to harness `Uint8Array` churn).

**SC-4 — 64 KB in ONE feed() call:**
1. DevTools Console → Clear.
2. Click **64 KB Stress** once.
3. Expect exactly these four log lines (ONE occurrence each):
   - `Terminal.feed 64KB: X ms`
   - `[SC-4] Fed 65536 bytes in ONE feed() call`
   - `[SC-4] Elapsed: N ms`
   - `[SC-4] If this log appears ONCE (not 65536 times), SC-4 is satisfied.`
4. DevTools Performance → Record → click **64 KB Stress** once more → Stop. Inspect the flame graph: ONE `Terminal.feed` (or `__wbg_feed_*`) frame should appear, not thousands.

**Record for SUMMARY:**
- www/pkg/bestialitty_core_bg.wasm file size (ls -lh output)
- SC-4 elapsed-ms value from the console log
- Any deviation from the expected cursor/dirty readouts (expected: `cursor=(1,5)`, dirty `11000...`)
  </how-to-verify>
  <verify>(human-performed per how-to-verify above; resume-signal is the completion witness)</verify>
  <resume-signal>
Reply with one of:
- **approved** — All four SCs demonstrated; proceed to `/gsd-verify-phase 2`.
- **failed SC-N: description** — SC-N did not demonstrate as expected; include console log / screenshot notes so the root cause can be addressed (likely a harness bug in Plan 04 or a wasm build issue to fix before re-running).
  </resume-signal>
  <done>
Author has replied `approved`; all four SCs visually and log-confirmed; bundle size recorded; any deviations noted in the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Smoke script temp dir → filesystem | mktemp directory created and cleaned up via trap; no persistent mutation |
| README instructions → author operation | Documentation is read-only; author's manual steps are the trust-root for SC confirmation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | smoke-wasm-build.sh clobbering www/pkg/ | mitigate | Script uses `mktemp -d` with a trap-based cleanup; never writes to www/pkg/. Acceptance criterion: git status --porcelain www/pkg/ is unchanged after the smoke run. |
| T-02-05-02 | Denial of Service | smoke-wasm-build.sh leaving temp dirs on crash | mitigate | `trap 'rm -rf "$TMPDIR_OUT"' EXIT` cleans up on any exit path including `set -e` failures. |
| T-02-05-03 | Spoofing | README claims SC passed when it didn't | mitigate | The checkpoint task requires the author to copy-paste the actual console log lines into the SUMMARY. The log strings are quoted verbatim in README so visual diff is trivial. Silent pass is not possible because the author enters the resume-signal. |
| T-02-05-04 | Information Disclosure | Bundle size leaking in the SUMMARY | accept | The wasm binary size is a public project metric; no secrets involved. |
</threat_model>

<verification>
```
# Plan 05 completion gates:
./scripts/smoke-wasm-build.sh            # [smoke] OK
test -f www/README.md && grep -c '^## SC-' www/README.md   # expect 4 (SC-1..SC-4 sections)
# Plus the human checkpoint's four manual confirmations
```
</verification>

<success_criteria>
- scripts/smoke-wasm-build.sh exists, is executable, exits 0
- www/README.md documents dev-server options + SC-1..SC-4 procedures
- Author has demonstrated SC-1 (wasm-pack load), SC-2 (paste → render), SC-3 (zero-copy), SC-4 (one feed call for 64 KB) in Chromium and replied approved
- Informational bundle-size recorded in the plan SUMMARY
</success_criteria>

<output>
After completion, create `.planning/phases/02-wasm-boundary-minimal-js-harness/02-05-SUMMARY.md`
including:
- `ls -lh www/pkg/bestialitty_core_bg.wasm` output
- The verbatim SC-4 console log from the author's run
- Confirmation lines for each of SC-1, SC-2, SC-3, SC-4
</output>
